# Handoff: C10 (§6 fine_scale_mixing) attacked — Parseval brick + decomposition landed

**Date**: 2026-07-15 ~00:00 UTC · **Branch**: `main` · HEAD = `d7aa6bf`, `lake build` green
(3283 jobs), tree clean. **Re-read `DIRECTION.md` first each lap — it outranks this handoff.**

## 🎯 What happened this run (three green commits)
Objectives 1 (§7 two sorries) and 2 (X10/X10a repair) are BOTH confirmed complete (no §7
sorries; `triangle_encounter_le`/`encounter_apex_proximity` restored beside their `_rpow`
engines). With those + all SyracRV done, the headline now rests on the two HEROIC analytic
nodes; dependency order (`… → C10 → C9 → C6`) makes **C10 (§6) upstream of C9 (§5)**, so this
run attacked C10.

1. `e7785f7` — **Lemma 1.12 `syracZ_recursion` PROVED** (the ZMod fiber crux) → `Syracuse/
   SyracRV.lean` fully sorry-free & axiom-clean. (Details: HANDOFF-2026-07-14-2300.)
2. `2f083d0` — **`ZMod.dft_parseval` PROVED** (node S4) — `Fourier/Parseval.lean`. Complex +
   real `L²` Parseval for mathlib's `ZMod.dft` (which lacked it), from `stdAddChar`
   orthogonality. Axiom-clean. The upstream prerequisite for C10's Plancherel step.
3. `d7aa6bf` — **C10 `fine_scale_mixing` DECOMPOSED** — `Sec6/MixingFromDecay.lean`. Prop 1.14
   now PROVED from two named sub-lemmas; the reduction is machine-checked.

## 🧩 C10 state — `fine_scale_mixing` reduced to two sub-lemmas (both `sorry`)
- **`osc_le_sqrt_highfreq`** — `osc m n hmn (dens) ≤ √(∑_{highFreq m n} ‖𝓕(densC n) ξ‖²)`.
  Cauchy–Schwarz + `ZMod.dft_parseval`. Needs the "3ᵐ-conditional-average = low-freq
  inverse-DFT" identity (relate `osc`'s castHom-fiber average to `∑_{low ξ} 𝓕(densC)(ξ)e(ξ·/3ⁿ)`),
  then Finset Cauchy–Schwarz, then Parseval.
- **`highfreq_l2_le`** — `∑_{highFreq m n} ‖𝓕(densC n) ξ‖² ≤ C·m^{-A}` (∀A>0). From Prop 1.17
  `charFn_decay` (PROVED, axiom-clean) via the `syracZ_map_cast` projection `ĉ_n(3ʲη)=ĉ_{n-j}(η)`
  + per-level Parseval count + geometric sum. Needs the cexpect↔dft sign bridge.
- **Reduction PROVED**: `fine_scale_mixing A` calls `highfreq_l2_le (2A)` so the `√` restores
  `m^{-A}`. New defs: `densC n` (ℂ density), `highFreq m n` (`¬3^{n-m}∣ξ.val` modes).

## 📋 Remaining non-headline sorries (whole repo)
- `Sec6/MixingFromDecay.lean` `osc_le_sqrt_highfreq`, `highfreq_l2_le` — the two C10 pieces
  (genuine §6 analytic content; attack next, route in PENDING_WORK fruit-7).
- `Sec5/FirstPassage.lean:81` `stabilization` (Prop 1.11, C9) — HEROIC, consumes C10; narrow only.
- `Statement.lean:22,28` — the two headlines (DO NOT TOUCH).

## ⚠️ Gotchas banked this run
- Mathlib `ZMod.dft` (`Mathlib/Analysis/Fourier/ZMod.lean`) has DFT + inversion `dft_dft`
  but NO Parseval — use the new `ZMod.dft_parseval`/`dft_parseval_complex` in `Fourier/Parseval`.
- Char orthogonality: `AddChar.sum_eq_zero_of_ne_one (isPrimitive_stdAddChar N h)` +
  `AddChar.map_neg_eq_conj` (`stdAddChar(-x)=conj(stdAddChar x)`) + `AddChar.map_add_eq_mul`.
- `Complex.sq_norm z : ‖z‖² = normSq z`; `((‖z‖²:ℝ):ℂ) = z*conj z` via `Complex.mul_conj`+`sq_norm`.
- `rw [pow_add]`/`pow_mul` greedily rewrite `3^(n+1)` inside `x.val`'s TYPE → "motive not type
  correct". Give explicit args (`pow_add (2:ZMod _) a P`) or `generalize C.val = v` first.

## 📁 Key files
- `TaoCollatz/Fourier/Parseval.lean` — NEW (node S4); `ZMod.dft_parseval` (+complex).
- `TaoCollatz/Sec6/MixingFromDecay.lean` — C10 decomposition; 2 sub-sorries + proved reduction.
- `TaoCollatz/Syracuse/SyracRV.lean` — sorry-free (Lemma 1.12 done).
- `PENDING_WORK.md` — fruit-7 (newest): full C10 route + next-lap attack on both sub-lemmas.

---
**→ Next session: discharge the two C10 sub-lemmas in `Sec6/MixingFromDecay.lean`
(`osc_le_sqrt_highfreq` then `highfreq_l2_le`) — every input (Parseval, charFn_decay,
syracZ_map_cast) is in hand. Then C9 `stabilization`. Re-read `DIRECTION.md` first.**
