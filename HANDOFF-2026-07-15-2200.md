# Handoff: C10 assembly — k-sum cast CRACKED, high-regime DECOMPOSED, `hbudget` DISCHARGED

**Real date**: 2026-07-14 (filename follows the sequence's drifted clock so it sorts newest).
**Branch**: `main`, HEAD `1dc227a`, build 🟢 green (3285 jobs), tree clean. **Sorry census 6**
(2 `Statement.lean` headlines + C9 `stabilization` + C10's `osc_mainHigh_bound` +
`error_l1_high_bound` + `osc_syracZ_regime_telescope`). `fine_scale_mixing` and
`osc_syracZ_high_regime` are both now **sorry-free**. Differ 27/29 char-identical (both watched
crux statements untouched). All new lemmas `[propext, Classical.choice, Quot.sound]`.

## Judge pass 28 landed this run — read `DIRECTION.md` "🚨 JUDGE PASS 28" + `judge/pass-28.md`
Binding: discharge `hbudget` from the **TIGHT** window (never (6.8) — its ½-window has a *negative*
C² coeff, no C_A rescues it); **C_A ≥ 23** (fixed `caConst = 30`); ⚠️ **SHOW** the A′-absorption of
`n^{O(C_A²)}` at C_A=30 when wiring obl-2, do not assert it. Alt allowed: re-prove the kernel at
ε=1/4 (unwatched internal lemma) → threshold back to ≳10.

## What landed this run (3 green commits: `d24618b`, `8a6b7be`, + docs)
1. **The k-sum dependent-index cast `(n−1−k)+(k+1)=n` — DISCHARGED** (the "main new friction" for
   3 laps). `cutEq`/`osc_cast`/`osc_cast'`/`castedTerm`/`osc_castedTerm`/`mainDensity`/
   `osc_mainDensity_le`. KEY: the k-varying cut lives on a different `ZMod(3^…)`, but its
   *oscillation is a real number* moved losslessly to level n. GOTCHAS banked: state the cast lemma
   with FREE vars ⇒ `subst` collapses the `Eq.rec`; and wrap the `▸` in its own `def` (`castedTerm`)
   so it stays opaque to the `osc_sum_le` unifier (a raw `▸` under the sum whnf-blows into
   `condDensW`'s `tsum`); name the eq lemma so the proof term is syntactically stable.
2. **`osc_syracZ_high_regime` DECOMPOSED** (was one opaque sorry → PROVED): rests on
   `osc_syracZ_split_le` (proved: `osc(syracZ) ≤ osc(main) + 2‖err‖_{L¹}`) applied to `mainHigh`,
   leaving two named obligations `osc_mainHigh_bound` + `error_l1_high_bound`.
3. **`hbudget` DISCHARGED** — `lRange_hbudget` (judge tripwire #1): for `l ∈ lRange caConst n`,
   `l·log2 + (C log2 + 5/4(C log2)²)log n + log4 < n log3` at C_A=30. The `log n` coeff
   `L(1125L−810) < 0` (L=log2<0.72) is where C_A≥23 bites. Only deferred: the clean `hwin` hyp
   (window non-degeneracy `hi ≥ 0` = standard `n/log n → ∞` threshold; caller supplies via n₀).

Concrete §6 objects now defined: `caConst`(=30), `caThr`(6.6), `lRange`(tight (6.8) per pass 28),
`mainHigh`, `mainDensity`.

## → Next (hardest-first): finish `osc_mainHigh_bound` (`Sec6/MixingFromDecay.lean`)
The frontier. `osc_mainDensity_le` (DONE) reduces it to per-cut `condDensWB_osc_le` (DONE); the
`hbudget` input is `lRange_hbudget` (DONE). What remains, in order:
1. **`hunif` (obl 2)** — the HARD one, route-decisive valuation bookkeeping: connect
   `ξ ∈ highFreq m (j+p)` (i.e. 3-adic val `< n−m ≤ 0.1n` in the high regime) to a reduced freq `η`
   at residual level `q ≥ ~n/10` with `¬3∣η.val`, so `head_factor_norm_le_charFn` (PROVED) gives
   `‖head‖ ≤ C_A·q⁻ᴬ'`. ⚠️ **A′-absorption SHOW**: use head decay at `A' = A + C_A²·log2 (≈A+624)`
   because `√(3ⁿ·2⁻ˡ) = 3^{n/2}2^{-l/2} ≤ n^{C²log2/2}` (≈n^312) for `l ≥ lRange` lower endpoint;
   the product `C_A·q⁻ᴬ'·n^{C²log2/2} ≤ C·n^{-A}` (q≈n/10). `charFn_decay` holds for every A'.
2. **geometric l-sum** `∑_{l∈lRange} √(2⁻ˡ) = ∑(1/√2)^l ≤ (1/√2)^{l_lo}/(1−1/√2)` — pure, provable
   (mathlib `tsum_geometric_of_lt_one` / `geom_sum_eq`; I was mid-writing `geom_pow_Icc_le`).
3. **k-count** (≤ n terms) + **constant chase** + the `hwin`/`n/log n` threshold for lRange_hbudget
   (provable via `log n ≤ 2√n`, needs `n ≥ ~1.21e6` and a `log 3 > 1.05` lower bound).
Then `error_l1_high_bound` (obl 1: (6.3) P(Ē) + (6.4) E→Eₖ via S3 sub-Gaussian tails; also where the
event-partition correctness of `mainHigh` vs `syracZ` must be shown) and `osc_syracZ_regime_telescope`
(obl 0). Full attack + gotchas: PENDING_WORK top two entries.

**Do NOT** touch watched statements (`fine_scale_mixing`/`stabilization`) or any ratified pin.
