# HANDOFF — 2026-07-12-k (lap 56: review + WHITE-EXIT KERNEL largely PROVED)

**Branch** `main`, HEAD `2771a60`, `lake build` green (3280 jobs). Lap 56 =
review (`2d9747c`) → [judge passes 15/16 ran here] → three proof commits
(`389048d`, `5134282`, `2771a60`). The white-exit kernel `fpDist_white_exit_deep`
(X9's only open input, shared with X8's Case-2 twin) went from a monolithic sorry
to a fully-structured proof resting on **two isolated analytic sorries**.

**Uncommitted (NOT mine, leave for host)**: `.obsidian/workspace.json`.

## ⚠️ READ FIRST — top next-lap item (JUDGE PASS 16 DEMAND, cheap + pre-authorized)

Judge pass 16 (`judge/pass-16.md`) demands: **strengthen `fpDist_white_exit_deep`'s
mass pin from `1/2 < p₀` to `51/100 ≤ p₀`** (explicit margin). Reason: the consumer
`many_triangles_white` sets `ε₀ = min(1/100, (2p₁−1)/2)`; X11 instantiates at the
fixed `ε = epsBW = 10⁻⁴`, so it needs `ε₀ ≥ 10⁻⁴ ⟺ p₁ ≥ 1/2 + 10⁻⁴`, which bare
`1/2 < p₀` does NOT certify.

**This is now a 5-minute edit** because I PROVED `fpDist_white_exit_deep` this lap
with `p₀ = 3/4` (≥ 51/100 with room): change the statement's `1 / 2 < p₀` to
`51 / 100 ≤ p₀` (and the `refine ⟨3 / 4, by norm_num, …⟩` still closes it —
`by norm_num` proves `51/100 ≤ 3/4`). Then rebuild and confirm `many_triangles_white`
still compiles and its ε₀-floor obligation discharges (`ε₀ ≥ min(1/100,1/100) =
1/100 ≥ 10⁻⁴`). Judge says this is **ratification-preserving** (pure strengthening
of the same (7.59) content) — re-check is just the character diff + numeral
arithmetic. DO THIS FIRST next lap.

## What I proved this lap (all axiom-clean glue; sorryAx only via the two tails)

`fpDist_white_exit_deep` (`Sec7/ManyTriangles.lean`) is PROVED via a clean
(7.50)-geometry decomposition:
- **`endpoint_notMem_start_triangle`** (PROVED, axiom-clean): overshoot
  (`fpDist_support_snd_gt`: `s<e.2`) clears the start-triangle apex height ⟹ the
  non-white mass is FOREIGN-triangle only.
- **`outStripSet` / `phaseInFamily`** + reduction glue (axiom-clean): the white
  strip complement splits via `white=¬black` + `F.cover` into out-of-strip +
  phase-in-some-triangle; pointwise `1_W ≥ 1 − 1_out − 1_tri`, summed via
  `Summable.tsum_sub` + `fpDist_tsum_toReal` ⟹ `p₀ = 3/4` from two `≤1/8` tails.
- **`hasSum_int_shift_exp`** (PROVED): support-shifted exp over ℤ = geometric.
- **`fpDist_col_le`** (PROVED): X6 `fpDist_location_bound` collapsed over the
  height `l` (support `l>s` kills `e^{-c(l-s)}`) ⟹ per-column Gaussian bound.
- **`fpDist_out_of_strip_le`** (PROVED): Fubini (`Summable.tsum_prod'` + fiber
  summability via `comp_injective`) → column marginals ≤ `fpDist_col_le` → budget
  cast from `budget_le_of_mem_triangle`. Depends only on `gaussian_col_tail`.

## Open kernel sorries (the two white-exit tails)

1. **`gaussian_col_tail`** (`ManyTriangles.lean:~1547`) — pure real-analysis:
   `∑_{j>m} C'·Gweight(1+s, c(j-s/4))/√(1+s) ≤ 1/8` for `m ≥ Cthr` under budget
   `s·log2 ≤ (m+2)·log9`. Split `Gweight = exp(-x²/t)+exp(-|x|)`: `exp(-|x|)` part
   geometric in `j` (ℕ analogue of `hasSum_int_shift_exp`); `exp(-x²/t)` part via
   `exp(-x²/t) ≤ exp(-x₀·x/t)` (`x²≥x₀·x` on tail `x≥x₀=m+1-s/4>0`) then geometric;
   each `≤1/16` for `Cthr` large (gap `x₀ ≥ ~0.2m → ∞`). Finite-range analogues:
   `sum_range_exp_neg_sq_le`, `sum_exp_geom_le` in `FpLocation`. **Mechanical.**
2. **`fpDist_any_triangle_le`** (`ManyTriangles.lean:~1632`) — the SEPARATION FIGHT
   (the genuinely hard half): foreign-triangle mass ≤ 1/8 via `apex_separation` +
   the (7.11) slope band confining the Gaussian-concentrated endpoint to an O(1)
   slab about the start diagonal; `endpoint_notMem_start_triangle` (proved) gives
   the start triangle is empty. Reuse `fpDist_col_le` for column-wise decay.

## All open src crux sorries (7: BlackEdge ×4, ManyTriangles ×3)
- ManyTriangles: `triangle_encounter_le` (X10, :286), `gaussian_col_tail` (:1547),
  `fpDist_any_triangle_le` (:1632).
- BlackEdge: `fpDist_edgeWeight_le` (:217), `fpDist_white_exit` (:245, X8 twin —
  DERIVE from `fpDist_white_exit_deep`, same conclusion + unused budget hyp),
  `Q_black_edge_case2` (:333), `Q_black_edge_case3` (:355).
- Plus 13 deliberate spine stubs (frozen by directive).

## NEXT (priority order)
1. **The 51/100 pin strengthening** (see READ FIRST) — cheap, discharges a live
   judge demand, certifies the ε₀-floor.
2. **`gaussian_col_tail`** — closes `fpDist_out_of_strip_le` end-to-end (mechanical
   Gaussian/geometric arithmetic; route above).
3. **`fpDist_any_triangle_le`** — the separation crux (hardest; `apex_separation`
   + slope band). Closing both tails ⟹ `fpDist_white_exit_deep` axiom-clean ⟹ X9
   (`many_triangles_white`) COMPLETES end-to-end.
4. **Derive `fpDist_white_exit`** (X8 twin) from the deep version, then X10
   (`triangle_encounter_le`) / X8 assembly.

## Gotchas this lap
- `Summable.tsum_prod'` takes TWO args: `Summable f` AND `∀ b, Summable (fun c =>
  f (b,c))`. Pass the fiber via `hgsum.comp_injective (fun c1 c2 h => by simpa
  using h)`. After the `rw`, the goal carries `(b,c).1`; normalise with a `show …
  (if m < a …)` (defeq) before the final `exact`, else the `tsum` function
  comparison won't reduce the projection.
- `exp(-c)·(1-exp(-c))⁻¹` → `exp(-c)/(1-exp(-c))` via `← div_eq_mul_inv` (NOT
  `mul_one_div`, which wants `_ * (1/_)`).
- `#print axioms` needs the fully-qualified `TaoCollatz.<name>`.
