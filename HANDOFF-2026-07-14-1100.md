# Handoff: 🏆 §7 MONOTONICITY COMPLETE (`prop_7_8` axiom-clean) + Collatz (1.2) closed

**Date**: 2026-07-14 ~11:00 UTC · **Branch**: `main` · HEAD = `539d359`, `lake build` green
(3282 jobs), tree clean. **Re-read `DIRECTION.md` first each lap — it outranks this handoff.**

## 🎯 What happened this lap (a landmark run)
The campaign's central crux — the §7 pinnacle (X8/X10/X11, the 65–75% risk concentration) — is
**DONE**. Four green commits:
1. `c67500d` — **assembled `few_white_mass_le` (7.56)**, the deepest §7 leaf, from its three proved
   terms (reach / E∗ / bad-column) + `few_white_pointwise_split`. Case3 sorries 2→1.
2. `b0ea748` — **proved `col_tail_mass_le` (7.54 bad-column Gaussian tail)** → the whole §7 spine
   `col_tail_mass_le → few_white_mass_le → Q_black_edge_case3 → prop_7_8` goes **axiom-clean**
   (`[propext, Classical.choice, Quot.sound]`, NO `sorryAx`; verified via `#print axioms`).
   **`Case3.lean` is SORRY-FREE.**
3. `eb2f831` — PENDING doc: milestone + pivot to objective 3.
4. `539d359` — **proved `colMin_eq_syrMin_oddPart` (paper 1.2)** in `Basic/Collatz.lean` (axiom-clean),
   the Collatz→Syracuse reformulation the whole reduction rests on. Via two structural facts
   (`col_reaches_syr`, `oddPart_col_iterate`) + `sInf` monotonicity; 8 new axiom-clean helpers.

## 🧠 Context to carry forward
- **§7 is the spine and it is complete.** `prop_7_8` no longer carries `sorryAx`. This retires the
  campaign's hardest risk. The remaining work is downstream reduction + foundational stubs (objective 3).
- New reusable helper `exp_neg_mul_le_rpow_neg` (`Case3.lean`, before `col_tail_mass_le`): super-exp
  beats polynomial, `exp(−ρm) ≤ δ·m^{−A}` for large m. Extracted from `hold_fst_tail_le`'s `hclose`.
- `col_tail` route (STANDARD, now proved): walk→`fpDistPlus` marginal (`fpDist_walk_eq_fpDistPlus`,
  p=P) → containment `{0.9m ≤ x.1} ⊆ {2D ≤ |x.1−s/4|}` (D=m/40) via budget `s < 3.2(m+2)`
  (from `hs2` + `log9 < 3.2·log2` via `9^5 < 2^16`) → `fpDistPlus_col_tail` exp tail crushed by the
  new helper → `≤ m^{−A}/2`.

## 🎬 Next actions (DIRECTION objective 3 — the fruit; §7 done so this IS the order now)
1. **`Syracuse/SyracRV.lean`** (3 sorries: `syracZ_map_cast`, `syracZ_recursion`, `syracZ_eq_rev_fnat`)
   — foundational Syracuse-RV identities, likely the next tractable on-path targets. START HERE.
2. **`Sec5/FirstPassage.lean`**: `logUnifOdd` normalization sorry (line ~42) — CHEAP but needs a
   `dite` refactor to bring the `Nonempty` hyp into scope (the `if…then PMF.ofFinset…(by sorry)`
   branch has no proof of nonemptiness in scope; convert the outer `if` to `if h : …`). `stabilization`
   (Prop 1.11) itself is HEROIC analytic (multi-lap) — narrow, don't expect to close.
3. **`Sec6/MixingFromDecay.lean`** `fine_scale_mixing` — HEROIC analytic (§6), multi-lap.
4. `Sec7/White.lean`, `Sec7/Reduction.lean`, `Sec7/BlackEdgeQ.lean`, `Prob/Basic.lean` — each carries
   a sorry; inventory which are on-path (check `#print axioms` reachability from the reduction chain).
5. **`ManyTriangles.lean` split** (5,519 lines, zero-risk hygiene) — DIRECTION obj-3 item 1, queued
   many laps. Pure moves + thin re-export shim. Do it if math targets thin out.
6. **Pin C8** (§5 first-passage) — mark `RATIFY-C8` in a comment, say so in handoff, never `\leanok`.

## 📁 Key files
- `TaoCollatz/Sec7/Case3.lean` — SORRY-FREE. `few_white_mass_le`, `col_tail_mass_le`,
  `exp_neg_mul_le_rpow_neg` (helper), `Q_black_edge_case3` all axiom-clean.
- `TaoCollatz/Basic/Collatz.lean` — `colMin_eq_syrMin_oddPart` (1.2) + 8 helpers, all axiom-clean.
- `DIRECTION.md` — judge pass 26 directive (authoritative; objective 3 is now the active order).
- `PENDING_WORK.md` — lap records (newest: fruit-1 Collatz, X11d-DONE §7 complete).
- `STATUS.md` — STALE (still says X11 open); refresh next lap to reflect §7 complete.

## ⚠️ Gotchas (this lap)
- `Function.iterate_succ_apply'` in `rw [←…]` fails with a metavariable pattern — use the explicit
  term `(Function.iterate_succ_apply' f n x).symm` instead.
- `Set.range` membership goals `(fun k => …) k = a` aren't auto-beta-reduced for `rw` — `show` the
  reduced form first.
- `padicValNat_self` needs `haveI : Fact (Nat.Prime 2)`; `padicValNat.mul` takes `a≠0 → b≠0 →`.

---
**→ Next session: §7 (the spine) is DONE and axiom-clean. Pick up at objective 3 — start with the
`SyracRV.lean` foundational stubs (Next action #1). Re-read `DIRECTION.md` first.**
