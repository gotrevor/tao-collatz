# HANDOFF — 2026-07-14-b (X9 white-exit kernel CLOSED; X8 crux decomposed + core proved)

**Branch** `main`, HEAD = `9e15ca3`, `lake build` green (3281 jobs), working tree
clean. **Re-read `DIRECTION.md` CURRENT DIRECTIVE FIRST** — it outranks this handoff.
Directive steps 1–4 (make `B`,`Y` explicit → close X9 kernel) are **DONE**; the
directive's fallback clause routes remaining work to the BlackEdge X8 crux, which
this lap has been decomposing and proving.

## What landed this lap (all axiom-clean = `[propext, Classical.choice, Quot.sound]`)

### ✅ HEADLINE — X9 white-exit kernel CLOSED (`94444b9`)
`fpDist_any_triangle_le` (`ManyTriangles.lean`) **PROVED** — the last route-decisive
blocker on Prop 1.17's Case-3 chain. Discharges `fpDist_white_exit_deep` (X9's only
open external input). Both axiom-clean. Wired the sharp explicit constants `B=64`,
`Y=150` into the localization box (`40000000→64` — symbolic, cancels in the facewidth
`nlinarith`; existential `Y`→numeral 150 via the sharp leaves), added
`sep_const_gt_two_hundred` (`Triangles.lean`: `sep=100·log10>200`), closed
`hsepXY: 51²+150²=25101 < 200² < sep²`. Moved the three box lemmas above their consumer
(forward-ref fix). **This completes the entire judge-pass-24 directive.**

### ✅ X8 crux `fpDist_edgeWeight_le` — decomposed, unblocked, analytic core proved
The (7.48) weight-degradation crux (`BlackEdge.lean`, still `sorry`) is now a fully
routed decomposition:
- **`edgeWeight_summand_le` PROVED** (`2b422d1`): uniform pointwise weight bound
  `max(m−J,1)^{−A} ≤ m^{−A}·exp(2A·J/m) + 1_{m<2J}` (`J=e₁+d₁`) — collapses the double
  sum with NO Fubini barrier (main region via concavity core `one_sub_rpow_neg_le_exp`,
  tail via weight ≤ 1).
- **`fpDist_fst_mgf_le`** added as named src sub-goal (`6253abb`, `sorry`): the
  first-coord fpDist MGF `∑_e fpDist·exp(2A e.1/m) ≤ 1+δ`. Both the main MGF factor and
  the tail of `fpDist_edgeWeight_le` reduce to it.
- **Module re-layering** (`7cede57`,`21b0e0c`,`0c72d6f`): the X8 lemmas need X6
  (`fpDist_location_bound`) + the column marginal + `Gweight` toolbox, which lived
  DOWNSTREAM. Verified the Sec7 import DAG is acyclic and **added `import
  TaoCollatz.Sec7.FpLocation` to `BlackEdge`**; moved `fpDist_col_le`,
  `hasSum_int_shift_exp`, `hasSum_nat_tail_exp` UP to `FpLocation`. All three X8
  BlackEdge sorries are now unblocked. (Pure moves; existing ManyTriangles consumers
  still resolve.)
- **`gaussExp_col_tail` PROVED** (`0a26b44`, `FpLocation.lean`, axiom-clean): the
  Gaussian×growing-exp column tail — the analytic MEAT of `fpDist_fst_mgf_le`. The tilt
  `e^{θj}` folds into the shifted geometrics exactly, so it's `gaussian_col_tail` with
  decay rates `γ₂,c ↦ γ₂−θ, c−θ` + a bounded prefactor `e^{θs/4}`. Hyps: `0≤θ≤½min(c,c²/20)`,
  `m≥25`, budget `s·log2 ≤ (m+2)·log9`.

## NEXT — assemble `fpDist_fst_mgf_le`, then glue `fpDist_edgeWeight_le` (PURE ASSEMBLY, no new analysis)
Full plan in `PENDING_WORK.md` (top entry). In brief:
1. **`fpDist_fst_mgf_le`** (`BlackEdge.lean:~449`): Fubini 2D→1D (`tsum_prod'` + `tsum_mul_left`,
   `exp(θj)` const in `l`) → `∑'_j exp(θj)·M(j)`, `M(j) := ∑'_l fpDist(s,(j,l)).toReal ≤
   fpDist_col_le`, `∑'_j M(j) ≤ 1`. Split at `K` (`θ=2A/m`, `θK ≤ log(1+δ/2)`): finite bulk
   `∑_{j≤K} exp(θj)M(j) ≤ exp(θK)·1 ≤ 1+δ/2`; tail via `gaussExp_col_tail` (its RHS → 0 as
   `m→∞`). Pick `Cthr ≥ 25` big enough that `θ=2A/m ≤ ½min(c,c²/20)` and gaussExp RHS ≤ δ/2.
   Derive the `s·log2 ≤ (m+2)·log9` budget from the `s ≤ m/log²m` hypothesis (log²m room).
2. **`fpDist_edgeWeight_le`**: sum `edgeWeight_summand_le` over `d` (hold MGF `tiltZ_hold_fst_le`,
   → 1) then over `e` with `fpDist` (`fpDist_fst_mgf_le` for the `e.1` factor); tail =
   `P(e.1+d.1>m/2)` via a Chernoff of `fpDist_fst_mgf_le` (`e.1>m/4`) + hold Chernoff
   (`holdSum_halfspace_le`, `d.1>m/4`). `Cthr = max` of the region thresholds; `(1+δ/2)+(δ/2)=1+δ`.
3. Then `fpDist_white_exit` / `Q_black_edge_case2` (X8 Case-2) follow (now that FpLocation is
   imported, the same geometry as `fpDist_white_exit_deep` is available — derive by citing it
   or reusing the decomposition). Then `Q_black_edge_case3_assembled` (X11d, `Case3.lean`).

## Open sorries (all X8/X11 Case-2/3; the spine leaves stay open by directive)
- `BlackEdge.lean`: `fpDist_fst_mgf_le`, `fpDist_edgeWeight_le`, `fpDist_white_exit`, `Q_black_edge_case2`.
- `Case3.lean`: `Q_black_edge_case3_assembled` (X11d).
- Spine stubs (`Statement.lean`, `Basic/Collatz.lean`): downstream, do NOT touch (directive).

## Gotchas this lap
- BlackEdge is UPSTREAM of the fp-concentration machinery; fixed by importing FpLocation
  (DAG is acyclic — FpLocation only pulls HoldLocal+Mgf+LocalInstances). A `#check` right
  after editing an import can give a stale-olean false negative; rebuild first.
- Tilt-fold exponent identities (`θj−γ₂X = θs/4−(γ₂−θ)X` with `X=j−s/4`) are NOT free ring
  identities — must expand `X`'s definition (`rw [hX]`) before `ring`.
- `exponentiation.threshold 3000` still set in White/Triangles/BlackEdge/ManyTriangles.
