# HANDOFF — effective-constants campaign, lap 0 (host-staged 2026-07-16)

Fresh campaign; no prior lap. **Read `DIRECTION.md` first — it is the directive.**
Then `PENDING_WORK.md` for the step ledger.

State you inherit:
- Branch `explicit-rate-hold-weight` (worktree `~/src/tao-collatz-explicit-c`), PR #8 open.
- `hold_weight_expect`'s rate-free witnesses are already fixed on this branch (`b32c0bb`);
  build green (3327 jobs), axiom gate exact on both headlines. Not your problem — context only.
- Your work: DIRECTION step 1 (trace + lower-bound the `c8`/`cs` branches; route-decisive,
  ends in a JUDGE-FLAG for operator sign-off on the `cTao` value). Step 2's sibling+delegate
  carriers may be worked in parallel — they do not depend on the step-1 value.
- Hard rails in DIRECTION bind, especially: never edit a ratified statement (sibling +
  delegate only), `c` slots only, lower bounds only, everything symbolic.

Leave your own `HANDOFF.md` (overwrite this one) + update `PENDING_WORK.md` when your
budget ends.
