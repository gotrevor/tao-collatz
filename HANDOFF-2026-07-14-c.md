# HANDOFF — 2026-07-14-c (X8 first-coord MGF CLOSED; `fpDist_edgeWeight_le` decomposed)

**Branch** `main`, HEAD = `c393c48`, `lake build` green (3281 jobs), working tree
clean. **Re-read `DIRECTION.md` CURRENT DIRECTIVE FIRST** — it outranks this handoff.
The X9 kernel (judge-pass-24 directive steps 1–4) is DONE; the directive's fallback
clause routes remaining work to the BlackEdge X8 crux sorries, which this lap advanced.

## What landed this session (three green commits, all axiom-clean where proved)

### ✅ `fpDist_fst_mgf_le` FULLY PROVED (`19ea98d` + `e96c2e7`)
The genuinely-new analytic input of the (7.48) X8 crux — the first-coordinate `fpDist`
MGF `∑_e fpDist(s,e)·exp(2A·e₁/m) ≤ 1+δ` for `m ≥ C`. `#print axioms =
[propext, Classical.choice, Quot.sound]` (no `sorryAx`). Two-part landing:
- **Mechanical spine** (`19ea98d`): Tonelli 2D-summability (`summable_prod_of_nonneg`),
  the `e^{θK}`-capped bulk + `gaussExp_col_tail` tail envelope `U`, `g ≤ U` pointwise,
  Fubini collapse (`Summable.tsum_prod'`), bulk `≤ 1+δ/2` via marginal mass `∑_j M(j)=1`,
  tail `≤ δ/2`. Template = `fpDist_out_of_strip_le` (`ManyTriangles.lean:1919`).
- **Numeric core `fpDist_fst_mgf_numeric`** (`e96c2e7`): `Cthr = 25+N₁+N₃+N₈₅+N₄`,
  split `K = ⌊mL/(2A)⌋` (`L=log(1+δ/2)`), five estimates E1–E4. Rests on two new
  reusable helpers **`log_sq_ge_of_large`** (`∀b ∃N ∀m≥N, b≤log²m`) and
  **`exp_neg_mul_le_of_large`** (`exp(-ρm)` below any pos bound for m large). Uses
  `maxHeartbeats 4000000`.

### ✅ `hold_fst_mgf_le_real` PROVED (`c393c48`)
ℝ-valued first-coord `Hold` MGF `∑_d hold(d)·exp(θd₁) ≤ 1+4θ+32θ²` for `|θ|≤1/100`
— the `Z_hold` factor; bridges `ℝ≥0∞` `tiltZ_hold_fst_le` → ℝ via `ENNReal.tsum_toReal_eq`
(NOTE: its direction is `(∑f).toReal = ∑(f).toReal` — needs `.symm`) + `toReal_mono`.

### 🧭 ROUTE CORRECTION on `fpDist_edgeWeight_le`'s tail (`c393c48`)
Prior handoffs claimed the tail `P(e₁+d₁>m/2) ≤ (δ/2)m^{−A}` is "a Chernoff of
`fpDist_fst_mgf_le`". **FALSE** — the `2A/m` tilt gives the non-decaying `e^{−A/2}`,
but `m^{−A}→0` needs genuine decay. The tail requires a **FIXED-tilt** (`θ₀=Θ(1)`)
Chernoff = genuine new analytic input, not pure glue. Recorded in docstrings + PENDING.

## Open sorries in `BlackEdge.lean` (X8) and the decomposition
`fpDist_edgeWeight_le` (the (7.48) glue) now reduces to FOUR named src sub-lemmas:
- `fpDist_fst_mgf_le` — MGF factor `Z_fp(2A/m)`. **✓ PROVED.**
- `hold_fst_mgf_le_real` — MGF factor `Z_hold(2A/m)`. **✓ PROVED.**
- `fpDist_fst_tail_le` — `∑_e fpDist·1_{m<4e₁} ≤ δ·m^{−A}`. **OPEN, the hardest.**
- `hold_fst_tail_le` — `∑_d hold·1_{m<4d₁} ≤ δ·m^{−A}`. **OPEN.**
Then `fpDist_white_exit`, `Q_black_edge_case2` (BlackEdge), `Q_black_edge_case3_assembled`
(`Case3.lean`) remain (X8 Case-2 / X11d).

## NEXT — hardest-first: prove `fpDist_fst_tail_le` (fixed-tilt fp tail)
Full route in `PENDING_WORK.md` top entry. Sketch: obtain `c,C'` from `fpDist_col_le`,
set `θ₀ = ½·min(c, c²/20)` (FIXED). Chernoff `∑_e fpDist·1_{e₁>m/4} ≤ e^{−θ₀m/4}·Z_fp(θ₀)`.
Bound `Z_fp(θ₀)` by Fubini + `fpDist_col_le` + `gaussExp_col_tail` at cutoff `K'=Θ(s)`
(budget `s·log2 ≤ (K'+2)log9`, take `K' = ⌈s·log2/log9⌉+2`): bulk `≤ exp(θ₀K')`, tail
`= gaussExp_RHS`, both `exp(O(m/log²m))`. So `e^{−θ₀m/4}·Z_fp = exp(−θ₀m/4 + O(m/log²m))
≪ m^{−A}`, closed by `exp_neg_mul_le_of_large` + `log_sq_ge_of_large`. ~150 lines,
reuses the `fpDist_fst_mgf_le` scaffolding almost verbatim. Then `hold_fst_tail_le`
(needs `iidSum hold 1 = hold` via `iidSum_succ`/`iidSum_zero`), then the double-`tsum`
glue: `∑_e fpDist·edgeWeight ≤ m^{−A}·Z_fp·Z_hold + P_fp(e₁>m/4) + P_hold(d₁>m/4)`
(`edgeWeight_summand_le` over d, `exp(θ(e₁+d₁))=exp(θe₁)exp(θd₁)`, Fubini;
`1_{m<2(e₁+d₁)} ≤ 1_{4e₁>m} + 1_{4d₁>m}`). Choose δ-splits so `(1+δ/2)+(δ/2)=1+δ`.

## Gotchas this session
- `div_le_iff`→`div_le_iff₀`, `div_le_div_iff`→`div_le_div_iff₀`,
  `pow_le_pow_left`→`pow_le_pow_left₀` in this mathlib (v4.31).
- `ENNReal.tsum_toReal_eq : (∑' f).toReal = ∑' (f a).toReal` — apply `.symm` to go
  `∑' (f).toReal → (∑' f).toReal`.
- `rw [tsum_congr h]` can't infer the target `g`; use it as a `calc` step instead.
- `set_option maxHeartbeats N in` must go BEFORE a doc-comment, not between the `-/`
  and the `theorem`.
- Scratch iteration: a file `import`ing `TaoCollatz.Sec7.BlackEdge` compiles in ~10s
  (deps cached); rename clashing theorems (`_TEST`) since BlackEdge already declares them.
- `exponentiation.threshold 3000` still set in White/Triangles/BlackEdge/ManyTriangles.
