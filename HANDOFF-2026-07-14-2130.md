# Handoff: C10 COMPLETE (axiom-clean) · C8 PINNED · next = prove C7 (1.19)

**Real date**: 2026-07-14 ~21:30 UTC · **Branch** `main`, HEAD `f42f0fb`, build 🟢 green (full `lake
build`, 3293 jobs), tree clean. **Read `DIRECTION.md` first — it outranks this doc.** Campaign order:
**C10 → C8(pin) → C7(prove) → C8(close) → C9**. Objectives 1 and 2 are DONE this lap; **next is
Objective 3: prove C7 = `first_passage_nonescape` (1.19)**.

## What landed this lap
1. **Objective 1 — C10 CLOSED.** `g3_mass_le` (the suffix-window deficit tail) proved via the new
   suffix marginal `iidMap_suffix` (`(geomHalf.iid n).map (sufSum · r) = iidSum geomHalf r`, from
   `sufSum a r = pre (a∘rev) r` + `iid_map_rev` exchangeability + `iidMap_pre`). `MixingError.lean`
   is now sorry-free. **`error_l1_high_bound` and `prob_not_globalGood_le` are axiom-clean.**
   ➜ **MILESTONE: `fine_scale_mixing` (C10, Prop 1.14) is now `#print axioms` = `[propext,
   Classical.choice, Quot.sound]`** — the §6 crux is believed COMPLETE + clean. `blueprint_audit`
   flags it "MISSED FLIP" (understated). **Judge to verify + flip C10 to `\leanok`** (not ours to set).
2. **Objective 2 — C8 PINNED + ROUTED + PROBED.** New file `Sec5/ApproxFormula.lean`:
   `first_passage_approx` (RATIFY-C8, paper (5.8)) with defs `nZero`/`mZero`/`goodTuple`(𝒜)/`Iy`/
   `Eprime`(E')/`approxMainTerm`. Decomposed into `approx_good_tuple_whp` (5.12) and
   `approx_passtime_window` (5.16). `blueprint_audit`: **0 orange, 0 drift, 0 false-green** (13 nodes
   proved + clean). `\lean{first_passage_approx}` added to node C8; kept `\notready` (judge sets `\leanok`).
   - **What C8 needs from C7** (the pin's deliverable): C7 is consumed at EXACTLY ONE place, the
     (5.16) step (`approx_passtime_window`). Its complement `{¬passes} ∪ {passes ∧ T_x∉I_y}`; the
     `{¬passes}` escape term `ℙ(T_x=∞) ≪ x^-c` IS `first_passage_nonescape` = C7/(1.19). Nothing else
     in Prop 5.2 touches C7. So C8 consumes C7 as (1.19) essentially as blueprinted.

## → NEXT: Objective 3 — prove C7 `first_passage_nonescape` (`Sec5/FirstPassage.lean:91`)
Statement (pinned, RATIFY-C7): `∃ c C x₀, 0<c ∧ 0<C ∧ ∀ x≥x₀, ∀ y∈{x^α,x^{α²}},
(logUnifOdd y (y^α)).expect (indicator {N | ¬passes ⌊x⌋₊ N} 1) ≤ C·x^(-c)`. This is `ℙ(T_x(N_y)=∞) ≪ x^-c`.

**Route (Tao pp.20–21, DIRECTION Obj 3) — one new brick, rest over proved machinery:**
1. ⚠️ **THE INTEGRAL TEST (the whole risk):** `dTV(N_y mod 2^{n'}, unifOddMod n') ≪ 2^{-n'}` for
   the log-uniform window at `n' = 3·n₀`. It is exactly the hypothesis `valuation_dist` (Prop 1.9 /
   C5) takes. Not in Lean. Build it first.
2. Prop 1.9 (C5 ✅ clean) ⟹ (5.4) `dTV(valVec N n₀, geomHalf.iid n₀) ≪ 2^{-c·n₀}`.
3. Lemma 2.2 (S3 ✅ clean; `geomHalf_tail_bound` two-sided covers the LOWER tail) ⟹ (5.5)
   `ℙ(|ā^{(n₀)}| ≤ 1.9·n₀) ≪ 2^{-c·n₀} ≪ x^-c`.
4. Descent arithmetic: `|ā^{(n₀)}| > 1.9·n₀` ⟹ `Syr^{n₀}(N_y) ≤ 3^{n₀}·2^{-1.9n₀}·x^{α³}+O(3^{n₀}) =
   O(x^{0.99}) ≤ x`, so `T_x ≤ n₀ < ∞`. `n₀ = ⌊log x/(10 log 2)⌋` (5.1) — now DEFINED as `nZero`.
   `m₀ = ⌊(α−1)/100·log x⌋` (5.2) is `mZero`.

Then Objective 4: close C8's 3 sorries (feed C7 into `approx_passtime_window`), then C9 `stabilization`.

## Rails / gotchas
- Watched statements (`fine_scale_mixing`/`stabilization`) + all ratified pins UNTOUCHED. `caConst`/
  `epsBW`/window frozen. Never set `\leanok` yourself.
- New `nZero`/`mZero` (5.1)/(5.2) are in `Sec5/ApproxFormula.lean` — reuse them for C7's `n₀`.
- Suffix-marginal route banked: `iid_map_rev`/`pre_comp_rev` are now PUBLIC in `SyracRV.lean`.
- Report remaining work: **"3 sorries + 0 orange"** (C8's `first_passage_approx` +
  `approx_good_tuple_whp` + `approx_passtime_window`) plus C7 (`first_passage_nonescape`) and C9
  (`stabilization`) and the 2 frozen headline stubs. C10 is believed done pending judge flip.

## 📁 Key files
- `TaoCollatz/Sec5/ApproxFormula.lean` — C8 pin + defs (NEW this lap).
- `TaoCollatz/Sec5/FirstPassage.lean:91` `first_passage_nonescape` (C7, NEXT); `:103` `stabilization`
  (C9, WATCHED — do not edit).
- `TaoCollatz/Sec6/MixingError.lean` — C10 tail, now sorry-free; `iidMap_suffix` (~566).
- `TaoCollatz/Syracuse/ValuationDist.lean` `valuation_dist` (Prop 1.9, C5) — the integral-test consumer.
- `TaoCollatz/Prob/LocalInstances.lean:540` `geomHalf_tail_bound` (two-sided, for (5.5)).
