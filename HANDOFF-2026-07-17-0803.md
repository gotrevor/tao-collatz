# Handoff — explicit assembled big-C restart plan

**Date:** 2026-07-17 08:03 EDT · **Branch:** `explicit-big-c`

## Outcome

The fixed-small-pin campaign remains closed, but the requested objective can be separated
cleanly from it. The existing tower route already names the multiplicative constant through
`C_spine X`; only its existential cutoff `X` remains to be transcribed. Fresh `#print axioms`
output for `renewal_white_encounters_at` and `tao_collatz_quantitative_spine_atC` is
`[propext, Classical.choice, Quot.sound]`; the route is believed clean, judge to verify.

`BIG_C_EXPLICIT_BOUND_PLAN.md` is the launch-ready successor plan. It finishes the X-chase,
defines the closed term

```lean
C_tao_assembled := max (C_spine X_spine) ((Real.log 2) ^ cTao)
```

and proves an additive `tao_collatz_quantitative_assembled`. It does not edit the frozen
`CTao`, does not consume `Q_black_edge_tight`, and does not require new §7 mathematics.

## Verified baseline

- `tools/check_blueprint.py`: all 26 checks pass.
- `tools/tao_stmt_diff.py fabea6f HEAD`: 35/35 character-identical.
- Blueprint audit: 25 proved/clean, 0 orange, 0 drift, 0 false green.
- Source census: exactly 2 real sorries, at `Statement.lean:68` and `Bridge.lean:742`.
- Remaining-work report at this docs-only handoff: **2 sorries + 0 orange nodes**.

## Governance

The current `DIRECTION.md` is operator-owned and closes the campaign, so no Lean work from
the plan was started. `PENDING_WORK.md` now carries the required `JUDGE-FLAG`. The
operator/judge can activate the ready-made directive at the end of the plan; until then the
treadmill must stop.
