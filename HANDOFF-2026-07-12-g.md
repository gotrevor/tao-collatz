# HANDOFF — 2026-07-12-g (lap 53, X1 CLOSED: (7.5) pairing proved, Prop 1.17 derived)

**Branch**: `main`, HEAD `6e730d4`, `lake build` green (3278 jobs). All new proved
declarations `#print axioms` = `[propext, Classical.choice, Quot.sound]` (checked).

**Uncommitted edits I did NOT make** (pre-existing, leave for host):
`.obsidian/workspace.json`.

## READ FIRST: `DIRECTION.md` CURRENT DIRECTIVE (outranks this handoff)
Lap-51 directive (pin X9/X10) was completed in laps 51–52. This lap executed the
operator red-queue item X1. An altitude lap should refresh the directive: the
un-pinned RED remainder is only **X5** (Lemma 7.6 joint tail, p.42) and **C8**
(§5 first passage); X8/X9/X10 assemblies are routed (lap-51/52 PENDING_WORK).

## What happened (lap 53, 6 green commits, new file `Sec7/Reduction.lean`)

**X1 = §7.1 reduction chain: RED → CLOSED in one lap.** `Sec7/Reduction.lean` is
SORRY-FREE:
- `fCond` (paper (7.4) conditional factor, concrete uniform-pair form), `xArg`.
- **Lemma 7.2 proved exactly** (`fCond_three_norm`): `|f(x,3)| = |cos πθ(j,l)|`,
  via character algebra `eC_char_add` + u2-unit bridge + half-angle
  `norm_one_add_eC_neg`. Axiom-clean.
- **`cexpect_pairing` (the (7.5) crux) PROVED axiom-clean**:
  - cexpect calculus: `cexpect_map`, `cexpect_bind` (Fubini via
    `Summable.tsum_comm'` / `summable_prod_of_nonneg`), `cexpect_iid_succ`,
    `cexpect_norm_le`, `cexpect_const_mul`;
  - `tsum_geom_pair`: head-pair reindex `(a₀,a₁)↦(a₀+a₁,a₁)` (injective
    zero-extension + `Summable.tsum_prod'`), weight `2⁻ᵇ = pascal(b)/(b−1)`;
  - `cexpect_pairing_gen`: strong induction on remaining coordinates, generalized
    over `(k, L)` offsets; two-coordinate peel; the ZMod (1.26)-sum split closed
    by `linear_combination (−3^{2k})·(inv2_cancel)`; odd-`n` leftover via
    `cexpect_norm_le` (= paper `|g| ≤ 1`).
- `key_fourier_decay` (Prop 7.1) MOVED from `Holding.lean` (deleted there) and
  PROVED from `cexpect_pairing` + `prod_fCond_le_damping` +
  `renewal_white_encounters` (Prop 7.3, already proved in Bridge.lean).
- `charFn_decay` (**Prop 1.17**, `Decay.lean`) PROVED from Prop 7.1 via
  `cexpect_map` (`syracZ` is definitionally the (1.26) reversed pushforward).

**Sorry-surface of Prop 1.17 now**: exactly `white_cos_bound` (X2) + the
Prop 7.8 chain (BlackEdge ×4 / ManyTriangles ×2 sorries).

## NEXT (in order)
1. **X2 `white_cos_bound`** (`Sec7/White.lean`, the one sorry there): white ⟹
   `|θ| > ε` ⟹ `|cos πθ| ≤ exp(−ε³)`. Elementary real analysis
   (`Real.cos_lt_one`-style + Taylor/convexity on `[ε, 1/2]`; ε = 1/10⁴ concrete
   `ℚ`). Cheapest way to shrink Prop 1.17's surface; likely ≤ 1 lap.
2. **X9 R-induction assembly** (lap-52 PENDING_WORK entry: CLAIM-G coupling,
   two-level Y/Z claim over `encExpect_block_le`; needs the (7.59)-shaped
   white-exit without the Case-2 hypothesis — `fpDist_white_exit_deep`).
3. **X10 Σ-count assembly** for `triangle_encounter_le` (lap-51 route).
4. Pin X5 (Lemma 7.6, p.42: joint exponential tail, mean (4,16), aperiodicity)
   and C8 (§5) — the last RED statements.

## Gotchas this lap (also in PENDING_WORK; consider corpus files)
- `Function.Injective.tsum_eq` wants `Function.support f ⊆ Set.range i`
  (use `Function.support_subset_iff'.2`), but `Function.Injective.summable_iff`
  wants the raw `∀ x ∉ range i, f x = 0`. Same file, different interfaces.
- Never `rw [show (1:ℕ) = 0+1 …]` under `Fin.cons` — the numeral occurs in the
  `Fin (m+1)` type index and the motive breaks. State `pre` equations at the
  syntactic shapes (`0+1`, `0+1+1`, `j+1+1+1`) produced by
  `Finset.sum_range_succ'`; `pre_cons` then rewrites directly.
- After `set H := fun … with hH`, a `rw [lemma]` can close goals by set-defeq;
  a following `simp only [hH]` then errors "no goals". Also `rw` under an
  unreduced beta-redex (from `show (∑' w, _) = _`) fails — use
  `congr 1; funext w` instead.
- `‖1+e^{iφ}‖`: factor `exp(iφ/2)·2cos(φ/2)` via `Complex.cos` definition +
  `div_mul_cancel₀`; pairing of `congr 1` goals follows mathlib's
  `cos z = (exp(zI)+exp(−zI))/2` order — put the positive-angle cos in the
  target to keep the pairing straight.
- `linear_combination` closes ZMod pow-cancellation goals where `ring` fails;
  find the coefficient empirically from the printed residual (sign flips are
  cheap to test).
- pointwise mass lemmas: `geomHalf_toReal`, `pascal_toReal` now exist
  (Reduction.lean) — `pascal b = if b < 2 then 0 else (b−1)·2⁻¹^b` is `rfl`.
- Open src sorries (unchanged otherwise): White ×1 (X2), BlackEdge ×4,
  ManyTriangles ×2, spine stubs (Statement ×2, SyracRV ×3, ValuationDist ×2,
  Sec5 ×2, Sec6 ×1, Basic ×2, Prob ×1). `Sec6/MixingFromDecay` (Prop 1.14) is
  now the natural next consumer of `charFn_decay` — Plancherel route, §6 pp.32–33.
