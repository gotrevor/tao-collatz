# Judge handoff — 2026-07-14 14:45 EDT · after pass 29 · HEAD `7ff033b`

> ⏱️ **Timestamps are REAL local time.** The *lap* handoffs (`HANDOFF-2026-07-15-*.md`, repo root)
> run on a **drifted clock** and are named up to a day ahead. Never order judge passes against
> lap-handoff filenames. The live judge briefing is always the newest `judge/HANDOFF-JUDGE-*.md`;
> `judge/HANDOFF-JUDGE.md` is a stable pointer to it.

You are the **architect/judge** for the tao-collatz expedition (Tao 2019, arXiv:1909.03562v5).
Workers produce Lean; **the judge verifies and records.** Worker claims are hypotheses. A green build
≠ a faithful proof. **Next pass number: 30.**

## Protocol documents (source of truth)

1. `EXECUTABILITY.md` → **"Judge loop — standing ops"** (the per-pass recipe) + **"Live judge state"**
   + **"Campaign log"** (index of `judge/pass-NN.md`).
2. `DIRECTION.md` → **CURRENT DIRECTIVE** — the judge writes it; it OUTRANKS every handoff, and each
   grind lap re-reads it. The live steering channel: edit it mid-run to redirect.
3. `BLUEPRINT.md` — durable design. **Status is NOT here** → `blueprint/LEDGER.md` (generated).

## The campaign, as of now: **C10 → C8 (pin) → C7 (prove) → C8 (close) → C9**

- **C10** — one tail bound from done (below).
- **C8** — the **risk** (diff 4, 15–30 laps, **75%**, lowest on the board) and a **SEAM** (no Lean at
  all). **Pin + route + probe it FIRST.** Its `\uses{C7}` binds its *proof*; its *statement* is
  written over the first-passage **defs**, which exist — **so it is pinnable today.**
- **C7** — the other **SEAM**, and ⚠️ **it renders GREEN in the blueprint web and is NOT done**: it
  carried a statement `\leanok` while `\lean{}` named three *defs*, and its content — **(1.19)**,
  `P(T_x(N_y) = ∞) ≪ x^{-c}` — was nowhere in Lean. Fixed (`\notready`); the audit now gates on
  **FALSE STATEMENT-GREEN**. Cheap (85%) and unblocked; it is the input C8's *proof* consumes.
- **C9** — consumes C10 and C8.

⚖️ **The judge got this order wrong twice before Trevor corrected it.** Once by citing the dependency
graph without reading the C7 edge in it; once by de-risking in **cost** order instead of **risk**
order, treating a proof-dep as a statement-dep. **STATEMENT-deps ≠ PROOF-deps**, and the charter is
*de-risk breadth-first*: pin the scary node, then feed it. See `judge/pass-29.md` §6.

## Where things stand (pass 29 — read `judge/pass-29.md` before ruling on anything)

- 🏆 **§7 is COMPLETE and judge-verified** (pass 27). The campaign's stated 65–75% risk concentration
  is retired. 13 nodes proved + axiom-clean, 0 false-green.
- 🔬 **C10 is ONE TAIL BOUND from done.** Two machine-checked identities collapsed it:
  `mainHigh_eq_restrictedDensity` (`mainHigh` **is** the restricted Syracuse pushforward) and
  `sum_abs_syracZ_sub_mainHigh_eq` (`∑|syracZ − mainHigh|` **=** `P(¬mainEvent)`, an *equality*).
  The single remaining sorry, `error_l1_high_bound` (`Sec6/MixingError.lean:359`), is exactly
  **`P(¬mainEvent) ≤ (C/2)·m^{-A}`**. No structure left, no novel kernel, no constant risk.
- 🔓 **Both pass-28 tripwires are DISCHARGED, machine-verified.** `hbudget` → `lRange_hbudget` clean
  *and consumed inside a proved theorem*. The **`A′`-absorption at `C_A = 30`** → **shown**, not
  asserted (`osc_mainHigh_bound` clean; head decays at the shifted exponent `A' = A + C_A²·log2`).
- 🕳️ **SEAMS — internalize this.** *The sorry census counts only the holes someone wrote down.*
  **C7** (defs only) and **C8** (nothing at all) owe proofs and have **no Lean theorem behind them**:
  zero sorries, still unfinished. **Always state remaining work as "N sorries + M seams."**
  Today: **4 sorries + 2 seams.**
- 🔓 Zero open suspensions. Zero outstanding `JUDGE-FLAG:`s. `epsBW` frozen at `1/10^1000`.
  `caConst = 30` (`C_A ≥ 23`), **tight** window only — the paper's (6.8) provably cannot close for
  ANY `C` (the `C²` coefficient's *sign* is wrong).

## 🔴 What pass 30 must hunt

1. **The `globalGood ⊆ mainEvent` inclusion.** This is where all the remaining C10 content lives, and
   it is the step most likely to be waved through. A lap that proves the tail bound and gestures at
   the inclusion has proved nothing. Read it against pp.28–31; do not merely axiom-check it.
2. **Any NEW event definition that claims to be a partition.** Demand the proved disjointness lemma.
   (Pass 29's rule, bought by a real bug — see below.)
3. **C8, once it exists.** It will arrive as fresh statements marked `RATIFY-C8` (and later C7 /
   `RATIFY-C7`). **A new pin is a claim, not a fact.** Read each against its numbered display on
   pp.22–25 (C7: pp.20–21) *before* setting `\leanok` — and add every one to
   `tools/tao_stmt_diff.py`'s watch list the same pass. **Ratify ⟹ watch.**
4. **Any `\leanok` a worker set itself** — statement or proof. `blueprint_audit.py` now fails the
   build on a statement `\leanok` over a node with no theorem (**FALSE STATEMENT-GREEN**), but the
   gate is new and workers have not been told about it in a directive they have read yet.

## The lessons these passes bought (do not re-learn them)

1. **A judge-supplied numeral is a hypothesis too.** Three times running the worker's constant beat
   the judge's (`10^30` not `10^27`; `C_A = 30` not 23). Hold your own numbers to the standard you
   hold theirs.
2. **A worker-supplied numeral is a hypothesis too — and when two disagree, the one bolted to the
   machine-checked artifact wins.** The right number sat in the proved lemma's own docstring while
   `DIRECTION.md` ordered a lap to grind at an impossible target.
3. **The guard must follow the frontier.** When §7 closed, the differ was still watching the finished
   half and was blind to the two live sorries. **And an over-sensitive guard gets muted** — a guard
   that cries wolf is a guard nobody reads.
4. **Read the workers' handoffs BEFORE ruling.** Machine evidence tells you *what* changed; only the
   handoffs tell you what the lap was *thinking*, and the verdict often turns on it. Trevor has had to
   say this twice. `ls -t HANDOFF-*.md | head -5` + `PENDING_WORK.md`'s top + grep `JUDGE-FLAG:`.
5. 🆕 **A convention that has bitten twice is a HAZARD, not a convention — and the guard for it is a
   proved lemma, not vigilance.** The reversed-coordinate convention was known well enough to be
   encoded as a trap in the numeric harness, and it *still* produced a `stopEvent` that removed the
   wrong coordinate and did not form the stopping partition it claimed to. It compiled green.
   **An unproved partition claim is a seam wearing a definition's clothes.**
6. 🆕 **An internal definition cannot corrupt a pinned theorem — it can only make a lemma
   unprovable.** *It costs provability, never soundness.* Use this to triage fast: if a worker's
   deviation is confined to internal machinery, the question is "will they grind forever?", not "is
   the theorem still true?" Both matter; they are not the same alarm.

## Tools (all in-repo)

| tool | what it does |
|---|---|
| `tools/tao_stmt_diff.py OLD NEW` | **29 watched names / 13 files.** Statement erosion is the #1 unattended risk. Normalizes `:= by` vs `:=`. **Grow it when C8 is pinned.** |
| `tools/blueprint_audit.py [--write-ledger]` | **Derives** the node ledger from `content.tex` + a live kernel run. Gates on **FALSE-GREEN**; reports **DRIFT / SEAM / MISSED-FLIP**. CI runs it. |
| `tools/tao_hbudget_check.py` | The C10 constant arithmetic — which window discharges `hbudget`, at what `C_A`. |

## Hard rules (host/human constraints)

- **NEVER launch or offer to launch a treadmill.** Trevor fires them. Read-only `lean-treadmill
  status`/`list` are fine.
- **Public words are Trevor's.** Never post to Zulip/GitHub/anywhere outward; draft and hand over.
- **`epsBW` and altitude-class calls are the JUDGE's**, not the operator's. Trevor has no stake in the
  numeral — say "the judge dictates it," don't manufacture his authority.
- Bare `git` is hook-blocked → use `git-safe`. Judge commits: `git-safe commit --no-verify`.
  **Boxes cannot push; the judge pushes.**
- Paper gaps: document in-repo (`papers/literature-review.md`), **never announce**.
- `Statement.lean`'s two sorries are the **headline stubs** — not holes to fix.
- 🔒 **HARD RAIL 6**: *never EDIT a ratified pin — not to weaken, not to strengthen, not to
  generalize.* This covers the OPEN crux statements too (`fine_scale_mixing`, `stabilization`).
  Decomposing *below* a crux is always allowed; moving the goalposts is not. Blocked? → `JUDGE-FLAG:`
  in `PENDING_WORK.md` and move on.

## 👥 There is a second worker: Codex

Codex (OpenAI) works this repo alongside the treadmill boxes, and it is **good** — this range it
discharged `hbudget`, showed the `A′`-absorption, collapsed C10 to a single tail bound, and **found a
real faithfulness bug the whole apparatus had missed.** Two frictions to keep managing:

- Its commit messages under-report. The reversed-coordinate finding shipped as *"C10: formalize the
  stopping partition."* **Mine its prose reports, not just its diffs.**
- It proposed a governance regime (protected-path hook + CODEOWNERS) and began rewriting
  `BLUEPRINT.md` wholesale; both were stood down. Trevor: *"It's theater. The judge can do a git
  diff."* Its *findings* are kept; its *methods* are not.
- ⚠️ **"Coordinator" and "judge" must be one seat, or reconciled explicitly** — otherwise two channels
  write `DIRECTION.md` and we reinvent the problem one level up.

---
*Judge passes 1–23 by Ren/Fable; 24–29 by Ren/Opus. Written 2026-07-14 14:45 EDT at HEAD `7ff033b`,
treadmill idle, about to be fired on the C10 → C8 → C9 campaign.*
