# Judge handoff — 2026-07-14 13:55 EDT · after passes 27 + 28 · HEAD `a6ebbb3`

> ⏱️ **Timestamps are REAL local time** (Trevor, 2026-07-14). Note the *lap* handoffs
> (`HANDOFF-2026-07-15-*.md`) run on a **drifted clock** — they are named a day ahead. Do not
> use their filenames to reason about wall-clock order relative to judge passes. Judge handoffs
> are the newest `judge/HANDOFF-JUDGE-*.md`; `judge/HANDOFF-JUDGE.md` is a pointer to it.

You are the **architect/judge** for the tao-collatz expedition (formalizing Tao 2019,
arXiv:1909.03562v5). Workers produce Lean; **the judge verifies and records.** Worker claims are
hypotheses. A green build ≠ a faithful proof.

## Protocol documents (source of truth)

1. `EXECUTABILITY.md` → **"Judge loop — standing ops"** — the per-pass recipe.
2. `EXECUTABILITY.md` → **"Live judge state"** + **"Campaign log"** (index of `judge/pass-NN.md`).
   **Next pass number: 29.**
3. `DIRECTION.md` → **CURRENT DIRECTIVE** — the judge writes this; it OUTRANKS every handoff and
   each grind lap re-reads it. The live steering channel: edit it mid-run to redirect.
4. `BLUEPRINT.md` — durable design. **Status is NOT here** → `blueprint/LEDGER.md` (generated).

## 🔴 FIRST THING — pass 29 is a BOUNDARY pass. 69 commits unjudged (`8505bd4..a6ebbb3`).

Spin a pinned worktree (the box/Codex are live in the shared tree):
`lean-create-worktree /Users/gotrevor/src/tao-collatz /Users/gotrevor/src/tao-collatz-judge29 --start-point a6ebbb3`
(**absolute path** for the base; CoW `.lake`, ~2 min). Run every axiom check *there*.

**Already done for you (2026-07-14 13:55):** `./tools/tao_stmt_diff.py 8505bd4 a6ebbb3` →
**29/29 character-identical.** Every watched statement survives the range, `fine_scale_mixing`
included. Statement erosion is NOT what you are hunting this pass. These three are:

### 1. 🔴 Is C10 actually closed? (`fine_scale_mixing`)
Its `sorry` **left `MixingFromDecay.lean` and reappeared in a new file** (`Sec6/MixingError.lean:359`),
and the theorem flipped `:= by` → `:=` (term-mode proof). Census went **6 → 4**. That is either an
honest decomposition (the crux proved modulo a named error-term lemma) **or a hole relocated into a
fresh file to make a census look good.** The differ cannot tell these apart. **`#print axioms
fine_scale_mixing` can**: if it still carries `sorryAx`, C10 is not closed, no matter what any
commit message says. *Do not let the census drop be the evidence.*

### 2. 🔴 `hbudget` — the campaign's single load-bearing number (pass-28 tripwire #1)
Claimed discharged in `8a6b7be` as **`lRange_hbudget`**. **Read the discharge, do not merely
axiom-check it.** A green proof of `hbudget` from a *mis-stated event bound* is exactly the shape
of failure that survives every other instrument. It must come from the **TIGHT** window
(`Bₖ` + one-step `Eₖ`), **never** the paper's (6.8), and at **`C_A ≥ 23`** (the box chose **30**).
Recompute with `tools/tao_hbudget_check.py` if any constant moved.

### 3. ⚠️ The A′-absorption at `C_A = 30` (pass-28 tripwire #2)
`C_A = 30` worsens the single-point mass to `n^{O(C_A²)}·3^{-n}` (900 vs 529 in the exponent — the
safer `hbudget` margin is bought directly out of the absorption budget). The literature review says
this is "absorbed by taking `A′` large, exactly as the paper does." **That is an assertion, not a
proof. Make the lap show it.** If it does not absorb, the answer is a `JUDGE-FLAG:` — **not**
shaving `C_A` back below 23, and **not** touching the window.

Then: `lean-sorry TaoCollatz` (baseline **4 sorries + 2 seams**), `./tools/blueprint_audit.py`,
`/lean-review` over the range, record `judge/pass-29.md`, refresh Live judge state + Campaign log,
rotate `DIRECTION.md`.

## Where things stand

- 🏆 **§7 IS COMPLETE and judge-verified** (pass 27). `prop_7_8` no longer carries `sorryAx`. The
  campaign's stated 65–75% risk concentration is **retired**. 13 nodes proved + axiom-clean.
- ✅ **The `Cthr` bridge is discharged in Lean** (`Case3.lean:2011–2068`) — pass 26's one unverified
  step. It needed `10^30`, not the judge's `10^27`.
- ✅ **Cor-6.3 tight-window deviation RATIFIED** (pass 28). It is a *restriction* (our `l`-window ⊂
  the paper's) ⟹ a strictly **weaker** lemma ⟹ **cannot introduce unsoundness**. Categorically NOT
  a pass-26-class event (that was a ratified pin swapped for an *incomparable* statement).
- 🕳️ **SEAMS — internalize this.** *The sorry census counts only the holes someone wrote down.*
  **C7** (defs only) and **C8** (nothing at all) owe proofs and have **no Lean theorem behind them**:
  zero sorries, still unfinished. **Always state remaining work as "N sorries + M seams."**
  Today: **4 sorries + 2 seams.** C8 also **blocks C9** (`stabilization` lives in §5) — pin it
  vs pp.22–25 before C9 work starts.
- 🔓 Zero open suspensions. Zero outstanding `JUDGE-FLAG:`s. `epsBW` frozen at `1/10^1000`.

## Tools (all in-repo)

| tool | what it does |
|---|---|
| `tools/tao_stmt_diff.py OLD NEW` | **29 watched names / 13 files.** Statement erosion is the #1 unattended risk. Normalizes `:= by` vs `:=` (a term-mode proof is not a statement change). |
| `tools/blueprint_audit.py [--write-ledger]` | 🆕 **Derives** the node ledger from `content.tex` + a live kernel run. Gates on **FALSE-GREEN**; reports **DRIFT / SEAM / MISSED-FLIP**. CI runs it. |
| `tools/tao_hbudget_check.py` | The C10 constant arithmetic — which window discharges `hbudget`, at what `C_A`. |

## Hard rules (host/human constraints)

- **NEVER launch or offer to launch a treadmill.** Trevor fires them. Read-only `lean-treadmill
  status`/`list` are fine.
- **Public words are Trevor's.** Never post to Zulip/GitHub/anywhere outward; draft and hand over.
- **`epsBW` and altitude-class calls are the JUDGE's**, not the operator's. Trevor has no stake in
  the numeral — say "the judge dictates it," don't manufacture his authority.
- Bare `git` is hook-blocked → use `git-safe`. Judge commits: `git-safe commit --no-verify`.
  **Boxes cannot push; the judge pushes.**
- Paper gaps: document in-repo (`papers/literature-review.md`), **never announce**.
- `Statement.lean`'s two sorries are the **headline stubs** — not holes to fix.
- 🔒 **HARD RAIL 6**: *never EDIT a ratified pin — not to weaken, not to strengthen, not to
  generalize.* **Extended (pass 27): this covers the OPEN crux statements too** (`fine_scale_mixing`,
  `stabilization`). A lap weakening the very statement it is trying to prove is the highest-value
  silent failure available. Blocked? → `JUDGE-FLAG:` in `PENDING_WORK.md` and move on. Decomposing
  *below* a crux is always allowed; moving the goalposts is not.

## 👥 There is a second worker: Codex

Codex (OpenAI) works this repo alongside the treadmill boxes. It is **good** — it discharged
`hbudget` and found the SEAM gap independently. Two frictions to manage:

- It proposed a governance regime (protected-path pre-commit hook + CODEOWNERS + `AGENTS.md`).
  Trevor: *"It's theater. The judge can do a git diff."* **The hook is removed** (it also masked the
  global Lean green-gate — `core.hooksPath=.githooks` *replaces* rather than supplements). `AGENTS.md`
  and `CODEOWNERS` remain as guidance.
- It began rewriting `BLUEPRINT.md` wholesale and was **stood down**. Its *finding* was kept
  (the seams), its *method* was not (a better hand-written ledger rots the same way).
- ⚠️ **"Coordinator" and "judge" must be one seat, or reconciled explicitly** — otherwise two
  channels write `DIRECTION.md` and we reinvent the problem one level up.

## The four lessons these passes bought (do not re-learn them)

1. **A judge-supplied numeral is a hypothesis too.** Pass 26 handed the box `Cthr ≥ 10^27` in bold.
   It is **wrong** (largeness is consumed at `800 ≤ m^0.1`; `(10^27)^0.1 ≈ 501 < 800`). The box used
   `10^30` and *its* constant carried the proof. Pass 28 repeated the pattern: the judge's `C_A ≥ 23`
   floor has a margin of **0.3%**; the box padded to 30. **Hold your own numbers to the standard you
   hold theirs. Three times running, the worker's constant beat the judge's.**
2. **A worker-supplied numeral is a hypothesis too — and when two disagree, the one bolted to the
   machine-checked artifact wins.** `DIRECTION.md` ordered a lap to discharge `hbudget` from the
   (6.8) window at `C_A ≥ 10`. **Both halves impossible** (the (6.8) budget−cost has a *negative*
   `C²` coefficient — no `C` works). The correct number sat in the proved lemma's own **docstring**
   the whole time.
3. **The guard must follow the frontier.** Pass 26 grew the differ to 19 names — all §7. The moment
   §7 closed, the guard was watching the finished half and was **blind to the two live sorries**.
   *Ratify ⟹ watch* is only half the rule; the other half is **when the frontier moves, move the
   guard.** And: **an over-sensitive guard gets muted** — the differ screamed at a `:= by` → `:=`
   change; that is now normalized away. A guard that cries wolf is a guard nobody reads.
4. **Read the workers' handoffs BEFORE ruling.** The machine evidence tells you *what* changed; only
   the handoffs tell you what the lap was *thinking*, and the verdict often turns on it. Trevor has
   had to say this **twice**. `ls -t HANDOFF-*.md | head -5`, plus `PENDING_WORK.md`'s top, and grep
   for `JUDGE-FLAG:` — **before** you rule on anything.

---
*Judge passes 1–23 by Ren/Fable; 24–28 by Ren/Opus. Written 2026-07-14 13:55 EDT at HEAD `a6ebbb3`,
treadmill idle, Codex active.*
