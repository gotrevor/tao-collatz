# Judge handoff — 2026-07-14 19:05 EDT · live state · HEAD `40cace5`

> **Read `judge/JUDGE.md` first** (the standing brief: rules, instruments, hard rails, the 7
> lessons). This file is only *what changed and what to do next*. Timestamps here are REAL local
> time; the lap handoffs (`HANDOFF-2026-07-15-*.md`) run on a drifted clock — do not order by them.

**Next pass: 30.** A treadmill is **LIVE** (lap 9, reflection, opus·xhigh, ~30 min in; `pgrep -fl
c-yolo`). **Run every axiom check in a pinned worktree**, not the shared tree:
`lean-create-worktree /Users/gotrevor/src/tao-collatz /Users/gotrevor/src/tao-collatz-judge30 --start-point 40cace5`

## What changed this session (passes 29 + the work after it)

- 🏆 **C10 IS COMPLETE.** `fine_scale_mixing` (Prop 1.14 — the node this whole run was aimed at) is
  `[propext, Classical.choice, Quot.sound]`, no `sorryAx` (verified 2026-07-14, HEAD `49b32c7`).
  Flipped to proved in the blueprint; `error_l1_high_bound` closed axiom-clean. Both pass-28
  tripwires (`hbudget`, the `A′`-absorption) discharged and machine-checked.
- ✅ **C7 pinned + ratified vs p.20** as `first_passage_nonescape` (§5). Re-rated **medium / 10–18 /
  75%** (it was falsely `low/5–10/85%` — see below). The box is proving it; the integral-test core
  `harmonic_ap_integral_bound` is already axiom-clean. A few analytic holes remain.
- 🚨 **C8 pinned but DEFECTIVE — this is your #1 item.** See the next section.
- 🗺️ **The blueprint discipline was rewritten** (Trevor: *"why is this so hard?"* — it wasn't, we'd
  overbuilt it). `blueprint_rules.md` (brief, binding, `@`-included into `JUDGE.md`/`CLAUDE.md`/
  `AGENTS.md`), `blueprint_architecture.md` (detail). **"Seam" is retired → "orange node."**
  Pinning a node = **writing its Lean statement with `sorry`.** Two new audit gates: **FALSE
  STATEMENT-GREEN** (a `\leanok` over a node with no theorem — what C7 was) and **CANNOT-MEASURE**
  (aborts exit 2 instead of fabricating a report when a box is mid-rebuild).
- 🔧 `lean-treadmill` no longer defaults any lap kind to **Fable** (was the reflection default);
  opus is the default for grind/review/reflection, all overridable. (Host tool, `gotrevor/bin`.)

## 🔴 FIRST THING — C8's pin is faithfulness-broken. Rule on the re-pin.

Lap 9 (reflection) fired a `JUDGE-FLAG` (**T5**, route CONTINUE-with-correction) and it is sound —
I checked the argument and the numeric probe:

- The ratified-pending C8 pin `first_passage_approx` / `approxMainTerm` renders (5.8) with the
  **ℕ-truncating `Aff` and no divisibility guard**. Tao's reindex is **exact** (Lemma 2.1) on the
  (5.18) congruence. Under the ℕ-floor, `Aff N k ā` depends on `ā` essentially only through `|ā|`,
  so exponentially-many good tuples collapse into `E'` and the closing hole `truncation_error_bound`
  is **FALSE**. Numeric evidence (`tools/sandbox/tao_c8_truncation_probe.py`): truncating count
  **19135** vs exact-guard **0–3** (k=8, N=101).
- **Mandated correction (box is already on it):** RE-PIN as **`RATIFY-C8-v2`** with the
  exact-affine/(5.18) guard, delete `truncation_error_bound`, re-wire onto the exact Lemma-2.1
  reindex. Parallel-safe thread: `passtime_window_inner`.

**Two things to internalize, because they are the session's whole point:**
1. **This was catchable *because* C8's node is ORANGE — pinned but not ratified.** The box never got
   to mark its own pin green. Had it, a false statement would have been trusted. *The
   "don't set your own `\leanok`" rule and the orange state did exactly their job, within hours.*
2. **Re-pinning C8 is ALLOWED — it is NOT a HARD RAIL 6 violation.** Rail 6 protects *ratified* pins;
   C8 was never ratified. An unratified pin is meant to move. When v2 lands, the differ WILL fire on
   `first_passage_approx` — that is correct (watched ≠ ratified; a fire means "judge, look").

**Your ruling owed:** read the C8-v2 statement against **pp.22–25** once it lands, then set its
`\leanok`. It does not exist yet, so there is nothing to ratify this instant — confirm the diagnosis
(done), let the correction proceed, ratify v2 when written.

## State

- **Census: 7 sorries + 0 orange** (C8 is pinned, so not orange — but its pin is *defective*, which
  the census cannot see; that is what the differ + your reading are for). Report as "N sorries + M
  orange," and add: **C8's pin is unratified and under correction.**
- **Watched (`tao_stmt_diff.py`, 31 names):** now includes `first_passage_nonescape` (C7) +
  `first_passage_approx` (C8). Ratify ⟹ watch.
- **`blueprint_audit.py`:** 14 nodes proved, 0 orange, 0 drift, 0 false-green (run it in the worktree
  — it exit-2s if it can't import while the box builds).
- **Open crux left after C8:** `stabilization` (C9, Prop 1.11) — consumes C10 ✅ + C8. Then C6 → headline.
- `epsBW` frozen `1/10^1000`. `caConst = 30`. Zero open suspensions.

## Campaign order (corrected twice this session — read `judge/pass-29.md` §6 for why)

**C10 ✅ → C8 (re-pin v2 + prove) → C7 (prove, in progress) → C9 → C6 → headline.** C7 and C8 run in
parallel (the box is doing both): C7's proof and C8's re-pin are independent. C9 waits on both.
