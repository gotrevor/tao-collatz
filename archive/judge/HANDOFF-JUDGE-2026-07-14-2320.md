# Judge/Architect handoff — 2026-07-14 23:20 EDT · JUDGE PASS 30 · HEAD ~`dcf18d8` (box live)

> **Read `judge/JUDGE.md` first** (standing brief: rules, instruments, the 7 lessons). This file =
> what changed this pass + what to do next. `DIRECTION.md`'s CURRENT DIRECTIVE (pass 30) is the live
> steering channel and outranks this. Timestamps here are REAL local time.

A treadmill is **LIVE** (lap 10+, grind, opus·high; `stop:when-done`, tokens 275k). Run all axiom
checks in a pinned worktree — a fresh one exists at `~/src/tao-collatz-judge30`
(built green @ `d16c710`; note the box has since advanced — re-pin at the current HEAD if you need live proof status).

## What this pass did — a full independent audit + the owed rulings

**A complete paper-vs-blueprint-vs-Lean faithfulness audit** (5 parallel auditors, one per slice,
each pinned to the frozen worktree + the paper; + a judge verbatim re-read of §5 against PDF pp.22–25;
+ an independent `tao_stmt_diff` erosion run). **Verdict: the formalization is faithful. Nothing false
is claimed anywhere.** Slice results:

| slice | verdict |
|---|---|
| **Headline / spine** | `tao_collatz` renders Thm 1.3 in **genuine logarithmic density** (`AlmostAllPos` = Def 1.2 verbatim, `1/N` weights) — the competitor's natural-density trap is avoided. Thm 3.1 (Colmin form) + Collatz/Syracuse defs faithful. |
| **C8 (Prop 5.2)** | **v2 re-pin FAITHFUL** — verbatim-verified vs (5.8)/(5.9)/(5.10)/(5.11)/(5.18)/(5.19). The exact affine guard `3^{n−m₀}N + fnat = M·2^{|ā|}` IS Tao's exact reindex; v1 truncation defect genuinely repaired (probe 19135→0–3). |
| **Definitions** | No live body-vs-paper bug. `Aff` floor walled off (every use divisibility-guarded → floor=exact). `fnat=2^{|ā|}F`, `syracZ` (ℤ/9 table), `dTV` (no-½ vs (1.9)) all faithful. |
| **§7 pinnacle** | No statement drift. `triangle_encounter_le` states Lemma 7.10 `m/log²m<s` faithfully (the `m^0.8` form is confined to internal `_rpow` engines). Lemma 7.9 `exp(2ε)` + depth-gate **sound + consumable** (verified vs the p.55 consumer; the whole chain to `charFn_decay` is a sorry-free proof term). |
| **§6 / C7 / C9** | C10 `fine_scale_mixing` (Prop 1.14) faithful; the tight-window deviation is **statement-invisible + sound** (window/C_A only in proof lemmas; caConst≥30 > 23 budget floor). C7 (1.19) + C9 `stabilization` (Prop 1.11, (1.19)+(1.20)) faithful. |
| **Erosion** | 29/31 watched pins **character-identical** across the C8 re-pin range; the 2 "not found" are C7+C8 *newly pinned*, not vanished. Rail 6 held. |
| **Kernel** | `blueprint_audit` → **15 nodes proved + axiom-clean, 0 orange, 0 drift, 0 false-green** (after the flips below). |

### Rulings executed this pass (in `blueprint/src/content.tex`)
- ✅ **C7 `first_passage_nonescape` FLIPPED** — the pass-29 "missed flip." Kernel-clean + verbatim-faithful to (1.19); proof `\leanok` set.
- ✅ **C8 `first_passage_approx` STATEMENT RATIFIED** (RATIFY-C8-v2) — statement `\leanok` set (green border); proof still owed. Both are now **WATCHED + rail-6-protected**.
- ✅ **C8 numeric trap added** (`tools/check_blueprint.py` check13, verified `trunc=15945 >> guard=2`) — fulfils the pin's own "not done until a numeric trap checks it" rule.

### DIRECTION.md rewritten (pass-30 CURRENT DIRECTIVE) — front-loaded de-risk plan
Cross-checked against an independent **Fable** strategy review and adopted (it caught that a linear
`C8→C9→C6` reverts to the cost-order/discovery-last error the charter warns against). Overnight order:
1. **C9 assembly-spine PROBE first** (highest de-risk): sorry Lemma 5.3 + (5.18)–(5.21), make the Prop 1.11 assembly compile from C8+C10 as black boxes. The seam test — *do the frozen statements compose at scale m₀?* Interface mismatch → `JUDGE-FLAG`, do not edit the ratified pins.
2. **PIN C6 intermediates** (Thm 1.6, Thm 3.1-Syracuse, (1.2) reductions) — cheap, and the headline is the worst seam to leave unpinned. Box pins (sorried, no `\leanok`); judge ratifies next pass.
3. **C8 (5.17) `first_passage_stepback_reduce` hardest-first**, with a **stall-switch**: bank (5.16)-type machinery on any zero-progress (5.17) lap. *(The box just proved (5.16) — good.)*
4–6. C9 ribs → C6 proof from the pinned intermediates.
Plus: **event-triggered judge passes** (ratify-on-pin; reflect the lap C8's last hole closes; goalpost-pressure) on top of every-9/every-3; census bump from steps 1–2 is PROGRESS not regression.

## Open items for the NEXT judge
- **Ratify-on-pin**: when the box pins the C6 intermediates (Thm 1.6, Thm 3.1-Syr) and the C9 locals, read each vs §3/§5 and set `\leanok` + add to `tao_stmt_diff.py` watch list (ratify ⟹ watch).
- **Watch the C9 seam** — Fable's (and my) bet for the last-mile surprise is the Prop 1.11 assembly where C8+C10 are consumed together at scale m₀ (quantifier order / uniformity-in-n / normalization), NOT C8's (5.17).
- **Doc-hazard still in the tree**: the stale `ApproxFormula.lean:247–251` comment ("reindex APPROXIMATE / do not attempt exact `=`") is dead v1 residue, now provably false (the exact reindex is proved). DIRECTION mandates fixing it early; verify it's gone. Same for the `Aff` docstring (`Valuation.lean:152`).

## Non-blocking follow-ups (do not spend a crux lap)
- Scrub stale "OPEN/sorry/owed" docstrings on PROVED nodes: `BlackEdgeQ.lean:115`, `Case3.lean:2922`, `FirstPassage.lean:981/985/1325`, `Basic/Collatz.lean:16`, `Prob/Basic.lean:16`.
- `check_blueprint.py` check11 traps `epsBW=1/10^4`; deployed is `1/10^1000` — update the value.
- `papers/literature-review.md` ledger row says `ε:=10⁻⁴` (stale; deployed `10⁻¹⁰⁰⁰`).

## Inherited live state (carried, unchanged)
- **Zero open suspensions.** Both pass-28 tripwires discharged.
- **Constants FROZEN**: `epsBW=1/10^1000`, `caConst=30`. Any change RE-ARMS the ε-sweep re-ratification list → `JUDGE-FLAG`, do not adjust.
- **Watched set** (`tao_stmt_diff.py`): the full ratified §7 set + `fine_scale_mixing`/`stabilization` + `first_passage_nonescape`/`first_passage_approx` + the two `Statement.lean` headlines.
- **Census**: 5 sorries + 0 orange (C8 leaves, C9 `stabilization`, 2 headline stubs). Report "N sorries + 0 orange."

*Judge: Ren/Opus, pass 30. Workers this range: the treadmill boxes (+ an external Codex session historically). The `Anchors.lean` untracked file at handoff time is the box's in-flight work — not part of the judge commit.*
