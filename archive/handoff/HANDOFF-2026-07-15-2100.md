# Handoff: k-sum CAST cracked; `osc_syracZ_high_regime` DECOMPOSED into named obl 1+2

**Real date**: 2026-07-14 (filename follows the sequence's drifted clock so it sorts newest).
**Branch**: `main`, HEAD `d24618b`, build 🟢 green (3285 jobs), tree clean. **Sorry census 6**
(2 `Statement.lean` headlines + C9 `stabilization` + C10's `osc_mainHigh_bound` +
`error_l1_high_bound` + `osc_syracZ_regime_telescope`). `fine_scale_mixing` + `osc_syracZ_high_regime`
both now **sorry-free**. Differ 27/29 char-identical (incl. both watched crux statements).

## First: judge pass 28 landed (read `DIRECTION.md` "🚨 JUDGE PASS 28 — CORRECTION" + `judge/pass-28.md`)
The Cor-6.3 tight-window deviation is RATIFIED. But two corrections are BINDING:
- Discharge `hbudget` from the **TIGHT** window (`l ≤ n log₂3 − (C_A²−2C_A)log n`), **never (6.8)**
  (its ½-window has a *negative* C² coefficient — no C_A rescues it).
- **C_A ≥ 23**, not 10 (the proved kernel runs ε=1/5, cost 0.601C²). Fixed `caConst = 30`.
- ⚠️ SHOW the `A′`-absorption of `n^{O(C_A²)}` at C_A=30 when wiring obl-2/obl-3 — do not assert it.
  If it fails: `JUDGE-FLAG:`, do NOT shave C_A, do NOT touch the window. (Alt: re-prove the kernel at
  ε=1/4 → threshold back to ≳10 — allowed, it's an unwatched internal lemma.)

## ⭐ This lap: the k-sum cast (the "main new friction" for 3 laps) is DISCHARGED
All proved + axiom-clean `[propext, Classical.choice, Quot.sound]` (`Sec6/MixingFromDecay.lean`,
before `osc_syracZ_high_regime`):
- `cutEq`/`osc_cast`/`osc_cast'` — transport `osc` across `(n−1−k)+(k+1)=n`. Trick: **free-var**
  statement ⇒ `subst` collapses the `Eq.rec`. The cut lives on a different `ZMod(3^…)`, but its
  *oscillation* is a real number moved losslessly to level n.
- `castedTerm`/`osc_castedTerm` — one cast stopping-cut density. **Gotcha banked**: a raw `▸` under
  `osc_sum_le` forces `whnf` into `condDensW`'s `tsum` (heartbeat blowup); wrap it in its own `def`
  so it's opaque, and *name* the eq lemma so the proof term is syntactically stable.
- `mainDensity`/`osc_mainDensity_le` — `osc(main) ≤ ∑_{k,l} B k l` (the k-sum glue).
- `osc_syracZ_split_le` — `osc(syracZ) ≤ osc(main) + 2·‖err‖_{L¹}` (`osc_add_le`+`osc_le_two_mul_l1`).

`osc_syracZ_high_regime` (was one opaque sorry) is now PROVED via `osc_syracZ_split_le` on `mainHigh`.

## → Next lap (hardest-first): attack `osc_mainHigh_bound` — it holds `hbudget`
`osc_mainHigh_bound`: `osc(mainHigh n) ≤ C·m^{-A}`. `hbudget` is the campaign's single load-bearing
undischarged number (judge tripwire armed for pass 29). Route:
1. Feed `osc_mainDensity_le` (DONE) the per-cut `condDensWB_osc_le` (DONE) ⇒ reduce to
   `∑_{k∈range n} ∑_{l∈lRange 30 n} D_{k,l}·√(3ⁿ·2⁻ˡ) ≤ C·m^{-A}` with `D_{k,l}=C_A·q⁻ᴬ` (obl 2 /
   `hunif`, from `head_factor_norm_le_charFn`).
2. `√(3ⁿ·2⁻ˡ) = 3^{n/2}·2^{-l/2}`; on `lRange` (`l ≈ n log₂3`) the `2^{-l/2}` cancels `3^{n/2}`.
   Geometric l-sum `∑√(2⁻ˡ)` + k-count (both polynomial) + constant chase; **discharge `hbudget`
   from the TIGHT window** + **SHOW the A′-absorption at C_A=30**.
Then `error_l1_high_bound` (obl 1 error, the (6.3) P(Ē) + (6.4) enlargements via S3 sub-Gaussian
tails; also where the event *partition* correctness of `mainHigh` vs `syracZ` must be shown), and
`osc_syracZ_regime_telescope` (obl 0). Full attack + gotchas: PENDING_WORK top entry.

**Do NOT** touch watched statements (`fine_scale_mixing`/`stabilization`) or any ratified pin.
