# Handoff: REVIEW LAP — C10+C8 verified DONE; C7 crux reframed + numeric leg proved

**Real date**: 2026-07-14 ~21:xx UTC · **Branch** `main`, **HEAD `3b69ddb`** (match by commit — the
`21xx`/`23xx`/`07-15` handoff filenames are clock-skew artifacts from earlier runs; this is the newest
baton). Build 🟢 green (full `lake build`, 3293 jobs), tree clean. **Read `DIRECTION.md` first — it
outranks this doc; its CURRENT DIRECTIVE was updated this lap.**

## This was a fresh-mind REVIEW lap. What it found + did.

**Direction verdict: SOUND, route CONTINUE, no trigger fired.** The pass-29 order
**C10 → C8(pin) → C7(prove) → C8(close) → C9** stands. Positions re-verified by fresh `#print axioms`:
- ✅ **C10 DONE, axiom-clean** — `fine_scale_mixing`, `error_l1_high_bound`, `prob_not_globalGood_le`
  all `[propext, Classical.choice, Quot.sound]`. **Judge to flip C10 `\leanok`.**
- ✅ **C8 PINNED + ROUTED** — `first_passage_approx` (RATIFY-C8) + 2 sub-sorries; consumes C7 at
  exactly one place (`approx_passtime_window`, (5.16)).
- 🎯 **C7 is the live target**, down to 2 sub-sorries.

**⚡ KEY FINDING — the C7 crux was mis-framed.** The 2130 handoff called `integral_test_logUnif`
"no equidistribution machinery (grepped)." That was a search for the *dynamical* `{ξθⁿ}` theorem
(genuinely absent from mathlib). **Our lemma is the ELEMENTARY integral test**, and mathlib HAS its
pieces: `AntitoneOn.sum_le_integral`/`integral_le_sum` (`Analysis/SumIntegralComparisons.lean`),
`integral_inv` (`∫1/t=log`), `Nat.Ioc_filter_modEq_card` (exact AP count, `Data/Int/CardIntervalMod.lean`).
This is the lap's route-decisive de-risking. Recorded in DIRECTION CURRENT DIRECTIVE + PENDING top.

## Code landed this lap (HEAD `3b69ddb`, all green, all axiom-clean)
Attacked the C7 crux hardest-first. In `Sec5/FirstPassage.lean`:
- **`intTest_numeric`** — **PROVED**: `2^{3n₀}/y ≤ 2^{-3n₀}` (via `2^{6n₀} ≤ x^{0.6} ≤ x^{1.001} ≤ y`).
  The numeric closure; mirrors `descent_pow_bounds`.
- **`integral_test_logUnif`** — now a machine-checked ASSEMBLY of `intTest_numeric` + `intTest_error`
  (`sorryAx` solely via `intTest_error`). Statement UNTOUCHED (RATIFY-C7 frozen).
- **`intTest_error`** — **the ONE remaining analytic brick**, cleanly isolated: `dTV ≤ K·(2^{3n₀}/y)`.
  Docstring carries the full mathlib route.
- **`logUnifOdd_map_even_zero`** — **PROVED** (first structural brick of `intTest_error`'s dTV
  reduction: even residues get pushforward mass 0). `logUnifOdd_support_le` was relocated up above
  `intTest_numeric` so this brick can consume it. See the map-form gotcha + working recipe under NEXT.

## → NEXT (hardest-first, in order)
1. **`intTest_error`** (`FirstPassage.lean:~138`) — THE crux. Expand the dTV reduction
   (`PMF.map_apply` ⟹ mass `S_r/D`; supported on odd residues ⟹ `dTV = (1/D)∑_{r odd}|S_r−2D/M|`),
   then the per-odd-class integral test (`AntitoneOn.sum_le_integral`/`integral_le_sum` on `t↦1/t`
   over each AP + `integral_inv`; AP counts via `Nat.Ioc_filter_modEq_card`), then sum the `M/2` odd
   classes / divide by `D` (**a positive CONSTANT `D ≥ 1/2` suffices** — per-class error is `O(1/y)` with
   no log, so `dTV = (1/D)·O(M/y)` closes for any constant `D`; corrected from an earlier `c·log y`).
   Full plan: **PENDING_WORK top**. Decompose further into `intTest_class_dev` (per-class) +
   `intTest_D_lower` + dTV-reduction glue.
   - ✅ **LANDED this lap** — `logUnifOdd_map_even_zero` (the reduction's odd-support step: even
     residues ⇒ pushforward mass 0), PROVED + axiom-clean, at `FirstPassage.lean` right after
     `logUnifOdd_support_le` (which was relocated up above `intTest_numeric` so the brick can see it).
     **⚠️ TOOLING GOTCHA it exposed (read before touching this map elsewhere).**
     `(logUnifOdd lo hi).map fun N => (N : ZMod (2^n'))` does NOT elaborate to a single
     `PMF.map ↑ logUnifOdd`; **coercion-lifting turns it into a DOUBLE map** —
     `PMF.map (fun z : ZMod => z) (PMF.map (↑) logUnifOdd)` (verified via `extract_goal`) — AND the whole
     thing sits in raw `Bind.bind`/`<$>` Monad-typeclass form, so `rw [PMF.map_apply]`/`PMF.bind_apply`/
     `PMF.support_map`/`PMF.mem_support_map_iff` either fail to match or mis-unify with the identity
     outer map (`f := id`), giving a spurious sum/∃ over `ZMod` (index `ZMod`, not `ℕ`) — that is why
     `intro` binds a `ZMod` and downstream `N % 2`/`support_le` fail with `HMod (ZMod ..) ℕ ..`. **The
     working recipe** (now in the brick's proof): `rw [show (fun z : ZMod (2^n') => z) = id from rfl,
     PMF.map_id]` to collapse the outer id map, THEN `show (PMF.map (fun N : ℕ => (N : ZMod (2^n')))
     (logUnifOdd lo hi)) r = 0` to force the `PMF.map` head (defeq), THEN `rw [PMF.map_apply,
     ENNReal.tsum_eq_zero]; intro N` gives a clean `N : ℕ` goal `if r = ↑N then logUnifOdd N else 0 = 0`.
     Names: `ZMod.val_natCast` (`(↑a).val = a % n`, no `NeZero`), `Nat.mod_mod_of_dvd N (dvd_pow_self 2
     hn'.ne')`. **The next lap's `intTest_error` reduction will hit this SAME double-map/bind form** —
     reuse this recipe (and `logUnifOdd_map_even_zero` itself) rather than re-deriving it.
2. **`valSum_lower_tail`** (`:~175`) — downstream of the crux + mechanical (clone `valuation_tail`'s
   upper-tail via two-sided `geomHalf_tail_bound`; consumes the integral test through `valuation_dist`).
3. Then Objective 4: close C8 (feed C7 into `approx_passtime_window`), then C9 `stabilization`.

## Rails / gotchas
- Watched statements (`fine_scale_mixing`/`stabilization`) + all ratified pins UNTOUCHED. `caConst`/
  `epsBW`/window frozen. **Never set `\leanok` yourself.**
- `valuation_dist`/`valuation_tail` (`Syracuse/ValuationDist.lean:999/1066`) are PROVED and take
  exactly the integral-test dTV bound as their `hmod` hypothesis — so `intTest_error` is the ONLY
  thing owed for the geometric-approximation chain.
- `git-safe` lives at `/Users/gotrevor/personal/bin/git-safe` (not on PATH by default).
- Sorry census: **6 working** (2×C7 = `intTest_error`+`valSum_lower_tail`, 3×C8, 1×C9) + 2 frozen
  headline stubs. **0 orange nodes.**

## 📁 Key files
- `TaoCollatz/Sec5/FirstPassage.lean` — C7 (`intTest_numeric` ✅, `intTest_error` sorry,
  `integral_test_logUnif` assembled, `valSum_lower_tail` sorry, `first_passage_nonescape` assembled);
  `stabilization` (C9, WATCHED — do not edit).
- `TaoCollatz/Sec5/ApproxFormula.lean` — C8 pin + 2 sub-sorries.
- `TaoCollatz/Syracuse/ValuationDist.lean:999/1066` — `valuation_dist`/`valuation_tail` (proved consumers).
- `PENDING_WORK.md` top — the full C7 integral-test attack plan.
