# Handoff: C10 §6 machinery — the full Plancherel/factorization/osc chain proved; 2 of 3 obligations reduced to single number-bounds

**Date**: 2026-07-15 ~12:00 UTC · **Branch**: `main` · HEAD = `a7ce0ee`, `lake build` green (3285 jobs).
**Re-read `DIRECTION.md` CURRENT DIRECTIVE (JUDGE PASS 27) + HARD RAIL 6 EXTENDED first — it OUTRANKS
this handoff.** Do NOT edit the statements of `fine_scale_mixing` (C10) or `stabilization` (C9) —
both are in the differ watch-list. Decompose BELOW them freely.

## 🎯 This session (11 axiom-clean commits, `48a58dd..a7ce0ee`): C10 §6 machinery — the whole analytic core
All new lemmas `#print axioms`-clean `[propext, Classical.choice, Quot.sound]`, every build real-`lake
build` green. All in `Sec6/MixingFromDecay.lean`. **The entire §6 Plancherel + factorization +
oscillation chain is now proved and assembled end-to-end**; what remains is one infra scaffold + two
single number-bounds. Commits, in build order:

1. **`syracZ_char_descent`** (`48a58dd`) — THE genuine novelty (judge pass-27 flagged it as most
   likely to be waved through with a cast). Tao (1.22) on a char sum: `(syracZ (j'+q)).cexpect` at
   freq `3^{j'}·η` = `(syracZ q).cexpect` at `castHom η`; the `3^{j'}` valuation descends the level
   `j'+q → q`. Via `stdAddChar_pow3_descent`+`cexpect_map`+`syracZ_map_cast` (pre-existing (1.22)).
2. **head-block Stage A** (`06c02f3`): `stdAddChar_pow3_descent_right`, `castHom_two_inv_right`
   (right-summand `3^(j+p)→3ʲ` descent mirrors), `head_char_descent` (extracts the `3ᵖ` block prefactor,
   absorbs the frozen `2⁻ˡ` unit into the reduced frequency — it need NOT cancel).
3. **head-block reindex COMPLETE** (`26c3366`): `offset_cexpect_eq_syracZ`, `head_factor_eq_syracZ`
   (Stage A wrapped), `syracZ_char_eq_charFn` (Stage B + eC), **`head_factor_eq_charFn`** (capstone:
   head factor = level-q `charFn_decay` char sum), `head_factor_norm_le_charFn` (`≤ Cₐ·q⁻ᴬ`).
4. **`dft_condDens_norm_le`** (`b92d1e5`): per-ξ product bound `‖𝓕(densC condDens)ξ‖=‖head·tail‖≤Cₐ·q⁻ᴬ`;
   `tail_indicator_factor_norm_le` (the `≤1` Rényi block).
5. **`highfreq_l2_le_collision`** (`d61dbc3`): (6.11) `∑_{high}‖𝓕(densC c)‖² ≤ 3ⁿ·∑(c Y)²` (Parseval).
6. **tail Parseval** (`b3863b6`): `tailDens` (tail sub-density), `tail_factor_dft_eq`, `tail_factor_l2_eq`
   (`∑_ξ‖tail‖²=3^(j+p)·∑(tailDens)²`). **Generalized `dft_cond_density`** to any index type `ι`.
7. **`condDens_highfreq_l2_le`** (`dd48d86`): sharp (6.10)–(6.11) ℓ²-refinement `∑_{high}‖𝓕‖² ≤
   D²·(3^(j+p)·∑(tailDens)²)`, given uniform head decay `D` (`hunif` hyp).
8. **`condDens_osc_le`** (`3256a90`): Tao's (6.10) for one conditioning: `osc(condDens) ≤
   D·√(3^(j+p)·∑(tailDens)²)`. Assembles `osc_le_sqrt_highfreq` + the refinement.
9. **osc subadditivity** (`504e1d9`): `osc_add_le`, `osc_sum_le` (`osc(∑ᵢcᵢ)≤∑osc(cᵢ)`), `osc_nonneg`
   — the (6.1)–(6.8) telescope for the event decomposition.
10. **tail Rényi skeleton** (`a7ce0ee`): `sum_sq_le_max_mul_sum` (`∑d²≤M·∑d` for `0≤d≤M`),
    `tailDens_nonneg`, `tailDens_sum_le_one` (`∑ tailDens ≤ 1`), **`tailDens_renyi_le`** (given
    `tailDens Y ≤ M` uniformly, `∑(tailDens)² ≤ M`).

## 📋 The ONLY open C10 obligations — sharply scoped now (all in `fine_scale_mixing`'s `sorry`)
The chain is: `fine_scale_mixing` = `osc_sum_le` telescope over conditionings `(k,l)` + `condDens_osc_le`
per piece (needs `D`, uniform head decay) + `tailDens_renyi_le` (needs `M`, tail single-point mass) +
error from the bad event. **Three remaining inputs, first is the gate:**

1. **[GATE — NEW INFRA] Event/stopping-time scaffold.** Define the stopping time `k` (unique with
   `a_{[1,k]} ≤ n·log3/log2 − Cₐ²log n < a_{[1,k+1]}`), events `E` (sub-Gaussian (6.2)), `Eₖ`, `Bₖ`
   (`k=k`), `Cₖ,ₗ` (`a_{[1,k+1]}=l`), and the density decomposition `syracZ density = ∑_{k,l}
   condDens_{k,l} + error`, with `osc(error) ≤ small` (from `P(Ē) ≪ n^{-A-1}`). This fixes `j=n−k−1`,
   `p=k+1` and supplies the `j`/`p`/`m` relations that 2 & 3 below need. Largest remaining build; not
   yet started. Route: D1 (PMF+tsum, no measure theory) — the stopping time is a function of the iid
   coords, events are `DecidablePred`s.
2. **`hunif` uniform head decay** (feeds `condDens_osc_le`): for `ξ∈highFreq m (j+p)` (val `<(j+p)−m`),
   construct the per-ξ `j',η` decomposition (`j'`=3-adic val of `2⁻ˡ·castHom ξ`, `q=j−j'`), verify
   `head_factor_norm_le_charFn`'s `hfreq`+coprimality, and bound `q ≥ q_min` uniformly ⟹ `D=Cₐ·q_min⁻ᴬ`.
3. **`M` tail single-point mass** (feeds `tailDens_renyi_le`): `sup_Y tailDens j p l Y ≤ M ≈ 3⁻ᵖ` —
   the genuine Syracuse near-uniformity / offset-injectivity (Lemma 6.2). Everything else in the Rényi
   block is done.

## 📁 Key facts for next session
- `fine_scale_mixing` (`Sec6/MixingFromDecay.lean`, sole spine sorry in that file) — its `sorry` is
  the ONLY thing between the assembled chain and C10. Census still **4** (C10, C9 `stabilization`,
  2 `Statement.lean` headlines). Do NOT touch C9/headlines (downstream / frozen).
- The reindex uses `syracZ_map_cast` (`Syracuse/SyracRV.lean:77`, the (1.22) projection compat) — key.
- `condDens`, `tailDens` defs live in `Sec6/MixingFromDecay.lean`. `condDens_osc_le` + `tailDens_renyi_le`
  are the two "plug the number in" lemmas; wiring them into `fine_scale_mixing` awaits obligation 1.
- PENDING_WORK.md fruit-18 (this session's newest) → fruit-14 has the full running plan.

---
**→ Next session: START the event/stopping-time scaffold (obligation 1) — it gates 2 & 3. Re-read
`DIRECTION.md` (JUDGE PASS 27) + HARD RAIL 6 first. Working tree clean at `a7ce0ee`, build green 3285.**
