# Handoff: C10 — one sorry from complete; only `g3_mass_le` (the suffix marginal) remains

**Real date**: 2026-07-14 19:58 UTC · **Branch** `main`, HEAD `63c576e`, build 🟢 green (full `lake
build`, 3292 jobs), tree clean. **Read `DIRECTION.md` first — it outranks this doc.** Campaign order:
**C10 → C8(pin) → C7(prove) → C8(close) → C9**. Still on objective 1 (finish C10).

## Where C10 stands
`prob_not_globalGood_le` (the whole C10 tail node) is **fully assembled and proved modulo ONE sorry**.
`error_l1_high_bound` wraps it; everything above is axiom-clean. Over the last three laps the single
opaque C10 sorry became visible infrastructure + three per-event tail lemmas, of which **two are now
proved**:

Proved this campaign (all `[propext, Classical.choice, Quot.sound]`, in `Sec6/MixingError.lean`):
- **Marginal law**: `iidMap_pre` (prefix-block), `iid_map_coord` (coordinate), `iidSum_one`,
  `masked_tsum_map` (pushforward bridge), `pre_eq_fin_sum_castLE`.
- **Union bound**: `not_globalGood_pointwise_le` (the (6.3) decomposition, pointwise).
- **Assembly**: `prob_not_globalGood_le` (C = 6, `≤ 6·m^{-A}`), `caThr_nonneg_large`.
- **Shared tail machinery**: `geomHalf_tail_bound_explicit` (the c=1/400,C=2 witness),
  `Gweight_le_two_exp`, `exp_neg_mul_log_eq_rpow`, `log_ge_of_large`, `const_rpow_absorb`.
- **`g2_mass_le`** (coordinate overshoot) — DONE. **`g1_mass_le`** (total-mass deficit) — DONE.

## → THE ONLY REMAINING C10 SORRY: `g3_mass_le` (`Sec6/MixingError.lean`, ~line 932)
Statement (already pinned): for `r ∈ [1,n]`,
`∑' a, if 2r − C_A(√(r log n)+log n) ≤ sufSum a r then 0 else P a ≤ n^{-(A+2)}`, uniform in `r`.

**Step 1 (the one genuinely new brick): the SUFFIX marginal.** Prove
`(geomHalf.iid n).map (fun a => sufSum a r) = iidSum geomHalf r` for `r ≤ n`.
`sufSum a r = pre a n − pre a (n−r)` = sum of the **last r** coords. Two viable routes:
- **(preferred) reflect `iid_map_castLE`**: prove a last-block restriction marginal
  `(p.iid n).map (fun a => a ∘ (Fin.natAdd (n−r) ∘ cast)) = p.iid r`, then compose with `(∑ i, ·)`
  exactly as `iidMap_pre` does. May need an exchangeability/`Fin.natAdd` induction analogue.
- **(alt) block independence**: `cexpect_iid_append` (`Prob/Basic.lean:251`) splits `iid (j+q)` into
  head (first `j=n−r`) × tail (last `q=r`). With head observable `f ≡ 1` and tail `g` = the indicator,
  the head factor is 1 and the tail is the `iid r` marginal. It's stated for `cexpect` (ℂ); either add a
  real/`expect` analogue or push the indicator through. `sufSum a r = ∑ i, (fun i => a (natAdd (n−r) i)) i`.

**Step 2 (tail arithmetic — mirrors g1, already have all helpers).** With the suffix marginal:
`masked_tsum_map` → `iidMap_suffix` → mask `sufSum a r < 2r − C(√(r log n)+log n)` dominated by
`[λ ≤ |L − 2r|]` with `λ = C(√(r log n)+log n)`. Then `geomHalf_tail_bound_explicit r λ`:
- exp term: `exp(−λ/400) ≤ n^{−(A+3)}` from the `+log n` part (`λ ≥ C log n ≥ 400(A+3) log n`), same as g1's exp term.
- Gaussian term: `exp(−(λ/400)²/(1+r)) ≤ n^{−(A+3)}` from the `√(r log n)` part:
  `(λ/400)² ≥ (C√(r log n)/400)² = C² r log n/160000`, `/(1+r) ≥ C² log n/320000` (since `r/(1+r) ≥ 1/2`),
  and `C²/320000 ≥ A+3` (from `caConst_tail_exponent`), so `≥ (A+3) log n`. **No `log ≤ εn` threshold
  needed here** (unlike g1) — the Gaussian is already polynomial in n via `log n`. Cleaner than g1.
- Combine: `2·Gweight ≤ 4 n^{−(A+3)}`, then `const_rpow_absorb A 4 (A+3)` ⇒ `≤ n^{−(A+2)}`.
Watch: `√(r log n)` needs `r log n ≥ 0` (fine); `|sufSum − 2r|` domination needs `sufSum ≤ 2r` in the bad
branch is NOT required — use `neg_le_abs` as in g1 (`λ ≤ 2r − sufSum ≤ |sufSum − 2r|`). Mean of `sufSum a r`
is `2r` (r iid Geom(2) draws). Uniformity in r: `caConst_tail_exponent` is r-free; the `r/(1+r) ≥ 1/2`
step holds for all `r ≥ 1`.

When `g3_mass_le` lands: **C10 is COMPLETE and axiom-clean.** Verify `#print axioms
prob_not_globalGood_le error_l1_high_bound` = trust base only, commit, then move to **objective 2: pin
C8** (Prop 5.2 / (5.8), §5 pp.22–25, `RATIFY-C8` comments, never `\leanok`).

## Rails / gotchas
- `globalGood` documented as an ENLARGEMENT of `Eₖ` (pass-29). Watched statements
  (`fine_scale_mixing`/`stabilization`) + ratified pins untouched. `caConst`/`epsBW`/window frozen.
- Gotchas banked: `PMF.pure_map` (not `map_pure`); `Summable.tsum_add`/`.tsum_finsetSum` (dot form);
  `<;> [t;t]` invalid → `<;> first | t | t`; `one_div_le_one_div_of_le`; `neg_le_abs` for the deviation
  domination; `field_simp` alone closes the ε-cancel (no trailing `ring`); `pre_eq_fin_sum` name taken by
  `ValuationDist` (used `_castLE` suffix). `log_le_eps_mul_of_large` (`Sec7/BlackEdge.lean:316`) and
  `eventually_ca_window` (`Sec6/MixingMain.lean:12`) are the log-threshold templates.

## 📁 Key files
- `TaoCollatz/Sec6/MixingError.lean` — marginal infra (~540–630), shared tail helpers (~681–735),
  `g1_mass_le` (proved ~740), `g2_mass_le` (proved ~845), `g3_mass_le` (OPEN ~932), assembly
  `prob_not_globalGood_le` (~960).
- `TaoCollatz/Prob/Basic.lean:251` `cexpect_iid_append` (suffix-marginal route); `Syracuse/SyracRV.lean`
  `iid_map_castLE` (now public; template for the suffix restriction).
- `TaoCollatz/Prob/LocalInstances.lean:540` `geomHalf_tail_bound`; `MixingCore.lean` `caThr`, `caConst`,
  `caConst_tail_exponent`, `sufSum` (`MixingError.lean:368`).
