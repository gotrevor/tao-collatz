# HANDOFF — 2026-07-14-d (X8 (7.48) crux `fpDist_edgeWeight_le` CLOSED; both tails + Fubini glue proved)

**Branch** `main`, HEAD = `cfa6c46`, `lake build` green (3281 jobs), working tree clean.
**Re-read `DIRECTION.md` CURRENT DIRECTIVE FIRST** — it outranks this handoff. The
directive routes to the X8 Case-2 / Case-3 assembly; this session cleared the (7.48)
weight-degradation crux and its three open inputs.

## What landed this session (four axiom-clean proof commits + PENDING checkpoints)

All verified `#print axioms = [propext, Classical.choice, Quot.sound]` (no `sorryAx`).

### ✅ `fpDist_fst_mgf_general` extracted; `fpDist_fst_mgf_le` refactored (`5650861`)
Pulled the Fubini + `gaussExp_col_tail` envelope core out of `fpDist_fst_mgf_le` into a
**reusable** `fpDist_fst_mgf_general` (`BlackEdge.lean`): for ANY admissible tilt
`0 ≤ θ ≤ ½min(c,c²/20)`, cutoff `K ≥ 25`, budget `s·log2 ≤ (K+2)log9`, it returns
`Summable (fun e => fpDist·exp(θ e₁)) ∧ Z_fp(θ) ≤ exp(θK) + gaussExp_RHS`. Both the
vanishing-tilt MGF and the fixed-tilt tail specialize it. `fpDist_fst_mgf_le` is now a
4-line specialize (bulk `≤ 1+δ/2`, tail `≤ δ/2` from `fpDist_fst_mgf_numeric`). Also
added helper **`log_le_eps_mul_of_large`** (`∀ε>0 ∃N ∀m≥N, log m ≤ εm`, via `log m≤2√m`).

### ✅ `fpDist_fst_tail_le` PROVED (`b10c84e`) — the hardest new input
Fixed-tilt fp first-coord right tail `∑_e fpDist·1_{m<4e₁} ≤ δ·m^{−A}`. Fixed
`θ₀=½min(c,c²/20)` (NOT 2A/m). Chernoff `1_{m<4e₁} ≤ exp(θ₀(e₁−m/4))` ⟹
`T ≤ exp(−θ₀m/4)·Z_fp(θ₀)`; `fpDist_fst_mgf_general` bounds `Z_fp(θ₀) ≤ B·exp(θ₀K)` at
cutoff `K=⌊m/log²m⌋+25`; `K−m/4 ≤ −m/8` (m≥400, log²m≥16); close with
`B·exp(−θ₀m/8) ≤ δ·m^{−A}` (`log_le_eps_mul_of_large`+`exp_neg_mul_le_of_large`). HEARTBEAT 2M.

### ✅ `hold_fst_tail_le` PROVED (`e4fd298`) — the hold tail (clean, closed form)
`hold`'s first marginal IS the geometric `geomQuarter` (`hold_map_fst`), so via
`hold_tsum_fst` + `geomQuarter_tail`: `∑_{k>m/4} geomQuarter(k) = (3/4)^⌊m/4⌋`, then
`(3/4)^⌊m/4⌋ ≤ exp(−(log(4/3)/8)m) ≤ δ·m^{−A}`. No Fubini/MGF needed.

### ✅ `fpDist_edgeWeight_le` PROVED (`a55923e`) — the (7.48) Case-2 crux glue
Decomposed into **`fpDist_edgeWeight_split`** (NEW mechanical Fubini heart: sums
`edgeWeight_summand_le` over d (hold) and e (fpDist), splits the joint tail
`1_{m<2(e₁+d₁)} ≤ 1_{m<4e₁}+1_{m<4d₁}`, factoring into `m^{−A}·Z_fp(θ)·Z_hold(θ) +
T_fp + T_hold`, θ=2A/m; takes the two MGF summabilities as hyps) + the main proof
(ε=min(δ/8,2); Z_fp,Z_hold ≤ 1+ε; MGF ≤ (1+δ/2)m^{−A}; tails ≤ (δ/4)m^{−A} each; sum
= (1+δ)m^{−A}). HEARTBEAT 1M. NB: `edgeWeight A m e = ∑'_d hold·(max(m−e₁−d₁,1))^{−A}`
already contains the hold sum, so the glue is a genuine double sum.

## Open sorries in `BlackEdge.lean` (X8 Case-2 remaining) — only TWO left
- `fpDist_white_exit` (`BlackEdge.lean:1366`) — the (7.50)/(7.51) white-exit bound.
- `Q_black_edge_case2` (`BlackEdge.lean:1454`) — the (7.46)–(7.51) Case-2 assembly.
Then X11 `Q_black_edge_case3` (`Case3.lean:~941`).

## NEXT — hardest-first: `fpDist_white_exit`
**DIRECTION.md mandate**: it is the Case-2 TWIN of the now-proved deep kernel
`fpDist_white_exit_deep` (`ManyTriangles.lean`, a theorem) — "same geometry, budget
hypothesis `s ≤ m/log²m` added; **DERIVE it from `fpDist_white_exit_deep` if you can**."
First move: read both statements side by side (`grep -n "theorem fpDist_white_exit"
TaoCollatz/Sec7/*.lean`), diff the hypotheses, and try to obtain `fpDist_white_exit` as
a specialization/weakening of the deep variant (the kernel-merge was always the plan).
Then `Q_black_edge_case2` (assembles `fpDist_edgeWeight_le` ✓ + `fpDist_white_exit`).
Full route in `PENDING_WORK.md` top entry (Lap cont7).

## Gotchas this session (add to reference corpus if not there)
- `tsum_add` is NOT a free function here — use the method form `hf.tsum_add hg`
  (`Summable.tsum_add`). Same for `.mul_left`/`.mul_right`/`.tsum_le_tsum`.
- `edgeWeight A m e = ∑'d ...` closes by `rfl` (it's the literal def), NOT `by unfold`.
- `field_simp` on `A*(ρ/(k*A)*m) = ρ/k*m` closes the goal ALONE — a trailing `ring`
  errors "no goals". (Bit me twice this session.)
- `set_option maxHeartbeats N in` must sit BEFORE the doc-comment, never between `-/`
  and `theorem` (that's a parse error `unexpected token 'set_option'`).
- Big `set`-heavy proofs with nested `Real.exp` atoms blow the default 200k heartbeats
  (isDefEq/nlinarith cost) — bump to 1–2M with a `-- HEARTBEAT:` note.
- Hold-MGF summability: `tiltZ_hold_ne_top` (needs `|θ|≤1/50`) → `rw [← tiltZ]` →
  `ENNReal.summable_toReal`, then `.congr` with `(hold d * expW2 θ 0 d).toReal =
  (hold d).toReal·exp(θ d₁)` (via `expW2`, `ENNReal.toReal_mul`, `toReal_ofReal`).
- `exponentiation.threshold 3000` still set in White/Triangles/BlackEdge/ManyTriangles.
