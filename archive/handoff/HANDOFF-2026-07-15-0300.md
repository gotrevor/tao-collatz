# Handoff: C10 Cauchy–Schwarz bridge PROVED (axiom-clean) + decay-side route REFUTED & remapped

**Date**: 2026-07-15 ~03:00 UTC · **Branch**: `main` · HEAD = `3b564a2`, `lake build` green
(3285 jobs), tree clean. **Re-read `DIRECTION.md` first each lap — it outranks this handoff.**

## 🎯 What happened this run (8 green commits)

Attacked C10 (`fine_scale_mixing`, Prop 1.14, §6) — the upstream HEROIC analytic node. Two outcomes:

### ✅ The Cauchy–Schwarz + Parseval bridge is FULLY PROVED, axiom-clean
`osc_le_sqrt_highfreq` (`osc m n hmn c ≤ √(∑_{highFreq}‖𝓕(densC n)ξ‖²)`) went from one opaque
`sorry` to **8 machine-checked lemmas**, `#print axioms = [propext, Classical.choice, Quot.sound]`.
All in `TaoCollatz/Sec6/MixingFromDecay.lean`:
- `osc_eq_sum_norm_devC` (osc = ∑‖devC‖, cast) · `sum_norm_sq_devC_eq` (Parseval L²: `devC=𝓕⁻g` +
  `ZMod.dft_parseval`) · `densC_inversion` (Fourier inversion) · `devC_eq_highfreq_invDFT`
  (deviation = high-freq inverse DFT) · `condAvgC_eq_lowSum` (cond. avg = low-freq projection) ·
  `coset_char_sum` (the number-theoretic heart: char sum over a 3ᵐ-fiber) · `fiber_char_reindex`
  (pure combinatorial bijection fiber={Y+t·3ᵐ}) · `geom_sum_root_of_pow_eq_one` (geometric brick).
- Commits `f7bd432`, `0b5e28e`, `edbe2d8`, `a5c7c74`, `f3b7f85`, `d0b9f92`.

### 🚨 The decay side `highfreq_l2_le` is FALSE for raw syracZ — route REFUTED (commit `3b564a2`)
- By Parseval, `∑_{highFreq m n}‖ĉ_n(ξ)‖² = Q(n)−Q(m)`, `Q(ℓ):=3^ℓ∑_Y syracZ(ℓ,Y)²`.
- **Exact DP computation** (`scripts/syracZ_highfreq_l2.py`, no deps) shows this **GROWS ≈0.46·(n−m)**
  (m=1: n=2→0.476, 3→0.938, 4→1.402, 5→1.867), NOT `≤C·m^{-A}`. (The `=Q(n)−Q(m)` identity matches
  to full precision.) So `osc_le_sqrt_highfreq` on the RAW density is lossy (`osc≤√(0.46n)→∞`).
- **Deleted** the false `highfreq_l2_le`; **reverted** `fine_scale_mixing` to a documented `sorry`.
- **Root cause**: Tao's §6 applies Cauchy–Schwarz to a CONDITIONED density `g_{n,k,l}`, not raw
  syracZ. Full remapped route (paper lines 1920–2200, pdf pp.28–31) is in `PENDING_WORK.md` fruit-8.

## 📋 Remaining spine sorries (whole repo)
- `Sec6/MixingFromDecay.lean` `fine_scale_mixing` (Prop 1.14, C10) — HEROIC, needs §6 conditioning.
  The bridge lemmas above are PROVED and are the correct reusable bricks (apply them to `g`).
- `Sec5/FirstPassage.lean:81` `stabilization` (Prop 1.11, C9) — HEROIC, consumes C10; narrow only.
- `Statement.lean:22,28` — the two headlines (DO NOT TOUCH).

## → NEXT (hardest-first, per PENDING_WORK fruit-8 attack plan)
1. **(brick d, mechanical, START HERE)** Generalize `osc_le_sqrt_highfreq` + its helpers (`densC`,
   `devC`, `condAvgC`, `sum_norm_sq_devC_eq`, …) from `fun Y => (syracZ n Y).toReal` to an arbitrary
   real `c : ZMod (3^n) → ℝ` — the proofs never use syracZ-ness. Unblocks applying the bridge to `g`.
2. **(brick a)** The `F`-splitting `Xₙ = F_{k+1}(a_{k+1..1}) + 3^{k+1}2^{-l}F_{n-k-1}(aₙ..a_{k+2})`
   on the event `Cₖ,ₗ` as a Lean identity (uses `pre`/`fnat`, (1.5)/(1.26); some in `Basic/`,`Syracuse/`).
3. **(brick b)** Independence ⟹ character-sum factorization (D1 PMF product form).
4. Then the conditioning events (E/Eₖ/Bₖ/Cₖ,ₗ, stopping time k) + `charFn_decay` on the 2nd factor +
   triangle-inequality reassembly. `charFn_decay` (Prop 1.17) is PROVED, axiom-clean — the input is ready.

## ⚠️ Gotchas banked this run (also in PENDING_WORK fruit-8)
- Rewriting `3^n` when it's ALSO a `ZMod (3^n)` modulus → "motive not type correct"; extract a
  pure-ℕ helper so `3^n` isn't type-bound.
- `ZMod.castHom_apply` takes ONLY the element (`castHom h R i = cast i`), not `h`/`R` explicitly.
- `Complex.norm_real` (not `Complex.norm_ofReal`) + `Real.norm_eq_abs`.
- `Finset.sum_ite_mem_eq` (additive of `prod_ite_mem_eq`): `∑ if i∈s then f else 0 = ∑_{i∈s} f`.
- `Nat.ModEq.mul_right_cancel'` for `a·c≡b·c [MOD m·c] → a≡b [MOD m]`.
- No pip/numpy on box; `scripts/syracZ_highfreq_l2.py` uses stdlib `cmath`/`math` only.

## 📁 Key files
- `TaoCollatz/Sec6/MixingFromDecay.lean` — C10 bridge (proved) + `fine_scale_mixing` (sorry) + a
  prominent ROUTE-FINDING doc block explaining the refutation and the conditioning route.
- `scripts/syracZ_highfreq_l2.py` — exact DP proof that the raw high-freq L² mass grows.
- `PENDING_WORK.md` fruit-8 — full refutation + 7-step conditioning attack + prerequisite bricks.

---
**→ Next session: START with brick (d) (generalize the bridge to arbitrary `c`), then build the §6
conditioning apparatus. Re-read `DIRECTION.md` first.**
