# Handoff: §7 confirmed CLOSED (review lap) + C10 factorization scaffolded (bricks d, a, b-engine)

**Date**: 2026-07-15 ~06:00 UTC · **Branch**: `main` · HEAD = `1bf5e3e`, `lake build` green
(3285 jobs), tree clean. **Re-read `DIRECTION.md` CURRENT DIRECTIVE first — it outranks this handoff.**

## 🎯 What happened this lap (review + 6 crux commits — bricks d, a[×2], b-step1, b-engine)
All 8 new lemmas `#print axioms`-clean `[propext, Classical.choice, Quot.sound]`; every build
real-`lake build`-verified green. C10 went from ONE opaque `sorry` to a specified chain of provable
sub-lemmas with both reusable engines (density-general bridge + iid block-independence) DONE.

### 🏆 Review-lap finding: §7 — the campaign's stated 65–75% risk concentration — is DONE & axiom-clean
`#print axioms` (HEAD): `prop_7_8`, `Q_black_edge`, `Q_polynomial_decay`, `charFn_decay` (Prop 1.17),
`key_fourier_decay` (Prop 7.1) **all** = `[propext, Classical.choice, Quot.sound]`. Inventory: only
**4 live sorries** remain — the 2 frozen `Statement.lean` headlines + C10 `fine_scale_mixing`
(`Sec6/MixingFromDecay.lean:377`) + C9 `stabilization` (`Sec5/FirstPassage.lean:81`).
- The DIRECTION.md CURRENT DIRECTIVE (Judge Pass 26) and STATUS.md were **stale** — both pointed at
  the now-closed X11/§7. **Rewrote both**: new frontier = C10 via the fruit-8 conditioning route.
- No route trigger fired (T1/T2 are §7-specific, resolved). Route = CONTINUE. Commit `cedc330`.

### ✅ Brick (d) DONE: CS/Parseval bridge generalized to arbitrary real density `c` (commit `ad3dade`)
The whole `osc_le_sqrt_highfreq` machinery never used `syracZ`-ness. Generalized `densC n` →
`densC n c := fun Y => (c Y : ℂ)` and threaded `c : ZMod (3^n) → ℝ` through `condAvgC`, `devC`,
`osc_eq_sum_norm_devC`, `densC_inversion`, `condAvgC_eq_lowSum`, `devC_eq_highfreq_invDFT`,
`sum_norm_sq_devC_eq`, `osc_le_sqrt_highfreq`. Character lemmas already density-independent —
untouched. Build green, axiom-clean. So **`osc m n hmn c ≤ √(∑_{highFreq} ‖𝓕(densC n c)ξ‖²)` now
holds for ANY real density** — ready for the conditioned `g`.

### ✅ Brick (a) DONE — both levels (commits `6430476` ℕ, `8da67f9` ZMod), axiom-clean
- **`fnat_split`** (`Basic/Valuation.lean`) — Tao (1.26) integerified, purely algebraic:
  `fnat (j+p) a = 3^p·fnat j (first j) + 2^{pre a j}·fnat p (last p)`. Split `∑_{range(j+p)}` via
  `Finset.sum_range_add`; helpers `pre_castAdd`, `pre_natAdd_split`.
- **`syracZ_offset_split`** (`Syracuse/SyracRV.lean`) — the same split reduced mod `3ⁿ` into the exact
  reduced-offset form (the map of `syracZ_eq_rev_fnat`):
  `(fnat (j+p) a)·2⁻ᵖʳᵉ⁽ᵃ,ⁿ⁾ = 3^p·(fnat j head·2⁻ᵖʳᵉ⁽ᵃ,ʲ⁾)·2⁻ᵗᵃⁱˡᵛᵃˡ + (fnat p tail·2⁻ᵗᵃⁱˡᵛᵃˡ)`.
  The `3^p` on the head is the structural crux: mod `3ⁿ` it kills the low `j` ternary digits, so the
  head feeds LOW freqs and the tail carries HIGH freqs. Residual coupling `2⁻ᵗᵃⁱˡᵛᵃˡ` removed by
  conditioning on the cut-valuation.

### ✅ Brick (b) step 1 DONE: `char_offset_split` (commit `36917b5`, axiom-clean)
Pointwise `stdAddChar(-(X·ξ)) = stdAddChar(-(head·ξ))·stdAddChar(-(tail·ξ))` via
`AddChar.map_add_eq_mul` on `syracZ_offset_split`. Additive split → multiplicative char factorization.

### 🔑 KEY ROUTE FINDING (this lap): condition on the TAIL valuation
Coordinate-dependence: **Term1** (head) `= 3^p·(Fnat_j(head)·2⁻ᴸ)·2⁻ᴹ` depends on head AND tail
(via `2⁻ᴹ`, `M=pre(tail) p`); **Term2** (tail) `= Fnat_p(tail)·2⁻ᴹ` is tail-only. So the char factors
are NOT pure-head·pure-tail, and `E_a[stdAddChar(-(X·ξ))]` does NOT factor into head×tail directly.
**RESOLUTION**: condition on `M = pre(tail) p`. On `{M=l}`: `2⁻ᴹ→2⁻ˡ` const → Term1 head-only, Term2
tail-only → the conditional expectation factors over disjoint iid blocks. (Mirror of Tao's `l`;
`syracZ`'s `a∘rev` swaps head/tail roles. Pins exactly which valuation to condition on.)

### ✅ Brick (b) engine DONE: `cexpect_iid_append` (commit `d464dce`, axiom-clean)
The D1 product-form block-independence lemma in `Prob/Basic.lean`:
`(iid (j+q)).cexpect (fun v => f(v∘castAdd)·g(v∘natAdd)) = (iid j).cexpect f · (iid q).cexpect g`
for `‖f‖,‖g‖≤1`. Proof: `iid_apply_eq_prod`+`Fin.prod_univ_add` (mass factors) → `Fin.appendEquiv`
tsum reindex → `tsum_mul_tsum_of_summable_norm` (summability from new `summable_iid_norm_le_one`).
**This IS the head×tail separation** — with `g` carrying `1_{pre(tail)=l}` it gives the conditional
factorization. (Added imports to Prob/Basic: `Analysis.Complex.Norm`, `Analysis.Normed.Ring.InfiniteSum`,
`Algebra.BigOperators.Fin`, `Logic.Equiv.Fin.Basic`.)

## → NEXT (brick b — assemble the conditional char factorization; PENDING_WORK fruit-9 top)
1. **Combine `char_offset_split` + `cexpect_iid_append`**: `𝓕(densC g_l) ξ = E_a[stdAddChar(-(X·ξ))·1_{pre(tail)=l}]`;
   factor pointwise (char_offset_split), apply `cexpect_iid_append` with `f`=head char (head-only on
   `{M=l}`), `g`=tail char × `1_{pre(tail)=l}`. Both char norms=1, indicator≤1 → `‖·‖≤1` hyps hold.
2. **Tail factor = level-`p` Syracuse char sum** ⟹ `charFn_decay` (Prop 1.17, PROVED) via
   `syracZ_map_cast`-style reindex at level `3^p` (`syracZ_eq_rev_fnat` links `Fnat_p·2⁻ᵖʳᵉ` to `syracZ p`).
3. **Conditioning events + reassembly** (stopping time k, E/Eₖ/Bₖ/Cₖ,ₗ, union over k,l, triangle ineq;
   (6.2)–(6.10)); bridge `𝓕(densC g) ↔ cexpect` (`g(Y)=E_a[1_{X=Y∧ev}]`, finite-∑↔tsum swap). Decompose
   into named `sorry`s in `Sec6/MixingFromDecay.lean` as built. Full plan: fruit-9 + fruit-8.

## 📋 Remaining spine sorries (whole repo)
- `Sec6/MixingFromDecay.lean:377` `fine_scale_mixing` (Prop 1.14, C10) — **current crux**; bridge
  (proved, now general) + `charFn_decay` (proved) are the bricks; needs the §6 conditioning assembly.
- `Sec5/FirstPassage.lean:81` `stabilization` (Prop 1.11, C9) — downstream of C10; narrow only.
- `Statement.lean:22,28` — the two headlines (DO NOT TOUCH; discharge when C10/C9/C6 land).

## 📁 Key files
- `TaoCollatz/Sec6/MixingFromDecay.lean` — C10 bridge (proved, general) + `fine_scale_mixing`
  (sorry) + a ROUTE-FINDING block explaining the raw-density refutation and the conditioning route.
- `scripts/syracZ_highfreq_l2.py` — exact DP proof that the raw high-freq L² mass grows (why
  conditioning is mandatory).
- `PENDING_WORK.md` fruit-9 (this lap) + fruit-8 (full C10 attack path).

---
**→ Next session: START brick (a) (the F-split identity). Re-read `DIRECTION.md` CURRENT DIRECTIVE first.**
