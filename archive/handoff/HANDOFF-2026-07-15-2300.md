# Handoff: C10 — `globalGood ⊆ mainEvent` PROVED; error node is now a pure union bound

**Real date**: 2026-07-15. **Branch** `main`, build 🟢 green (full `lake build`, 3292 jobs), tree clean
after commit. **Sorry census 4** (2 `Statement.lean` headlines + C9 `stabilization` +
C10 `prob_not_globalGood_le`). Watched crux statements (`fine_scale_mixing`/`stabilization`) and all
ratified pins untouched. All new lemmas `[propext, Classical.choice, Quot.sound]`.

## What landed this lap (1 green commit) — all in `Sec6/MixingError.lean`
Per DIRECTION objective 1: **PROVE `globalGood ⊆ mainEvent` explicitly — that inclusion IS the
content.** Done, machine-checked, axiom-clean.

- **`globalGood_subset_mainEvent`** (the crux inclusion). `globalGood A n` = Tao (6.2) as an
  ENLARGEMENT of `Eₖ` (documented as such, per the pass-29 rail): (G1) `pre a n > caThr`, (G2)
  `∀ i, a i ≤ 2C·log n`, (G3) `∀ r∈[1,n], 2r − C(√(r log n)+log n) ≤ sufSum a r`. The witness cut is
  the **first-passage** `k = (least k with sufSum a(k+1) > T) − 1` via `Nat.find`; needs `0 ≤ caThr`
  (a large-`n` fact, threaded as a hypothesis / supplied by the caller's `n₀`). lRange lower end from
  the crossing `T < l`, upper end from G2, window from G3, straddle from `Nat.find` minimality.
- **`mainPieceEvent_of`** — packaging lemma reducing all three `mainPieceEvent` constituents
  (`pre vt(k+1)=l`, `condWindow`, `stopEvent`) to suffix-sum facts. This is where the reversed-tail
  index algebra lives (`pre vt s = pre a(j+s) − pre a j`).
- Support calculus: `pre_cast_tail_prefix`, `pre_succ`, `sufSum` (+ `_zero`/`_full`),
  `sufSum_succ_le_add` (coordinate-cap ⇒ suffix step bound).
- **`error_l1_high_bound`** is now a **fully-proved thin wrapper**: `sum_abs_syracZ_sub_mainHigh_eq`
  gives `P(¬mainEvent)`; a pointwise `Summable.tsum_le_tsum` uses the inclusion (`¬mainEvent ⊆
  ¬globalGood`) to bound it by `P(¬globalGood)`; `prob_not_globalGood_le` finishes.

## → Next (the ONLY remaining C10 content): `prob_not_globalGood_le` (`Sec6/MixingError.lean:543`)
A **pure probability/tail estimate** (no event algebra left):
`0 ≤ caThr (caConst A) n ∧ 2·∑' a, (¬globalGood-mask) ≤ C·m^{-A}` for `n ≥ n₀`, `9n ≤ 10m ≤ 10n`.
Full attack in `PENDING_WORK.md` top entry. Sketch, hardest-first:
1. Union bound: `¬globalGood ⊆ G1bad ∪ ⋃_i G2bad ∪ ⋃_r G3bad` ⇒ mass ≤ `P(G1)+Σ_i P(G2_i)+Σ_r P(G3_r)`
   (≤ `1+n+n` events; a `tsum` union bound).
2. Per-event: `geomHalf_tail_bound` (`Prob/LocalInstances.lean:540`). ⚠️ **First find/prove the
   marginal** that `pre a r` (a partial sum of the iid Geom(2) vector `geomHalf.iid n`) is distributed
   as `iidSum geomHalf r` — check `Syracuse/SyracRV.lean` / `Syracuse/ValuationDist.lean`. Then G3 at
   `λ≈C√(r log n)` ⇒ `Gweight ≈ n^{−cC²}`; `caConst_tail_exponent` (`A+3 ≤ C/400`) is the exponent room.
3. `0 ≤ caThr`: `n log₃/log2 ≥ C² log n` for large `n` (reuse the `log n ≤ 2√n` machinery scoped for
   `lRange_hbudget`'s `hwin`).
4. n→m: `m ≤ n ⇒ n^{-A} ≤ m^{-A}`; the ≤n-term sum × `n^{−cC²}` absorbs into `m^{-A}` (cC² ≫ A+1 at C=30).

## Rails honored / notes
- `globalGood` documented as an **enlargement** of `Eₖ`, never equal (pass-29 rail).
- The one disjointness rail: the partition claim behind `mainHigh` already carries its PROVED
  disjointness lemma (`mainPieceEvent_cut_unique`, pre-existing) — unchanged this lap.
- Do NOT touch watched statements or ratified pins. C8/C9 stay untouched until C10 is done.
- The C8 seam (objective 2) and C9 (objective 3) remain after `prob_not_globalGood_le`.
