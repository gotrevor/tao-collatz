# Handoff: Objective-3 fruit run — SyracRV stubs 1&2 CLOSED, §5 logUnifOdd CLOSED, Lemma 1.12 five cores proved

**Date**: 2026-07-14 ~21:00 UTC · **Branch**: `main` · HEAD = `838fede`, `lake build` green
(3282 jobs), tree clean. **Re-read `DIRECTION.md` first each lap — it outranks this handoff.**

## 🎯 What happened this run (objective 3 = burn down the fruit; §7 crux already DONE)
Seven proof commits, all axiom-clean `[propext, Classical.choice, Quot.sound]`, all build-green:

1. `224c7de` — **`syracZ_eq_rev_fnat` (paper 1.21)** PROVED — SyracRV stub 1/3. The reversed-form ↔
   `fnat`-offset LAW identity (NOT pointwise — genuinely distributional). Via iid exchangeability:
   pointwise `g(b)=f(b∘rev)` + reversal-invariance `iid_map_rev`. Moved `iid_apply_eq_prod` up to
   `Prob/Basic.lean` (namespace `PMF`) to dodge an import cycle; ValuationDist re-exports it.
2. `82bcaf3` — **`syracZ_map_cast` (paper 1.22)** PROVED — SyracRV stub 2/3. Projection compat
   `Syrac(ℤ/3ⁿ) mod 3ᵏ = Syrac(ℤ/3ᵏ)`. Via **`iid_map_castLE`** (prefix-k marginal of iid is iid —
   general, reusable) ∘ castHom truncation (`3^k=0` kills high terms, `φ(2⁻¹)=2⁻¹`).
3. `892938a` — **§5 `logUnifOdd` normalization** PROVED. `if→dite` refactor to bring nonemptiness
   into scope; `∑ N⁻¹/D = D/D = 1`. `Sec5/FirstPassage.lean` now carries ONLY heroic `stabilization`.
4. `1b18cde`, `45a1144`, `f95dcec`, `209469a` — **Lemma 1.12 (`syracZ_recursion`) decomposed; FIVE
   cores proved** (see below).

## 🧩 `syracZ_recursion` (last SyracRV stub) — STATE: 5 cores proved, 1 fiber lemma left
`Syracuse/SyracRV.lean:315` is the ONLY remaining sorry there. All its scaffolding is proved:
- `pre_succ_tail` — `pre a (m+1) = a 0 + pre (tail a) m`.
- `syracZ_offset_peel` — `Gₙ₊₁(a) = 2⁻ᵃ⁰·(1 + 3·Ĝ(tail a))` in `ZMod 3ⁿ⁺¹` (head-peel of the offset).
- `geom_fold` — `∑'_a 2⁻ᵃ·g(a) = (1−2⁻ᴾ)⁻¹·∑_{r<P} 2⁻ʳ·g(r)`, P-periodic g (divModEquiv + tsum_geometric).
- `two_pow_period` — `(2:ZMod 3ⁿ⁺¹)^{2·3ⁿ} = 1` (ℤ-dvd induction `A³−1=(A−1)(A²+A+1)`; no LTE).
- `geom_fold_geomHalf` — the exact Geom(2)-weighted, `Icc 1 P` fold the theorem consumes.

**NEXT = the ZMod fiber lemma** (the genuine number-theory crux, ~100+ lines). Full precise statement
+ 3-step route + final-assembly plan are in `PENDING_WORK.md` (top entry, lap fruit-5). Sketch:
```
∀ a0 x, ∑' w, (geomHalf.iid n) w * (if x = Gₙ₊₁(cons a0 w) then 1 else 0)
      = if (2^a0·x.val)%3=1 then syracZ n (((2^a0·x.val−1)/3:ℕ):ZMod 3ⁿ) else 0
```
(a) offset_peel ⟹ cond ⟺ `2^{a0}x = 1+3·Ĝ(w)`; (b) split on guard `2^{a0}x≡1 (mod 3)`; (c) `1+3Ĝ(w)=2^{a0}x`
⟺ `Ĝ(w)≡arg (mod 3ⁿ)` via `3·ZMod 3ⁿ⁺¹ ≅ ZMod 3ⁿ` + `Ĝ(w) mod 3ⁿ = Gₙ(w)` (castHom, reuse map_cast
machinery) ⟹ w-sum = `syracZ n arg` by `map_apply`. Then assembly: `map_apply` + `tsum_iid_succ_mul`
(peel a0) + fiber lemma + guard/arg periodicity (from `two_pow_period`) + `geom_fold_geomHalf`.

## 📋 Remaining non-headline sorries (whole repo)
- `Syracuse/SyracRV.lean:315` `syracZ_recursion` — DOABLE, 5 cores done; fiber lemma is the last piece.
- `Sec5/FirstPassage.lean:81` `stabilization` (Prop 1.11) — HEROIC analytic (multi-lap; narrow only).
- `Sec6/MixingFromDecay.lean:19` `fine_scale_mixing` — HEROIC analytic §6 (multi-lap; narrow only).
- `Statement.lean:24,31` — the two headlines (discharge only when the whole chain lands; DO NOT TOUCH).

## ⚠️ Gotchas banked this run
- `iid_apply_eq_prod` lives in `Prob/Basic.lean` now (namespace `PMF`); ValuationDist re-exports it.
- `Fin.revPerm i` defeq `Fin.rev i` but `exact`/`rw` can miss it — use `Fintype.prod_equiv Fin.revPerm`
  with an explicit per-index `rw [Fin.revPerm_apply]`, or `Finset.sum/prod` reindex.
- `Nat.Ico_succ_right` is GONE; for `Icc 1 P = (range P).map (·+1)` use a `Finset.map` embedding with
  `add_left_injective 1` (NOT `fun a b h => by omega` — the embedding fn isn't beta-reduced for omega).
- `ZMod.mul_inv_of_unit` + `ZMod.isUnit_iff_coprime` + `Nat.Coprime.pow_right` = `2*2⁻¹=1` mod `3ᵏ`.
- `map_ofNat φ 3 : φ 3 = 3` for ring homs (needs `Nat.AtLeastTwo`); ring homs preserve unit inverses
  only via right-inverse-uniqueness (I proved `φ(2⁻¹)=2⁻¹` that way, not a generic `map_inv`).

## 📁 Key files
- `TaoCollatz/Syracuse/SyracRV.lean` — stubs 1&2 done; `syracZ_recursion` = 5 cores + 1 fiber sorry.
- `TaoCollatz/Prob/Basic.lean` — now hosts `PMF.iid_apply_eq_prod`.
- `TaoCollatz/Sec5/FirstPassage.lean` — `logUnifOdd` normalized; only `stabilization` open.
- `PENDING_WORK.md` — laps fruit-1..fruit-5 (newest on top); fiber-lemma route is the fruit-5 entry.
- `DIRECTION.md` — judge pass 26 (authoritative; objective 3 is the active order).

---
**→ Next session: continue `syracZ_recursion` — prove the ZMod fiber lemma (route in PENDING_WORK
fruit-5), then wire the final assembly from the 5 proved cores. Re-read `DIRECTION.md` first.**
