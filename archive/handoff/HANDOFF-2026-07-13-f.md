# HANDOFF — 2026-07-13-f (grind lap: X11 constants `B` and `Y`)

**Branch** `main`, HEAD = `3cdd5c6`, `lake build` green (3281 jobs), working tree
clean (apart from `.obsidian/`, `tools/__pycache__/`). Re-read `DIRECTION.md`
CURRENT DIRECTIVE (judge pass 24) first — it outranks this handoff. This lap
executed directive steps 2 (`B`) and started step 3 (`Y`).

## What landed this lap (all axiom-clean = `[propext, Classical.choice, Quot.sound]`)

### ✅ Step 2 DONE — constant `B` discharged `4·10⁷ → 64` (commit `3625037`)
The (7.50) transverse-localization constant is now `64`, machine-verified with **no
`native_decide`** (pure real-analysis over `exp`). The old `4·10⁷` came from clamping
the Chernoff tilt at `θ=1/20000` (the `holdSum_halfspace_le` quadratic-MGF box). New:
- `tiltZ_hold_closed` (`Prob/Mgf.lean`): **exact** general `Hold` MGF closed form
  `Z(l₁,l₂) = (e^{l₁+3l₂}/4)/(1 − (3/4)e^{l₁}·Z_ne3(l₂))` (generalizes
  `tiltZ_hold_fst`/`tiltZ_hold_snd`). Finite up to `θ≈0.213`.
- `tiltZ_pascalNe3_le_num`, `tiltZ_hold_le_num`: at `(l₁,l₂)=(1,−5/16)` (θ=1/16 on
  `Z=16j−5l`) give `Z_hold ≤ 76/100 < 1`, via `Real.exp_bound` + `exp_one_lt_d9`.
- `holdSum_halfspace_le_of_mgf` (`Sec7/HoldLocal.lean`): Markov-under-tilt taking the
  MGF bound as a hypothesis (so the tilt exits the `|λ|≤1/200` box).
- `fpDist_linear_tail_sharp` + `fpDist_linear_tail_le_sixteenth_sharp`
  (`Sec7/FpLocation.lean`): threshold `64` ⟹ tail `≤ 1/16`.

### ✅ Step 3 crux DONE — renewal mass per height level `≤ 1` (commit `2daf42f`)
`renewal_level_le_one : ∀ u:ℤ, ∑_j renewalMass (j,u) ≤ 1` — the "trick" (heights
strictly increase by ≥3 ⟹ each level visited at most once). Reduced to the 1-D height
marginal `hold.map Prod.snd`, renewal equation `U = δ₀ + F⋆U` (`renewalHeight_eq`),
strong induction (`renewalHeight_le_one`). New API in `FpLocation.lean`:
`holdSnd_support_ge`, `pmf_map_add_apply`, `iidSum_holdSnd_apply`, `renewalHeight`
(+`_zero_of_neg`/`_eq`/`_le_one`), `renewal_level_le_one`.

## NEXT — finish Lap C (`Y` explicit), then Lap D (gated)

**Remaining Lap C assembly** (mechanical; the hard part is done):
1. **Single-step height Chernoff**: `∀ T:ℝ, ∑_d [T ≤ d.2] hold d ≤
   ofReal(e^{−μT})·tiltZ hold (expW2 0 μ)` — Markov in the 2nd coordinate. Reuse the
   `tiltZ_hold_snd` closed form; need a numeric bound `tiltZ hold (expW2 0 μ) ≤ ofReal M`
   at μ≈0.0575 (analog of `tiltZ_hold_le_num`; `tiltZ_hold_snd_le` only covers |μ|≤1/100).
   Pattern: `tiltZ_pascalNe3_le_num` → `tiltZ_hold_le_num` mirrors this exactly.
2. **Assembly** via `fpDist_le_renewal_conv`:
   `∑_e [s+Y≤e.2] fpDist s e ≤ ∑_p [p.2≤s] renewalMass p · (∑_d [s+Y−p.2 ≤ d.2] hold d)`;
   group by level `u=p.2≤s`, apply `renewal_level_le_one` (mass ≤1 per level), reindex
   `w=s−u≥0`, sum geometric `∑_w e^{−μw}` ⟹ explicit `Y`. Target `Y≈139`; any `Y≤~250`
   is fine (box dominated by Y).
3. New `fpDist_height_tail_le_sixteenth_sharp : ∀ s, ∑_e [s+Y₀≤e.2] fpDist s e ≤ 1/16`
   at explicit numeral `Y₀`. Leave the existential `fpDist_height_tail_le_sixteenth` in
   place.

**Lap D (epsBW-gated — JUDGE's call, do NOT touch epsBW)**: wire `64` and `Y₀` into
the `ManyTriangles.lean` box (`40000000` at lines ~1618/2706/2728, the existential Y at
2708). With `B=64`, `Y=139`: box `= √(⌈(5·139+64)/16⌉² + 139²) = √(48²+139²) ≈ 147`.
Report to judge; `epsBW` re-freeze `10⁻⁹⁰→10⁻¹⁰⁰⁰` (sep≈230) makes it fit. Until then,
`fpDist_any_triangle_le` stays sorried.

## Notes / gotchas
- `ManyTriangles.lean` split (BLUEPRINT §2) still queued; do it before editing that file
  for step 3 (it's 5204 lines and re-elaborates fully per edit). `B` lived in
  `FpLocation.lean` so was split-independent; step 3's new lemmas can also live in
  `FpLocation.lean` (where `renewalMass`/`fpDist_le_renewal_conv` are), keeping the big
  file untouched until Lap D rewiring.
- `Real.exp_bound` extraction pattern that worked:
  `have h := Real.exp_bound (x:=…) (by rw[abs_le]; constructor <;> norm_num) (n:=6) (by norm_num);
   simp only [Finset.sum_range_succ, Finset.sum_range_zero] at h; norm_num [Nat.factorial] at h;
   rw [abs_le] at h; nlinarith [h.1, h.2]`.
- `if`-with-Decidable-instance mismatch after `PMF.pure_apply` vs a statement's `if`:
  `congr_arg₂ (· + ·) (by congr) htail` bridges it (plain `rfl` fails on instances).
- Numeric scratch: `tools/tao_mgf_B_check.py` (verifies the `B` bounds+margins).
