# HANDOFF — 2026-07-12-c (seventh box session, laps 46–48)

**Branch**: `main`, HEAD `9027332`, tree clean, `lake build` green (3276 jobs),
all new proved declarations `#print axioms` = `[propext, Classical.choice,
Quot.sound]`. No uncommitted edits.

## What happened (operator red-queue steering: RED → YELLOW)

- **Lap 46 (X8/X10 statement design)** — `Q_black_edge` DECOMPOSED. New
  `Sec7/BlackEdge.lean` (Monotone/Unroll → BlackEdge → Bridge import chain;
  `prop_7_8`/`Q_polynomial_decay` moved there). PROVED: `TriangleFamily`
  bundle + `exists_triangleFamily`, the (7.46) endpoint step
  `Q_fp_endpoint_le` (subtraction-form white damping
  `1-(1-e^{-ε³})·1_whiteStrip` times `edgeWeight·Qm(m-1)`, out-of-strip
  absorbed via `edgeWeight_of_deep`/`one_le_Qm`), the (7.52) budget bound
  `budget_le_of_mem_triangle` (lattice extent point), and the `Q_black_edge`
  case-split glue itself. OPEN (4 named kernels): `fpDist_edgeWeight_le`
  ((7.48)/(7.49)), `fpDist_white_exit` ((7.50)/(7.51)), `Q_black_edge_case2`
  assembly (mechanical once kernels land), `Q_black_edge_case3` (X9/X10/X11
  subtree). Both Case-2 kernels funnel through X6.
- **Lap 47 (X6 step 1)** — new `Sec7/FpLocation.lean`; Lemma 7.7 moved here.
  PROVED **`fpDist_le_renewal_conv`**: `fpDist s e ≤ Σ_{p₂≤s}
  renewalMass(p)·hold(e-p)` (delta-convolution form, budget induction). Key
  insight: hold strictly increases height ⇒ NO barrier condition, plain
  renewal measure. Plus `stepMass_eq_conv` (U = δ₀ + hold⋆U),
  `tsum_delta_chain`/`tsum_conv_reindex` (ℝ≥0∞ Fubini helpers),
  `map_apply_ite` (Decidable-instance bridge for PMF lemmas).
- **Lap 48 (X6 step 2, in progress)** — renewal-sum toolkit PROVED:
  **`sum_range_exp_neg_sq_le`** (`Σ_{m<N} e^{-βm²} ≤ 3+2/√β`, integral-free
  M-split), `Gweight_anti`, `exp_neg_le_four_div_sq`,
  `one_sub_exp_neg_inv_le_one_add`, `sum_range_geom_le`. Factorization chain
  and k-sum envelope numerically validated FIRST (200k trials clean;
  constants c₄=c₀/4, a=c₀²/8, b=c₀/4 off hold_local_bound's c₀=1/400).

## NEXT (exact plan in PENDING_WORK lap-48 entry)

1. **`renewalMass_bound`** (FpLocation.lean, the one X6 kernel left before
   assembly): (a) `sum_abs_AP_le` two-branch AP reindex (route written out);
   (b) `iidSum_hold_snd_zero` k-truncation (3k ≤ l support, induction via
   `iidSum_succ_apply`); (c) per-k factorization (hold_local_bound +
   ‖·‖∞ ≥ y/2 + Gweight_anti + AB+CD ≤ (A+C)(B+D)); (d) k-sum split at
   L/32 (edge killed by `exp_neg_le_four_div_sq`, central by the AP lemmas).
2. **`fpDist_location_bound`** (X6 step 3): convolve renewalMass_bound with
   hold n=1 bounds over `fpDist_le_renewal_conv`, split l₁ ≤ s/2 vs > s/2.
3. Then the BlackEdge Case-2 kernels (`fpDist_edgeWeight_le`,
   `fpDist_white_exit`), then `Q_black_edge_case2` assembly; X9 Lemma 7.9
   skeleton for Case 3.

## Gotchas this session (also in PENDING_WORK per lap)

- PMF.map_apply/pure_apply emit `Classical.propDecidable` ites ≠ hand-written
  `instDecidableEqProd` ites ("synthesized instance not defeq"); bridge with
  `map_apply_ite` / `tsum_congr fun a => by congr`. if_pos/if_neg/by_cases
  are instance-agnostic; only rw/calc pattern matching breaks.
- `rw [zero_mul]` rewrites ALL occurrences of the matched instantiation;
  duplicated rewrites in a chain then fail.
- `exact zero_le` (no underscore) for ℝ≥0∞ goals; `Real.add_one_le_exp` is
  `x + 1 ≤ exp x` (not `1 + x`); `pow_le_pow_left` gone — feed nlinarith
  `mul_self_le_mul_self`; `inv_le_inv_of_le` gone — use `inv_anti₀`.
- Anonymous-constructor set membership under `Set.indicator_of_mem` needs a
  named `have hmem : _ ∈ …` (inline ⟨…⟩ fails expected-type inference).
- `set M := …` folds goals but NEW `have` goals show `↑M`: `rw [hM]` before
  `push_cast`.

## Open ledger (src sorries, 26)

FpLocation ×2 (renewalMass_bound, fpDist_location_bound — X6),
BlackEdge ×4 (edgeWeight/white-exit/case2/case3 — X8/X10/X11),
X5 Bridge ×3, X1/X2 key_fourier_decay + dTV (Decay, MixingFromDecay),
White, SyracRV ×3, ValuationDist ×2, Sec5 ×2, Statement ×2, Holding:289,
Prob/Basic:154, Collatz:80, Valuation:112.
