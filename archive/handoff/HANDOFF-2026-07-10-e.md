# HANDOFF — 2026-07-10-e (fifth box session, laps 22–28)

**Branch**: `main`, HEAD `1822701`. Tree clean (modulo .obsidian/blueprint noise),
`lake build` green, every new item `#print axioms` = `[propext, Classical.choice,
Quot.sound]`. Nothing uncommitted.

## Mission was: open the S3 front (risk kernel 1, Lemma 2.2). Status: OPENED AND DEEP

All four mission items delivered, then further: the 2-D circle-method route for
`hold_local_bound` is now proved end-to-end EXCEPT Gaussian summation + tilting.

By commit:

22. **`650e5a0`** — Lemma 2.2(i)(ii) STATED (8 sorried statements): `Prob/LocalBound.lean`
    NEW (`Gweight` factored from Unroll + pos/nonneg/le_two, `iidSum`,
    `geomHalf/geomQuarter/pascal_{local,tail}_bound`); `holdSum` + `hold_{local,tail}_bound`
    (d=2, mean (4,16), sup-norm) in `Sec7/Unroll.lean`. RATIFY-DRIFT notes: Gweight(1+n)
    vs G_n, ℕ index set, sup vs Euclidean norm. Judge: ratify vs paper pp.14–16, p.42.
23. **`5bc263a`** — d=1 warm-up PROVED: `negBinomial_apply` (exact `C(L-1,n-1)·2^{-L}`,
    iid peel + hockey stick `sum_range_choose_col`/`sum_Ico_choose_shift`) and
    `pascal_eq_map_iid`; generic `PMF.iid_support_coord` in Prob/Basic.
24. **`3635712`** — iidSum calculus: `iidSum_zero/succ/add` (renewal additivity via PMF
    monad laws), `iidSum_iidSum`, `pascal_eq_iidSum`, `iidSum_pascal_apply`
    (= `C(L-1,2n-1)·2^{-L}`; pascal_local_bound is now pure Stirling estimates).
25. **`6437843`** — circle-method entry: `iidSum` GENERALIZED to any AddCommMonoid;
    `iidSum_map`, `PMF.apply_le_map_apply` (mod-N reduction merges mass ⇒ upper bounds
    need NO tail truncation), `holdSum_eq_iidSum`, `modPair`, `holdSum_le_modPair`.
26. **`699c991`** — circle-method CORE (`Prob/CharFn.lean` NEW, fully proved):
    1-D/2-D orthogonality (`sum_stdAddChar_mul`, `sum_pairChar`), `charFn`,
    **`charFn_inversion`** (exact finite Fourier inversion on `ZMod N × ZMod N`),
    `apply_toReal_le_sum_norm_charFn`, `charFn_bind/map_add/iidSum` (r̂ of iid sum
    = r̂^n), **`iidSum_apply_toReal_le`** (`P(S_n = x) ≤ N⁻² Σ_ξ ‖r̂ ξ‖ⁿ`);
    composite `holdSum_toReal_le_charFn` in Unroll (every modulus N).
27. **`2a4ff10`** (+ atoms in `eea4cd3`) — (D) inputs: hold atom masses
    `(1,3)↦1/4, (2,5)↦1/16, (2,7)↦3/64, (2,8)↦1/32` (`hold_apply_pin/_two`,
    `pascalNe3_toReal`); **`charFn_normSq_pair_bound`** (two-atom anti-concentration,
    double-sum normSq expansion) and **`one_sub_re_stdAddChar_ge`** (Jordan
    `8·dist(j/N,ℤ)² ≤ 1 - Re e(j/N)`).
28. **`1822701`** — **(D) PROVED: `charFn_hold_decay`** — for N ≥ 4,
    `‖charFn (hold.map (modPair N)) ξ‖² ≤ 1 - ((nd ξ₁/N)² + (nd ξ₂/N)²)/768`,
    uniform in N. New `nd` cyclic-distance calculus in CharFn.lean (`nd_le_natAbs`
    by emod/ediv + generalize-then-omega, `nd_sub_le`, `nd_cast`), `pair_transfer`.
    Differences (1,2),(0,2),(0,3) of the four atoms give nondegeneracy;
    triangle: ξ₁ = j₁ - j₂, ξ₂ = j₃ - j₂.

## The remaining chain for `hold_local_bound` (X6's Lemma 2.2 input)

Proved: `P(holdSum n = v).toReal ≤ N⁻² Σ_ξ ‖charFn(hold mod N) ξ‖ⁿ` (all N) and
`‖φ(ξ)‖² ≤ 1 - D(ξ)/768` with `D(ξ) = (nd ξ₁/N)² + (nd ξ₂/N)²`. Remaining:

1. **(E) Gaussian summation** (next lap; plan detailed in PENDING_WORK.md Lap 28
   entry): `‖φ‖ⁿ ≤ (1 - D/768)^{n/2} ≤ exp(-nD/1536)`; the ξ-sum factorizes into
   two 1-D sums over `ZMod N`; index by `m = nd t` (each value hit ≤ 2×); at
   `N = ⌈√n⌉ + 1` the 1-D sum is O(1) by geometric domination
   (`exp(-am²) ≤ exp(-am)`). Yields the **center-regime bound**
   `P(holdSum n = v) ≤ C/(1+n)` for ALL v — state as `holdSum_apply_le_center`.
2. **(F) exponential tilting** for the Gaussian/exp factor of Lemma 2.2(i) off-center:
   tilted PMF `hold_λ`, MGF finiteness on a strip (= Lemma 7.6 engine, (7.30)),
   the identity `P(S_n = v) = M(λ)ⁿ e^{-λ·v} P_λ(S̃_n = v)`, then apply the center
   bound to the tilted walk (its charFn decay needs the same 4 atoms with
   λ-perturbed masses — the atom masses are continuous in λ, so a small fixed
   λ-box keeps masses ≥ half their λ=0 values; alternatively re-run the atom
   computation symbolically). This is the remaining real design work.
3. d=1 instances: `pascal_local_bound` via `iidSum_pascal_apply` + Stirling
   (corpus: 2026-06-19-mathlib-stirling-factorial-bounds.md has the missing
   upper bound recipe); `geomHalf/geomQuarter` similar (exact negative binomials).
4. Then Lemma 7.6 (Hold exp tail) + the k-free union bound = Lemma 7.7
   (`fpDist_location_bound`, X6) — see HANDOFF-2026-07-10-d for the s-induction
   reformulation note.

## Open ledger (unchanged elsewhere)

Headline chain still rests on `Q_black_edge` (X8/X10, Monotone.lean) +
`fpDist_location_bound` (X6, Unroll.lean); X6 now has the S3 machinery growing
under it. Other pre-existing leaves untouched (key_fourier_decay X1/X2, dTV lemma,
SyracRV ×3, ValuationDist ×2, Sec5 ×2, Sec6, White, Decay, Statement ×2).
New sorries added this session: the 8 Lemma 2.2 statements (intended; they ARE the
S3 statement layer).

## Gotchas this session (also useful next lap)

- `le_or_lt` is gone in current mathlib — use `le_or_gt` / `lt_or_ge`.
- `PMF.map_pure` doesn't exist; it's `PMF.pure_map`. `(pure ∘ f) a` goals close by `rfl`.
- `Finset.sum_pair`-shaped rewrites need `(f := fun y => …)` explicit or HO-unification fails.
- omega can't see `Int.emod/ediv` with a VARIABLE modulus: provide
  `Int.ediv_add_emod`, sign facts, then `generalize (N:ℤ) * (x / N) = t` and omega
  on the linearized system. (`Int.ediv_neg'` doesn't exist.)
- `congrArg _ (funext …)` on `PMF.map f p` unifies against the wrong argument
  (map's function is the FIRST explicit arg) — use `have hf : f₁ = f₂ := funext …; rw [hf]`.
- ENNReal numeral juggling is misery; state atom masses in `.toReal` form and use
  `geomQuarter_toReal`/`pascalNe3_toReal` + `norm_num`.
- Big nlinarith goals with products: factor into a helper lemma (`pair_transfer`)
  with explicit calc; give final linarith `sq_nonneg` hints to establish `X ≥ 0`.
- `Complex.mul_conj' : z * conj z = ↑‖z‖²`; `Complex.re_sum`, `Complex.re_ofReal_mul`,
  `Complex.abs_re_le_norm`, `Complex.inv_eq_conj (norm=1)`, `AddChar.map_neg_eq_inv`,
  `Real.mul_le_sin` (import `…Trigonometric.Bounds` explicitly), `ZMod.val_intCast`,
  `ZMod.natCast_eq_zero_iff` are the working names.

## State

`lake build` green at `1822701`. `Prob/CharFn.lean` + `Prob/LocalBound.lean` are the
new S3 layer; TaoCollatz.lean imports both. PENDING_WORK.md top entries (laps 22–28)
carry the detailed (E)/(F) attack plans.
