# Handoff: C10 — (6.3) union bound + marginal law PROVED; 3 pure tail sorries remain

**Real date**: 2026-07-14 19:41 UTC · **Branch** `main`, build 🟢 green (full `lake build`, 3292 jobs),
tree clean after commit. **Read `DIRECTION.md` first — it outranks this doc.**

## What landed this lap (1 green commit, all in `Sec6/MixingError.lean` + a 1-line SyracRV export)
Per DIRECTION objective 1: the C10 tail node `prob_not_globalGood_le` was ONE opaque sorry; it is now
**fully assembled** from the (6.3) union decomposition, resting on **three named per-event tail
sorries**. The genuinely novel content (the **marginal law**, flagged in the prior handoff as "the
genuine multi-lap content") is **proved axiom-clean** (`[propext, Classical.choice, Quot.sound]`):

- **`iidMap_pre`** `(geomHalf.iid n).map (fun a => pre a r) = iidSum geomHalf r` for `r ≤ n` — the
  prefix-block marginal. Proof: `pre a r = (∑ i, ·) ∘ (restrict-to-first-r)`, then the prefix
  restriction marginal `iid_map_castLE` (de-privatised in `SyracRV.lean`).
- **`iid_map_coord`** — single-coordinate marginal `(p.iid n).map (· i) = p` (induction on the cons
  structure). **`iidSum_one`** `iidSum p 1 = p`. **`pre_eq_fin_sum_castLE`** (renamed to avoid a
  collision with `ValuationDist.pre_eq_fin_sum`).
- **`masked_tsum_map`** — pushforward bridge: `∑' a, if Q(φ a) then 0 else P(a) = ∑' b, if Q b then 0
  else (p.map φ)(b)`. Turns a masked probability into a masked `iidSum` tail.
- **`not_globalGood_pointwise_le`** — the (6.3) union bound, POINTWISE: `¬globalGood`-indicator mass ≤
  G1 term + Σ_{i:Fin n} G2 term + Σ_{r∈Icc 1 n} G3 term. Proved by the three-way case split (¬G1 /
  ∃¬G2 / ∃¬G3) + `Finset.single_le_sum`.
- **`caThr_nonneg_large`** — `0 ≤ caThr (caConst A) n` for large `n` (via `log_le_eps_mul_of_large`,
  mirroring `eventually_ca_window`). This is what `globalGood_subset_mainEvent` consumes.
- **`prob_not_globalGood_le`** — full assembly: pointwise union → `tsum_le_tsum` → split via
  `Summable.tsum_add` + `Summable.tsum_finsetSum` → per-event bounds → `n^{-(A+2)}·(1+2n)` → `n→m`
  arithmetic. Final constant **C = 6**, bound `≤ 6·m^{-A}`.

`error_l1_high_bound` (the C10 crux node) remains the proved wrapper on top; C10 is complete **modulo
the three tail sorries** below.

## → Next (the ONLY remaining C10 content): 3 named tail lemmas, all in `Sec6/MixingError.lean`
Each is a **pure `geomHalf_tail_bound` calculation** — no event algebra, no novel kernel. Route for all:
`masked_tsum_map` (rewrite masked prob → masked `iidSum` tail via the marginal) → dominate the mask by
`[λ ≤ |L − mean|]` → `geomHalf_tail_bound` (`Prob/LocalInstances.lean:540`, gives `≤ 2·Gweight(1+r)(cλ)`,
`c = 1/400`) → `Gweight ≤ n^{-(A+2)}` via `caConst_tail_exponent` (`A+3 ≤ caConst/400`).

1. **`g2_mass_le`** (EASIEST — do first). Single coord, marginal is `geomHalf` via **`iid_map_coord`** +
   **`iidSum_one`** (`iidSum geomHalf 1 = geomHalf`). Deviation `λ = 2·C·log n − 2 ≥ C·log n`; the
   `exp(−cλ)` part of `Gweight` gives `n^{−cC} ≤ n^{−(A+3)}`. No suffix marginal needed.
2. **`g1_mass_le`**. Prefix `pre a n`, marginal `iidSum geomHalf n` via **`iidMap_pre n n`**. Deviation
   `λ = 2n − caThr ≈ 0.415n` (linear); `exp(−cλ)` is exp-small, ≤ `n^{-(A+2)}` for large n. Need
   `caThr ≤ 2n` (i.e. `n·log3/log2 − C²log n ≤ 2n`, true since `log3/log2 < 2`; reuse `hlog3lt`
   pattern from `lRange_card_le`).
3. **`g3_mass_le`** (HARDEST — needs the **suffix marginal**). `sufSum a r` = last-`r`-block sum; needs
   `(geomHalf.iid n).map (fun a => sufSum a r) = iidSum geomHalf r`. This is the last-block analogue of
   `iidMap_pre`. Cleanest route: `cexpect_iid_append`-style block split (head = first `n−r` coords with
   trivial observable `f≡1`, tail = last `r`), OR a `Fin.natAdd` reflection of `iid_map_castLE`. Once the
   suffix marginal is in hand, the tail arithmetic is: `λ = C(√(r log n)+log n)`; the `√(r log n)` part →
   Gaussian `≤ n^{−c²C²/2}`, the `+log n` part → `exp(−cλ) ≤ n^{−(A+3)}` (this is WHY the window carries
   the extra `log n`); both `≤ n^{-(A+2)}`, so `Gweight ≤ 2n^{-(A+2)}`, `≤ n^{-(A+2)}` after the ·2 room.
   (The `·2` in the per-event target is slack: geomHalf_tail_bound's `C=2` × `Gweight` — recheck the
   constant; if `2·Gweight` overshoots `n^{-(A+2)}`, bump the per-event RHS in all three statements +
   the assembly's `1+2n` factor uniformly. The final `C` is generous.)

## Rails honored / notes
- `globalGood` stays documented as an ENLARGEMENT of `Eₖ` (pass-29 rail). Watched statements
  (`fine_scale_mixing`/`stabilization`) and ratified pins untouched.
- Census: C10 went 1 → 3 named sorries (DECOMPOSITION = progress, per directive). After C10 lands:
  **pin C8** (objective 2), then prove C7 (1.19), then close C8, then C9.
- Gotchas this lap: `PMF.pure_map` (not `map_pure`); `Summable.tsum_add`/`.tsum_finsetSum` (dot form);
  bracket goal-split `<;> [t;t]` invalid → `<;> first | t | t`; `one_div_le_one_div_of_le` for
  neg-exponent rpow base-monotonicity; `pre_eq_fin_sum` already taken by `ValuationDist`.

## 📁 Key files
- `TaoCollatz/Sec6/MixingError.lean` — marginal infra (~540–615), union pointwise (~640), the 3 open
  tail sorries `g1/g2/g3_mass_le` (~685/693/705), assembly `prob_not_globalGood_le` (~720).
- `TaoCollatz/Prob/LocalInstances.lean:540` `geomHalf_tail_bound`; `Prob/LocalBound.lean` `iidSum`,
  `Gweight`; `Prob/Basic.lean` `tsum_map_mul`, `cexpect_iid_append` (suffix-marginal route).
- `Sec6/MixingMain.lean:12` `eventually_ca_window` (template for `caThr_nonneg_large`, now used).
