# HANDOFF — 2026-07-10 (first box session, laps 1–5)

**Branch**: `main`, HEAD `ba4d351`. Tree clean, `lake build` green, all proved items
`#print axioms` = `[propext, Classical.choice, Quot.sound]`. Nothing uncommitted.
(Untracked `.obsidian/` and a stray root `tao-2019-...-SUMMARY.pdf` predate this
session; left alone.)

## Mission context

De-risk the three risk kernels (X8/X10, S3, X3) per the operator instruction; then
grind. Orientation docs: `BLUEPRINT.md` (D1–D8 + node ledger), `EXECUTABILITY.md`
(now carries this session's findings), `PENDING_WORK.md` (per-lap log, newest on top).

## What happened (by lap)

1. **`299233e`** — Harness extended (`tools/check_blueprint.py` checks 7–11, all pass):
   Prop 7.3 coordinate convention validated end-to-end (RATIFY queue item 3 retired —
   `renewal_white_encounters` is correct as stated); Lemma 7.4 l*/j* decomposition in
   exact ℚ at 3 instances incl. **giant triangles** (sizes ~n·log3, NOT O(log 1/ε) —
   recorded trap); Case-2 white-exit MC ≈ 0.987; Lemma 7.10 Σ-separation verified;
   ε=1/10⁴ survives every §7 site (D4 ratified). **Statement bug fixed** (RATIFY-5):
   `black_structure` separation is between triangle POINT SETS, not corners.
   Ledger conf: X3 70→75, X8 65→70, X10 60→65.
2. **`29deeae`** — θ-exactness (`θq_succ_j_exact`, `θq_pred_l_exact`) + weakly-black
   claims (i) j/l-forms, (ii), (iii) (paper p.38); `Q_white_contract`, `Qm_le_rpow`.
3. **`3e3e4c7`** — **(7.16) proved**: `θq_lower_bound` (`3^{-(n-2j)} ≤ |θ|`, the ±1/3
   3-adic argument) + `black_nine_le` (black ⇒ `n-2j ≥ 9`).
4. **`1990c8b`** — (7.18) equality engine `θq_iterate_exact`; `θq_up_run` +
   `black_run_le` (upward black runs capped: `2^t ≤ ε·3^{n-2j}` ⇒ l* exists).
5. **`ba4d351`** — (7.18) inequality forms (`θq_iterate_abs_le`, sfrac range/idem
   lemmas); **corner map defined**: `upRun`/`lstar`/`leftRun`/`jstar` (Nat.find,
   classical) + full spec (`black_of_le_lstar`, `le_lstar`, `white_above_lstar`,
   `leftRun_pos`, `black_of_jstar_le`, `jstar_maximal`).

All of laps 2–5 live in `TaoCollatz/Sec7/Triangles.lean` (+ `Monotone.lean` for lap 2).

## NEXT (X3 critical path, in order)

1. **`θq_left_run`** (mirror of `θq_up_run`): along the black row at height l*,
   `θ(j*, l*)` relates to `θ(j, l*)` by exact 9-multiplications — same proof shape,
   using `θq_succ_j_exact` on black row points (black ≤ ε < 1/18 allows each step).
2. **Fibre identity**: `|θ(j,l)| = 9^{j-j*}·2^{l*-l}·|θ*|` for black strip points
   (compose `θq_up_run` down the column with `θq_left_run` along the row).
3. **Δ*-membership as ℚ-inequality**: `9^{j-j*}2^{l*-l}|θ*| ≤ ε` (immediate from 2 +
   blackness); then the ℝ-log `triangle` membership by monotone algebra
   (`s* := Real.log (ε/|θ*|)`).
4. **Claim (*) cases** (paper pp.39–41) using claims (i)–(iii) + `θq_iterate_abs_le` /
   `_exact`; then fibre equality Δ* = corner-fibre, separation, and `black_structure`
   assembly. Decompose into named sorries in `Triangles.lean` when starting — that
   RAISES the src sorry count and is progress.

Parallel threads if X3 stalls: Lemma 7.9 induction skeleton over `Q_rec` (X9), the
(7.45) unrolling statement design (X8), or S3's d=1 negative-binomial half.

## Gotchas learned this session (also grep the corpus first)

- This mathlib: `le_or_lt`/`Nat.le_or_lt` GONE — use `le_or_gt`/`Nat.lt_or_ge`;
  `Nat.pos_pow_of_pos` → `Nat.pow_pos`; `Nat.pos_or_eq_zero` → `Nat.eq_zero_or_pos`;
  `Int.abs_lt_one_iff` for |k|<1 ⇒ k=0; `pow_unbounded_of_one_lt` exists.
- `Nat.find` in defs: `open Classical in` on the def, `classical` tactic in proofs,
  and pass the existence witness EXPLICITLY to `Nat.find_min`/`Nat.find_spec`
  (underscore → stuck DecidablePred metavariable).
- omega atom mismatch: don't `unfold leftRun` in one hypothesis and not another —
  keep `leftRun`/`upRun` folded and rw [leftRun] only where Nat.find API is applied.
- Our `sfrac` range is `[-1/2, 1/2)` (mirror of paper's `(-1/2, 1/2]`); harmless
  (only |sfrac| used; odd denominators) — documented at `sfrac_mem`.
- `black` is a def; `have h : |θq …| ≤ epsBW := hb` works by defeq (no unfold needed
  in term position, but `simpa` output keeps the `black` form).

## State of the sorry ledger (src, 23 → unchanged count-wise; statement chain)

Open sorries = the spec'd statement chain (Statement, C-chain, S-chain, X-chain heads:
`black_structure`, `renewal_white_encounters`, `key_fourier_decay`, `prop_7_8`,
`Q_polynomial_decay`, etc.). No wip/, no axioms, no native_decide.
