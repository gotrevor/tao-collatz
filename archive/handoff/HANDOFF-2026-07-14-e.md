# HANDOFF — 2026-07-14-e (X8 COMPLETE; X11 crux opened — all 3 bridges proved)

**Branch** `main`, HEAD = `b206c80`, `lake build` green (3282 jobs), working tree clean.
**Re-read `DIRECTION.md` CURRENT DIRECTIVE FIRST** — it outranks this handoff, and re-read it
every lap (the judge updates it between laps). Directive routes to the Case-2/Case-3 assembly:
**Case 2 is now DONE; only X11 `Q_black_edge_case3` remains.**

## What landed this session (6 axiom-clean proof commits)

All verified `#print axioms = [propext, Classical.choice, Quot.sound]` (no `sorryAx`).

1. **`fpDist_white_exit`** (`f9303e6`) — the (7.50)/(7.51) Case-2 white-exit bound. It's a strict
   weakening of the proved deep kernel `fpDist_white_exit_deep` (no `s≤m/log²m` budget hyp, mass
   `51/100≤p₀`), but `ManyTriangles` imports `BlackEdge` so the deep kernel wasn't visible upstream.
   **Fix = relocation, statements FROZEN verbatim:** created `TaoCollatz/Sec7/BlackEdgeQ.lean`
   (imports `ManyTriangles`) and moved the Q-assembly tail of `BlackEdge.lean` there
   (`fpDist_white_exit`, `Q_black_edge_case2`, `Q_black_edge_of_case3`, `prop_7_8_of_black_edge`,
   `Q_polynomial_decay_of_prop_7_8`). `budget_le_of_mem_triangle` STAYED in `BlackEdge` (ManyTriangles
   uses it). `Case3.lean` now imports `BlackEdgeQ`. Proof = 4-line derivation from the deep kernel.
2. **`Q_black_edge_case2`** (`3384a59`) — the (7.46)–(7.51) Case-2 assembly. **All of X8 Case-2 is
   now COMPLETE** (both kernels + assembly). Entry `Q_le_fpDist_expect` (ℝ≥0∞→ℝ), per-endpoint
   `Q_fp_endpoint_le`, (7.47) split via `Summable.tsum_sub`, bounded by `fpDist_edgeWeight_le` +
   two NEW helpers (`edgeWeight_le_one`, `rpow_neg_le_edgeWeight` i.e. `edgeWeight ≥ m^{-A}`) +
   white-exit. In `BlackEdgeQ.lean`.
3. **`fstar_markov`** (`6ff9282`) — the X9-discharged (7.56) Markov bound. `fstar_markov_le` took
   Lemma 7.9's `encExpect ≤ e^{2ε}` as an unproved hyp; `many_triangles_white` (X9) supplies it.
   Composing fixes the gate `g` and gives the hypothesis-free (7.56) input. In `Case3.lean`.
4. **`fpDist_walk_eq_fpDistPlus`** (`3eb0bb3`) — the (7.53)→(7.54) bridge: `∑_e fpDist s e·∑_v
   (hold.iid T v)·g(e+pathSum v p) = ∑_x fpDistPlus s p x·g x` (p≤T). `iid_pathSum_law` + bind/map
   unfolding. In `Case3.lean`.
5. **`bigTriangle_walk_le`** (`b206c80`) — per-`p` E∗ term; **route-decisive validation** that
   bridge #4 composes with `triangle_encounter_le` (X10). In `Case3.lean`.

## THE only remaining §7 sorry: `Q_black_edge_case3` (`Case3.lean:1062`, X11)

The (7.53)–(7.67) Case-3 chain — deep triangle start, `m/log²m < s ≤ O(m)`. Feeds
`Q_black_edge` → `prop_7_8` → `Q_polynomial_decay` (all already DI-assembled in `Case3.lean`),
which close §7 monotonicity the moment this lands. This is the last route-decisive blocker on
the whole headline (Prop 1.17's Case-3 chain).

### X11 decomposition — three bridges NOW PROVED, remaining assembly (see PENDING_WORK.md top)
- **X11a `estar_union_le`** (NEXT): sum `bigTriangle_walk_le` (✓) over `p ∈ range(T+1)` at
  `s'=⌈4^A(1+p)³⌉`. Needs (a) `Σ_p (1+p)^{-2} ≤ 2` (telescoping `1/(k+1)² ≤ 1/k−1/(k+1)`), since
  `s' ≥ 4^A(1+p)³` ⟹ `A²(1+p)/s' ≤ A²·4^{-A}(1+p)^{-2}`; (b) geometric `Σ_p exp(−c·A²(1+p))` +
  the comparison `exp(−cA²) ≤ (const)·A²·4^{-A}` for `A ≥ A₀`. Net E∗-mass `≤ C'·A²·4^{-A}`.
  Regime OK: `T = encWindowIter A K R = O_{A,ε,R}(1)`, so `s'=O(1) ≤ m^{0.4}` for `m ≥ C_{A,ε}`.
- **X11c `few_whites_le`**: `fstar_markov` (✓) + `deterministic_encounter_claim` (✓);
  `K=⌈10A/epsBW³⌉`, `R:=⌈(K+(A+3)log10+2)/ε⌉`, {reaches R} ⊆ F∗ via `encFold_banked_le`.
- **X11d body** = `Q_black_edge_case3`: `Q_le_damped_iter` + (7.54) col split (`fpDistPlus_col_tail`,
  `budget_le_of_mem_triangle`) + few-white damping (weights ≤ m^A/10^A) + X11a + X11c.
  **⚠ two reconciliations X11d must handle:** the E∗ event uses the PHASE point `((pos p).1−1,…)`
  (per `deterministic_encounter_claim` cond (ii)) while `bigTriangle_walk_le` bounds the POSITION
  (−1 shift); and `bigTriangleSet ⌈4^A(1+p)³⌉` (ceil) vs the claim's strict `t.2.2 < 4^A(1+p)³`.

### Proved X11 machinery (all axiom-clean)
`Q_le_walk_damped`, `Q_le_damped_iter` (7.53), `iid_pathSum_law`, `fpDist_walk_eq_fpDistPlus`
(7.54 bridge), `bigTriangle_walk_le` (per-p E∗), `fstar_markov` (7.56 Markov),
`deterministic_encounter_claim` (7.67), `triangle_encounter_le` (X10), `fpDistPlus_col_tail`,
`encFold_banked_le`, `encFold_cumWhite`, `budget_le_of_mem_triangle`, `many_triangles_white` (X9).

## NEXT — hardest-first
Prove **`estar_union_le` (X11a)** in `Case3.lean`: the telescoping series `Σ_p (1+p)^{-2} ≤ 2`
(no mathlib lemma found for it) + exp-geometric comparison, summing `bigTriangle_walk_le` over
`p`. Then `few_whites_le` (X11c), then the X11d body. Full attack in `PENDING_WORK.md` top entry.

## Architecture notes / gotchas this session
- `ManyTriangles` imports `BlackEdge`; `Case3` imports `BlackEdgeQ` (which imports `ManyTriangles`).
  Root `TaoCollatz.lean` lists `…ManyTriangles, BlackEdgeQ, Case3`.
- `set c/QM/mA` AFTER specializing hyps so it folds them; later `have`s rely on defeq (set-name is
  defeq to its body). Standalone indicator `1` defaults to ℕ — force `(1 : ℕ×ℤ → ℝ)`.
- `PMF.toReal_tsum_mul_ofReal` / `PMF.tsum_mul_ofReal_le_one` (in `TaoCollatz.Prob.Basic`) are the
  ℝ≥0∞→ℝ workhorses; rewrite a `ℝ≥0∞` indicator as `ENNReal.ofReal` of the ℝ one first.
- Prod position reassoc: `ext <;> simp [add_assoc]`; then `congrArg _ h` not `rw [h]` for the
  indicator-point equality (tuple pattern with ascriptions won't `rw`-match).
- `Summable.tsum_sub ha hg`, `mul_le_one₀`, `mul_le_of_le_one_right`, `Real.rpow_le_rpow_of_nonpos`
  are the names used.
