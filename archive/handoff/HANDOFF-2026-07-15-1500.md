# Handoff: C10 §6 — obligation 3 (tail Rényi count) fully reduced to ONE analytic implication; obligation 1 error-tool banked

**Date**: 2026-07-15 ~15:00 UTC · **Branch**: `main` · HEAD = `5e4ae91`, `lake build` green (3285 jobs).
**Re-read `DIRECTION.md` CURRENT DIRECTIVE (JUDGE PASS 27) + HARD RAIL 6 EXTENDED first — it OUTRANKS
this handoff.** Do NOT edit the statements of `fine_scale_mixing` (C10) or `stabilization` (C9) — both
are in the differ watch-list. Decompose BELOW them freely. Sorry census unchanged at **4** (C10, C9,
2 `Statement.lean` headlines).

## 🎯 This session (6 axiom-clean proof commits + docs): the C10 §6 support machinery, hardest-first
Every new lemma `#print axioms`-clean `[propext, Classical.choice, Quot.sound]`; every build real-`lake
build` green. Commits in order:

1. **`syracZ_eq_tsum_condDens`** (`0b4a73b`, obl 1) — the (6.9) `l`-marginalization: `((syracZ (j+p)) Y).toReal
   = ∑' l, condDens j p l Y`. Summing the conditioned density over all tail-valuations recovers the raw
   density (exhaustiveness of the `{pre(tail)=l}` partition). ENNReal Fubini + collapse. `Sec6/MixingFromDecay.lean`.
2. **`fnat_inj_fixed_val`** (`5502020`, obl 3) — **Tao's Lemma 6.2 (offset injectivity)**, repo-native
   fixed-valuation form: among positive tuples of fixed total valuation `l`, `fnat n` determines the
   tuple. ℕ-native induction peeling the first coord (`fnat_cons`) + `two_pow_odd_eq` + `fnat_mod_two_of_pos`.
   In **`Basic/Valuation.lean`** (after `fnat_split`). Supporting: `two_pow_odd_eq`, `pre_cons_head`, `fnat_cons`.
   ⚠️ `pre_cons` was already taken (Sec7/Bridge.lean:49) — mine is `pre_cons_head`.
3. **`fnat_offset_zmod_inj`** (`0f474c7`, obl 3) — **Cor 6.3 wrapper**: bridges Lemma 6.2 (ℕ) to mod-`3^{j+p}`
   offset injectivity `tailDens` needs, given the window bound `fnat < 3^{j+p}`. Cancels the unit `(2⁻¹)^l`
   → congruence → nat equality → 6.2. `Sec6/MixingFromDecay.lean` (after `tailDens_renyi_le`).
4. **`osc_le_two_mul_l1`** (+ **`fiber_card`**) (`819723e`, obl 1) — the **error-term tool**: `osc(c) ≤ 2·∑|c Y|`.
   Turns "small total mass" into "small oscillation" (bounds `osc(syracZ − ∑condDens) ≤ 2·P(Ē)` and the
   truncation tail). Conditional average is an `L¹`-contraction via a `fiber_card` double-count; `fiber_card`:
   the `3ᵐ`-scale castHom fiber has exactly `3^{n-m}` points. `Sec6/MixingFromDecay.lean` (after `osc_eq_sum_norm_devC`).
5. **`fnat_lt_of_prefix_bound`** (+ **`sum_two_pow_lt`**) (`9913ad3`, obl 3) — the **window-bound geometric
   algebra**, pure-algebra half of Tao (6.14)→(6.15): given `∀ m<p, 3^{p-1-m}·2^{pre vt m+(p-m)} < 3^{j+p}`
   (prefix valuations not too large), `fnat p vt < 3^{j+p}`. ×2^p, split `2^p=2^m·2^{p-m}`, sum `∑2^m<2^p`.
   `Sec6/MixingFromDecay.lean` (before `fnat_offset_zmod_inj`).

## 📊 C10 status — all surrounding machinery banked; exactly 3 analytic gaps remain (all route through event `E`)
The single open C10 spine sorry is `fine_scale_mixing` (`Sec6/MixingFromDecay.lean`, ~line 1456). Its
three obligations:

- **Obligation 1 (event scaffold / gate)**: marginalization ✓ (`syracZ_eq_tsum_condDens`), error-tool ✓
  (`osc_le_two_mul_l1`). **GAPS**: (i) stopping time `k` + events `E`/`Eₖ`/`Bₖ`/`Cₖ,ₗ` as `DecidablePred`s
  on `Fin n → ℕ`; (ii) density decomposition `syracZ = ∑_{k,l} condDens^E + error` with a *windowed*
  condDens carrying the `E∧Bₖ` indicator; (iii) `P(Ē) ≪ n^{-A-1}` (sub-Gaussian tail, reuses §7).
- **Obligation 2 (`hunif` uniform head decay)**: unchanged — per-ξ valuation bookkeeping feeding
  `condDens_osc_le` (see prior handoff `HANDOFF-2026-07-15-1200.md` §📋 item 2).
- **Obligation 3 (tail single-point mass M)**: **FULLY REDUCED THIS SESSION** to one implication. Chain is
  complete + machine-checked: `fnat_lt_of_prefix_bound` → `fnat_offset_zmod_inj` → windowed `tailDens Y ≤
  2^{-l}` → `M` → `tailDens_renyi_le` (have) → the `√` in `condDens_osc_le` (have). **GAP**: the single
  analytic implication `window (6.12) ⟹ ∀ m<p, 3^{p-1-m}·2^{pre vt m+(p-m)} < 3^{j+p}`.

**The three gaps all converge on the sub-Gaussian event `E`/window (6.12) — THAT is the remaining crux
kernel.** Tao (6.12): `|a_{[i+1,j]} − 2(j−i)| ≤ Cₐ√((j−i)log n) + log n`. ⚠️ Genuinely real-analysis-heavy
(√, log, exp, Young's inequality) with a CRITICAL leading order — typical `fnat ≈ 4^{k+1} ≈ 3^n`, so the
window's job is to control the O(√) fluctuation right at the `3^n` boundary. Multi-lap sub-project.

## 📁 Key facts for next session
- Paper §6 text extracted to scratch earlier; the relevant pages are the full PDF `papers/tao-2019-almost-all-orbits.pdf`
  pp.30–32 (Lemma 6.2, Cor 6.3, the (6.14)→(6.15) geometric bound, the event stack). Re-extract with
  `pdftotext -f 28 -l 32`.
- `condDens`/`tailDens` defs + `condDens_osc_le` + `tailDens_renyi_le` are the "plug M/D in" lemmas — all
  proved; wiring them into `fine_scale_mixing` awaits obligation 1's scaffold.
- PENDING_WORK.md fruit-23 (newest) → fruit-19 = this session's running plan; fruit-18 → fruit-8 = prior.

---
**→ Next session (hardest-first, from fruit-23): recommended (a) define the window predicate `W n vt` (6.12)
in reals + START the Young estimate `W ⟹ per-prefix hyp` (finishes obl 3); alternative (b) define stopping
time `k`/events + decompose `fine_scale_mixing` into named sub-sorries wiring the banked machinery (obl 1
gate skeleton). Re-read DIRECTION.md JUDGE PASS 27 + HARD RAIL 6 first. Working tree clean at `5e4ae91`,
build green 3285.**
