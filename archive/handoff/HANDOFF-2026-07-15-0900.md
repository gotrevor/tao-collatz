# Handoff: C10 §6 Fourier machinery — the tail/head reindex bricks landed; orientation pinned from source

**Date**: 2026-07-15 ~09:00 UTC · **Branch**: `main` · HEAD = `6f6080f` (before handoff commit),
`lake build` green (3285 jobs). **Re-read `DIRECTION.md` CURRENT DIRECTIVE (now JUDGE PASS 27) first —
it OUTRANKS this handoff.**

## ⚖️ Concurrent judge pass 27 (not mine) — committed verbatim this handoff
A judge/altitude lap ran in a pinned worktree (`~/src/tao-collatz-judge27`, pinned at `8505bd4`) and
left three files in the working tree: `DIRECTION.md` (rewritten CURRENT DIRECTIVE — **ratifies C10**,
adds **HARD RAIL 6 EXTENDED**), `EXECUTABILITY.md`, and new `judge/pass-27.md`. I did NOT author these;
I committed them verbatim (persisting the altitude lap's output is hygiene, not editing the directive).
**NEW HARD RAIL** you must obey: `fine_scale_mixing` (C10) and `stabilization` (C9) statements are now
in the differ watch-list (`tools/tao_stmt_diff.py`, 29 names). **Do NOT edit those two statements** —
not weaken, not strengthen, not "generalize". Decompose BELOW them into named sub-`sorry`s freely. If a
statement looks wrong vs the paper, write `JUDGE-FLAG:` in `PENDING_WORK.md` and move on.

## 🎯 This lap (overnight run #2, this session): C10 §6 machinery — 6 axiom-clean crux bricks
All new lemmas `#print axioms`-clean `[propext, Classical.choice, Quot.sound]`, every build real-`lake
build` green. **Every genuinely-novel analytic ingredient of C10 is now proved**; what remains is the
§6 event-assembly bookkeeping (no new kernel). Commits `595b408..c195d29`. Bricks (all in
`Sec6/MixingFromDecay.lean`):
1. `cond_char_factor` (`595b408`) — head×tail conditional character factorization
   (`char_offset_split` + `cexpect_iid_append`; freeze `2⁻ᵖʳᵉ⁽ᵗᵃⁱˡ⁾→2⁻ˡ` on the event).
2. `dft_cond_density` + `condDens` + `dft_condDens_eq_cond_char` (`aa0d08f`) — the `𝓕(densC g)↔cexpect`
   bridge (general engine: `dft_apply`→`ofReal_tsum`→`tsum_finsetSum` swap→`sum_ite_eq` collapse).
3. `stdAddChar_eq_eC` (`d442d30`) — `stdAddChar(j)=eC(j.val/3ⁿ)` (mathlib char ↔ §7 phase).
4. `stdAddChar_pow3_descent` (`29f733e`) — character level-descent `stdAddChar_{3^(j+p)}(3ʲ·w)=
   stdAddChar_{3^p}(castHom w)`. **Already general** in `(j,p)`: reuse with any exponent split.
5. `castHom_two_inv`, `tail_char_descent` (`489a4e2`) + `eC_val_congr`, `stdAddChar_mul_eq_eC`,
   `tail_cexpect_eq_syracZ`, **`tail_factor_eq_charFn`** (`afab378`) — the tail char factor equals
   exactly a level-`p` Syracuse `charFn_decay` char sum. (Imported `Sec7.Decay` into `Sec6` —
   architecturally correct: C10 consumes Prop 1.17.)
6. `tail_factor_norm_le` (decay via charFn_decay), `head_factor_norm_le` (≤1) (`c195d29`).

## 🔑 DECISIVE SOURCE-READ (fruit-14, paper pp.29–31) — CORRECTS the block orientation
I read Tao §6 directly and pinned the exact conditioning. **The block roles in items 5–6 above are
mislabeled for the live path** (they're correct math, axiom-clean, banked — but not on the critical
frequency range). The TRUE structure:
- Split on `Cₖ,ₗ`: `Xₙ = Fₖ₊₁(aₖ₊₁,…,a₁) + 3^{k+1}·2^{-l}·Fₙ₋ₖ₋₁(aₙ,…,aₖ₊₂)`.
- **DECAY block = `Fₙ₋ₖ₋₁`** (the `3^{k+1}`-scaled term, UNRESTRICTED): high `ξ=3ʲ2ˡξ'` → `2^{-l}`
  cancels `ξ`'s `2ˡ` → `charFn_decay` at `ξ'`, level `n-k-j-1`.
- **≤1/Rényi block = `Fₖ₊₁`** (carries the indicator `1_{Ek∧Bₖ∧Cₖ,ₗ}`).
- In my `syracZ` (`a∘rev`) convention: my **HEAD** block (`3^p·Fnat_j`, first `j`) = Tao's DECAY block
  (`p=k+1, j=n-k-1`); my **TAIL** = Tao's ≤1 block. So `cond_char_factor` head=decay, tail=indicator.

## → NEXT (build the HEAD-block decay reindex — the live capstone; full detail in PENDING_WORK fruit-14)
1. **`head_factor_eq_charFn`**: for `ξ=3^{j'}2ˡξ'` (`j'<n-m`),
   `E_vh[stdAddChar(-((3^p·Fnat_j·2⁻ᴸ·2⁻ˡ)·ξ))] = (syracZ (j-j')).cexpect(Y↦eC(-(ξ'.val·Y.val)/3^{j-j'}))`.
   Reuse `stdAddChar_pow3_descent` (instantiate `j:=p+j'`, `p:=j-j'`), `castHom_two_inv`,
   `stdAddChar_mul_eq_eC`, and a HEAD-block `cexpect_map`/`syracZ_eq_rev_fnat` reindex (analog of
   `tail_cexpect_eq_syracZ`). **New wrinkle**: the `2⁻ˡ·2ˡ=1` cancellation — fold it into the `harg`
   `ring`-step.
2. Product bound per high `ξ`: `‖𝓕(densC g)ξ‖ ≤ Cₐ'·(n-k-j'-1)^{-A'}·1`.
3. Rényi ℓ²-mass + Plancherel (6.11) + `osc_le_sqrt_highfreq` on `condDens` closes (6.10).
4. Event assembly (6.1)–(6.8): telescope to `0.9n≤m≤n`; `E`/`Ek`/`Bₖ`/`Cₖ,ₗ` triangle+union.
   Decompose into named `sorry`s in `Sec6/MixingFromDecay.lean` as built.

## 📋 Remaining spine sorries (whole repo) — census 4
- `Sec6/MixingFromDecay.lean` `fine_scale_mixing` (Prop 1.14, C10) — **current crux**; all novel
  machinery proved, needs the §6 event assembly. **DO NOT edit its statement (HARD RAIL 6).**
- `Sec5/FirstPassage.lean:81` `stabilization` (Prop 1.11, C9) — downstream of C10. **DO NOT edit stmt.**
- `Statement.lean` ×2 — frozen headlines (discharge when C10→C9→C6 lands).

## 📁 Key files
- `TaoCollatz/Sec6/MixingFromDecay.lean` — ALL C10 machinery (bridge + factorization + reindex bricks
  + the `fine_scale_mixing` sorry). ~700 lines now.
- `PENDING_WORK.md` fruit-14 (this lap's source-read + the corrected HEAD-reindex plan) through fruit-8.

---
**→ Next session: build `head_factor_eq_charFn` (item 1 above). Re-read `DIRECTION.md` (JUDGE PASS 27)
+ HARD RAIL 6 first.**
