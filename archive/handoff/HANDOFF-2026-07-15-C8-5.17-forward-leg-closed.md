# Handoff: C8 (5.17) FORWARD LEG CLOSED — only the reverse leg remains

**Date**: 2026-07-15. **Branch** `main`, **HEAD `9c1e77e`**. Build 🟢 green (full `lake build`, 3323
jobs; pre-commit verified every commit). Tree clean, nothing uncommitted.
**Read `DIRECTION.md` first** — CURRENT DIRECTIVE (JUDGE PASS 30) outranks this. Campaign order:
C10 ✅ → C8 (pin ✅) → C7 ✅ → **C8 close (1 leaf, ½ done)** → C9 → C6 → headline.

## What this session did — closed the (5.17) forward inclusion

C8's last leaf `first_passage_stepback_reduce` (5.17) = `|firstPassMid − steppedMid| ≤ C·log^{-c}x` was
one monolithic `sorry`. It is now decomposed into two directional legs (assembly proved axiom-clean):
- **`firstPassMid_le_steppedMid` — PROVED axiom-clean** (the exact forward inclusion `S_n ⊆ T_n`).
- `steppedMid_le_firstPassMid_add` — the reverse defect, STILL SORRIED (the only C8 hole left).

**Key structural finding:** the forward inclusion is DETERMINISTIC — the `E'` size window is FORCED by
the passage of `M = Syr^{n−m₀}N` itself (not a whp restriction). All `O(log^{-c})` error is in the
reverse leg. See `PENDING_WORK.md` top section for the full narrative.

### New axiom-clean lemmas this session (all `#print axioms` = `[propext, Classical.choice, Quot.sound]`)
In `Sec5/ApproxFormula.lean`:
- `good_nested` (𝒜⁽ⁿ²⁾⊂𝒜⁽ⁿ¹⁾ tuple nesting), `mem_Iy_le_nZero`, `mem_Iy_bounds`
- `expect_mono_on_support` (support-restricted `expect∘indicator` monotonicity — weaker hyp than
  `expect_mono_le`, needed so `S_n⊆T_n` need only hold for ODD N)
- `mZero_le_of_mem_Iy` (`1 ≤ m₀ ≤ n` for `n∈I_y`, pure interval arithmetic)
- `pow_stepback_eq`, `three_pow_nZero_le` (`3^{n₀}≤x^{1/5}`), `slack_key`, `slack_upper`, `slack_lower`
- `stepback_passage_scale` (passage orbit-straddle core), `stepback_size_window` (E′ window assembly)
- `eprime_forces_passTime` (the **disjointness key** for the reverse leg)
Also: corrected the DIRECTION.md-flagged stale doc-hazard comment (reindex is EXACT, not approximate).

## State: 4 sorries + 0 orange nodes
- `Statement.lean:24,31` — the two headline stubs (Thm 1.3 / Thm 3.1), frozen; discharge when C8/C9/C6 land.
- `Sec5/FirstPassage.lean:1399` — C9 `stabilization` (Prop 1.11), consumes C10 ✅ + C8.
- `Sec5/ApproxFormula.lean` `steppedMid_le_firstPassMid_add` (~:2593) — **C8's LAST hole** = (5.17) reverse leg.

## Next steps — close the (5.17) reverse leg, then C9

**`steppedMid_le_firstPassMid_add`** (roadmap is on the theorem's docstring + `PENDING_WORK.md`):
1. Formalize the expect-subtraction: `steppedMid − firstPassMid = ∑_{n∈I_y} P.expect(𝟙_{T_n∖S_n})`
   (nonneg, `S_n⊆T_n` from the forward leg). Mirror `first_passage_window_reduce`'s `hsum`/`tsum`
   idiom (`ApproxFormula.lean` ~line 1806). `S_n={T_x N=n ∧ passLoc∈E ∧ good⁽ⁿ⁰⁾}`,
   `T_n={good⁽ⁿ⁻ᵐ⁰⁾ ∧ E′(Syr^{n−m₀}N)}`.
2. Split `T_n∖S_n` by `good⁽ⁿ⁰⁾`. **The `T_n` are disjoint** (`eprime_forces_passTime`), so
   `∑_n 𝟙_{T_n} ≤ 1` and each defect sum collapses to one `P(·)` — NO `O(log x)` blow-up.
   - (a) `¬good⁽ⁿ⁰⁾` half → `≤ P(¬good⁽ⁿ⁰⁾) ≤ C·log^{-c}` via disjointness + PROVED `approx_good_tuple_whp`.
     **Likely fully provable now** (all inputs exist) — do this first.
   - (b) `good⁽ⁿ⁰⁾` half → residual forces `n−m₀ > T_x N` (early-return / orbit dipped early). Bound via
     PROVED `approx_passtime_window` (5.16). ⚠ The genuinely hard whp piece; decompose into a named
     early-return sorry if it resists.
3. Closing it makes `first_passage_approx` (C8) axiom-clean → then **C9 `stabilization`** (directive step 1
   was the C9 assembly-spine PROBE; it's still un-started — consider it once C8 lands), then C6 → headlines.

## Rails / notes
- **Do NOT edit ratified pins** (`Eprime`, `Iy`, `firstPassMid`, `steppedMid`, `approxMainTerm`,
  C8/C7 statements, `fine_scale_mixing`, `stabilization`, the two headlines). ⚠ If the reverse leg seems
  to need touching one → `JUDGE-FLAG:`, do not improvise. Constants FROZEN: `epsBW=1/10^1000`, `caConst=30`.
- `git-safe` at `/Users/gotrevor/personal/bin/git-safe` (`export PATH="$HOME/personal/bin:$PATH"`).
  Axiom-check recipe: write `TaoCollatz/ZZ_ax.lean` with `#print axioms`, `lake env lean` it, `rm -f`.
- Gotchas hit this session (corpus-worthy): `set`-let vars still poison `nlinarith`/`ring` (kept
  `Real.log x` literal instead of `set`); use concrete rationals (`alpha−1=1/1000`) to dodge
  decimal-rpow poison; `div_le_div_iff` renamed (use nlinarith route); `PMF.mem_support_iff p a` takes
  BOTH args; `pow_le_pow_right₀` for monotone-in-exponent with base ≥1; `Nat.lt_floor_add_one` for `x<⌊x⌋+1`.
- **Work report: 4 sorries + 0 orange.** C8's forward leg (5.17) is closed; 1 reverse-leg hole remains.
