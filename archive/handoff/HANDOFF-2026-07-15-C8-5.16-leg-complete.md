# Handoff: C8 (5.16) window leg COMPLETE — `passtime_edge_of_good` proved axiom-clean

**Date**: 2026-07-15. **Branch** `main`, **HEAD `a185705`**. Build 🟢 green (full `lake build`, 3323
jobs; pre-commit verified each commit). Tree clean, nothing uncommitted.
**Read `DIRECTION.md` first** — CURRENT DIRECTIVE (JUDGE PASS 30) outranks this.
Campaign: C10 ✅ → C8 (pin ✅) → C7 (prove ✅) → **C8 close (1 leaf left)** → C9 → C6.

## What this lap did

1. **Committed the judge's pass-30 `Basic/Anchors.lean`** (non-vacuity comparator witnesses:
   `colMin_twentyseven`, `logProb_odd_window_two`, etc.) — it was uncommitted in the working tree.
2. **PROVED `passtime_edge_of_good`** (`Sec5/ApproxFormula.lean`) — Tao's **(5.15) pointwise
   inclusion**, the last open piece of the (5.16) window leg. Axiom-clean
   (`[propext, Classical.choice, Quot.sound]`). With it, **`passtime_window_inner` (5.16) AND
   `approx_passtime_window` (the C7 consumer) are now BOTH axiom-clean** (`#print axioms` verified).

### Proof shape (REUSE this for the (5.17) leg — same interval algebra)
Contrapositive: assume `N` in the strict window interior `y·e^s < N < y^α·e^{-s}` (`s = log^{0.8}x`),
derive `T_x(N) ∈ I_y` (`T ≤ nZero` ∧ `IyLo ≤ T ≤ IyHi`), contradicting `∉ I_y`. Applies the proved
orbit bracket `syr_iterate_good_bracket'` (`(3/4)^n·N·2^{±log^{0.6}x}`) at `n = nZero, T, T-1` +
`passTime = sInf` minimality (`Nat.sInf_le`; step-back `syr^[T-1]N > ⌊x⌋₊`). All numeric margins
reduce, via the substitution **`u := log^{0.2}x`** (`log^{0.6}=u³`, `log^{0.8}=u⁴`, `log x=u⁵`), to
polynomials `nlinarith` discharges, with `log(4/3) ∈ [1/4,1/3]` (`Real.log_le_sub_one_of_pos` +
`Real.log_inv`) and `log 2 ∈ (0.693,0.694)` (`Real.log_two_gt/lt_d9`).

### Gotchas that cost time (all documented; memory/corpus-worthy)
- **`set`-let vars poison `nlinarith`/`ring`/`positivity` via whnf.** Fix: `clear_value ℓ u s L ν`
  after establishing their defining facts, THEN use `linarith only [...]` / `nlinarith only [...]`
  to exclude the rpow/decimal context atoms (`Real.log x^0.6`, `alpha = 1.001`) that poison
  preprocessing. This is the `lean-linarith-decimal-rpow-poison` memory, at scale.
- **`unfold alpha; norm_num` inside a large context whnf-times-out.** Pre-establish alpha facts
  (`halpha_gt1`, `halpha_le2`, `halpha3`, …) EARLY, reference the `have`s.
- Needed `set_option maxHeartbeats 1600000 in` (HEARTBEAT-justified in source) — the single ~250-line
  proof term exceeds the default whnf budget even though every tactic passes.
- `mul_le_mul_right` is NOT the iff form here; use `mul_le_mul_of_nonneg_right`.
- `Nat.not_mem_of_lt_sInf` does not exist; derive minimality from `Nat.sInf_le` contrapositive.

## State: 4 sorries + 0 orange nodes
- `Statement.lean:22,28` — the two headline stubs (Thm 1.3 / Thm 3.1), frozen; discharge when C8/C9/C6 land.
- `Sec5/FirstPassage.lean:1391` — C9 `stabilization` (Prop 1.11), consumes C10 ✅ + C8.
- `Sec5/ApproxFormula.lean` (`first_passage_stepback_reduce`, ~:2137) — **C8's LAST leaf** = (5.17).

`fine_scale_mixing` (C10), `first_passage_nonescape` (C7), `approxMainTerm_eq_steppedMid`,
`passtime_edge_mass`, `passtime_edge_of_good`, `passtime_window_inner`, `approx_passtime_window` all
`#print axioms` = trust base only. `first_passage_approx` (C8) = trust base + `sorryAx` (only via
`first_passage_stepback_reduce` now).

## Next steps (CURRENT DIRECTIVE scope — close C8, then C9)
1. **`first_passage_stepback_reduce` (5.17)** — C8's last hole. `|firstPassMid − steppedMid| ≤
   C·log^{-c}x`. Forward inclusion EXACT (`firstPass_event_stepback_subset` ✅); remaining = reverse
   inclusion + the `E'` size window `exp(±log^{0.7}x)·(4/3)^{m₀}·x` (REUSE the interval-algebra idiom
   above: `syr_iterate_good_bracket'` + `two_rpow_slack_le_exp` PROVED + `IyLo/IyHi` rearrangement) +
   nested `𝒜⁽ⁿ⁰⁾ ⊂ 𝒜⁽ⁿ⁻ᵐ⁰⁾`. Closing it makes `first_passage_approx` (C8) axiom-clean.
2. Then **C9 `stabilization`** (`FirstPassage.lean:1391`), then C6 → the two `Statement.lean` headlines.

## Rails / notes
- **Do NOT edit ratified pins** (`Eprime`, `Iy`, `firstPassMid`, `steppedMid`, C8 statements,
  `approxMainTerm` RATIFY-C8-v2). Watched: `fine_scale_mixing`, `first_passage_nonescape`,
  `stabilization`, all ratified pins. ⚠ If (5.17) needs touching one → `JUDGE-FLAG:`, do not improvise.
- `git-safe` at `/Users/gotrevor/personal/bin/git-safe` (`export PATH="$HOME/personal/bin:$PATH"`).
  Axiom-check recipe: write `TaoCollatz/ZZ_ax.lean` with `#print axioms`, `lake env lean` it, `rm -f`.
- Numeric-trap hygiene still owed (non-blocking): `check_blueprint.py` entry for RATIFY-C8-v2 (probe
  `tools/sandbox/tao_c8_truncation_probe.py`); the `epsBW` trap value (check 11 = `1/10^4`, deployed
  `1/10^1000`); stale docstrings on proved nodes (DIRECTION.md follow-ups list).
- **Work report: 4 sorries + 0 orange nodes.** The (5.16) leg is fully closed; C8 has 1 leaf left.
