# HANDOFF — 2026-07-13-e (JUDGE-authored; entry point for the next treadmill run)

**Branch** `main`, HEAD = judge pass 24 (`1a4155d`), `lake build` green, working
tree clean. This handoff is written by the **judge**, not by a lap. It supersedes
the "NEXT" section of `HANDOFF-2026-07-13-d.md` on one point — read the ⛔→✅ below
before you plan your lap.

## ⛔→✅ THE ESCALATION GATE IS LIFTED

`HANDOFF-2026-07-13-d.md` says `fpDist_any_triangle_le` is *"ESCALATION-GATED, do
not touch"*, and `ROUTE-ESCALATION-2026-07-13.md` describes a route crisis.
**Both are superseded.** Judge pass 24 (`judge/pass-24.md`) re-read the paper's
p.48 localization argument and **downgraded the escalation: it is not
altitude-class.** The route is sound, the committed geometry is faithful to the
paper, and `fpDist_any_triangle_le` is now **the priority target**.

Do not re-litigate the escalation. Do not re-derive the diagnosis. It is done.

## What was actually wrong (so you don't repeat it)

The foreign-triangle tail reduces (already proved:
`fpDist_any_triangle_le_of_localization_box`) to one inequality —
`√(X²+Y²) < sep`, where `sep = (1/10)·ln(1/epsBW) = 9·ln10 ≈ 20.72` and
`X = ⌈(5Y + B)/16⌉`. The box came out at `≈ 2.6·10⁶` **only because two constants
are throwaway**:

1. **`B` (transverse threshold, `fpDist_linear_tail`, `Sec7/FpLocation.lean:366`)**
   ships `4·10⁷`. It bounds the `16j − 5l` MGF with a crude *quadratic* penalty
   (`1000·(λ₁²+λ₂²)`) that nearly cancels the `−16`/step drift (net exponent
   `−39/400000`), forcing the tilt down to `θ = 1/20000`.
2. **`Y` (overshoot radius, `fpDist_height_tail`, `Sec7/ManyTriangles.lean:2522`)**
   is **existential** — it sums X6's `fpDist_location_bound`, whose `(cL, CL)` are
   `∃`-bound. So the box is not even a *number*. **This is the real blocker.**

## 🎯 YOUR TARGETS (in this order)

### Lap A — 🗂️ the split (pure moves, still queued, now overdue)
`ManyTriangles.lean` is ~5,200 lines and every edit re-elaborates all of it.
Execute the split exactly as specified in **BLUEPRINT §2** (four dependency-ordered
files, **zero statement/proof edits**, names verbatim, thin re-export shim). A split
lap that also edits proofs forfeits the judge's cheap verification. Build green,
commit, done.

### Lap B — `B ≈ 42`: sharpen `fpDist_linear_tail` with the EXACT MGF
The step law is exactly `hold = geomQuarter.bind (k ↦ pascalNe3.iid (k−1) …)`:
- `k ~ geomQuarter`: `P(k) = ¼(¾)^{k−1}`, `k ≥ 1`, mean 4.
- `Δl = 3 + Σ_{i<k−1} vᵢ`, `v ~ pascalNe3` (Pascal(2,½) minus the `b=3` atom), mean 13/3.
- Mean vector `(4,16)` (`hold_mean_fst`/`hold_mean_snd`), so `Z = 16j − 5l` has drift **−16/step**.

Exact MGF (no quadratic bound needed):
`E[e^{θZ}] = e^{−15θ}·¼e^{16θ} / (1 − ¾ e^{16θ} φ(θ))`, `φ(θ) = E[e^{−5θv}] ≤ 1`,
convergent while `¾e^{16θ}φ(θ) < 1` (ceiling `θ_c ≈ 0.213`, vs the shipped `5·10⁻⁵`).
At `θ* ≈ 0.110` the threshold for tail `≤ 1/16` is **`B ≈ 42`**.

**Keep the lemma's existing shape** (`e^{−θB} · M/(1−M)`) — only the MGF input
changes. Any `B ≤ 250` is good enough; don't over-optimize.

### Lap C — `Y = 139`: re-prove `fpDist_height_tail` OFF X6, explicitly
**Do NOT make X6's constants explicit** (that re-opens a completed, verified node).
Take the elementary route; all three ingredients are already in the repo:
1. `fpDist_le_renewal_conv` — the endpoint is a pre-passage point *below* the budget
   line plus **one** `hold` step.
2. `hold`'s height increment is `Δl = 3 + Σ v` with `v ≥ 2`, so **`Δl ≥ 3 > 0`**:
   heights **strictly increase**, hence the walk visits each height level **at most
   once**, hence the renewal mass at any level is **`≤ 1`**. No renewal theorem, no
   local limit law. (This is the trick. It is the whole reason `Y` can be explicit.)
3. `Δl` has an exact MGF (ceiling `μ_c ≈ 0.0640`).

Chain: `P(height ≥ s+Y) ≤ Σ_{u≥0} P(Δl ≥ Y+u) ≤ E[e^{μΔl}]·e^{−μY}/(1−e^{−μ})`.
At `μ* ≈ 0.0575` this gives **`Y = 139`** for tail `≤ 1/16`.

Numerics for both laps: run `tools/tao_linear_tail.py` and `tools/tao_height_tail.py`
(they optimize the tilt and print the box). They are *targets*, not proofs — a Lean
proof may ship somewhat lossier constants, which is fine and expected.

### Lap D — close the tail
With `B` and `Y` numerals, `X = ⌈(5Y+B)/16⌉ = 47` and the box is `√(47²+139²) ≈ 147`.
Feed `exists_fpDist_localization_box` + the box inequality into the already-proved
`fpDist_any_triangle_le_of_localization_box` ⟹ `fpDist_any_triangle_le` ⟹
`fpDist_white_exit_deep` ⟹ **X9's kernel closes**.

## ⚠️ HARD STOP: the box does NOT fit at the current ε — and that is NOT yours to fix

`147 > sep ≈ 20.72`. Closing Lap D needs a **numeral re-freeze of `epsBW`**
(`10⁻⁹⁰ → 10⁻¹⁰⁰⁰`, giving `sep ≈ 230`). **`epsBW` is the JUDGE's to set** — it is a
D4 constant whose change fires a repo-wide ε-sweep re-ratification, so it moves once,
deliberately, on the judge's word and after seeing the constants you actually proved.

- **Do NOT change `epsBW`** on your own initiative.
- **Do NOT re-open `epsBW` as a parameter** (that remedy is explicitly off the table).
- **Do NOT introduce a `Real.exp`-valued ε** — the frozen rational power of ten is doctrine.

Land Laps B and C — **they are entirely ε-free and need no ruling** — report the
constants you actually proved, and STOP at the box inequality (leave
`fpDist_any_triangle_le` sorried, with the box numerals recorded in your handoff).
The judge sets the numeral from your real numbers, and Lap D lands right after.
If you finish B and C early, spend the remaining laps on the BlackEdge
crux sorries (`fpDist_edgeWeight_le`, `fpDist_white_exit`, `Q_black_edge_case2`) or
Case3's `Q_black_edge_case3` — do not idle on the gated tail.

## Standing constraints (unchanged)

- **Pinned statements are frozen.** Any edit to a ratified statement revokes its
  ratification. `TaoCollatz/Statement.lean` is the trusted base — its two sorries are
  the headline stubs, not holes to fix.
- **`native_decide` is PERMITTED as scaffolding** (operator, 2026-07-13) — but it plants
  `Lean.ofReduceBool`, so: tag every use with a `-- NATIVE_DECIDE:` justification
  comment, and understand that **a declaration whose trail contains it does not count
  as judge-verified**. It must all be discharged before publication. Prefer
  `decide +kernel` (adds no axiom). For the work above it is largely moot — the MGF and
  Chernoff facts are real-number inequalities over `exp`/`log`, which `native_decide`
  cannot decide at all.
- Local `maxHeartbeats` bumps need a `-- HEARTBEAT:` justification comment (three
  existing ones are missing it — fix them if you touch those decls).
- STATUS.md's self-log reuses lap numbers 58/59 — renumber when you next touch it.
