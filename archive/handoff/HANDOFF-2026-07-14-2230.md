# Handoff: C7 integral test — reduced to ONE combinatorial bridge + 2 counting lemmas

**Real date**: 2026-07-14 (~22:xx). **Branch** `main`, **HEAD `5cc41b3`**. Build 🟢 green (full `lake
build`, 3322 jobs), tree clean. **Read `DIRECTION.md` first** — it outranks this doc; its CURRENT
DIRECTIVE (Judge Pass 29 + review updates) puts the frontier at C7's integral test, attack hardest-first.
The `21xx/23xx/07-15` handoff filenames are clock-skew artifacts — match batons by HEAD/commit.

## This lap: the entire mechanical + analytic scaffolding of the C7 integral test landed (7 green commits)

DIRECTION objective 3 (C7 = `first_passage_nonescape`, (1.19)) hinges on the crux `intTest_error`
(the integral test `dTV(N_y mod 2^{3n₀}, Unif) ≪ 2^{3n₀}/y`). It is now **PROVED and its whole
interface machine-verified**, reduced to exactly **one combinatorial bridge + two counting lemmas**.
All proved lemmas are `[propext, Classical.choice, Quot.sound]` (checked by `#print axioms`).

**Landed this lap, all in `Sec5/FirstPassage.lean`, all axiom-clean:**
- `l1_normalize_telescope` — pure L¹ core: per-class dev `|S_r−t|≤ε` ⟹ `∑|s_r/D−1/|O|| ≤ 2ε|O|/D`.
- `map_res_apply_toReal` — pushforward mass `(P.map res)(r).toReal = classMass/windowMass = S_r/D`
  (+ defs `classMass`, `windowMass`, `logUnifOdd_apply_of_nonempty`; coercion double-map + ENNReal→toReal).
- `windowMass_eq_sum_classMass` — partition identity `D = ∑_{r odd} S_r` (`sum_fiberwise_of_maps_to`).
- `intTest_dTV_le` — dTV even/odd split: even residues vanish, odd collapse via telescope ⟹
  `dTV ≤ 2ε·2^{n'-1}/D`. (+ `cast_val_odd`.)
- `nZero_pos_of_large` (`0 < 3n₀` for `x ≥ 2^11`) + **`intTest_error` PROVED** from the three holes
  (final `2·(c/y)·2^{n'-1}/D ≤ (c/D₀)·2^{3n₀}/y` npow↔rpow arithmetic, `D ≥ D₀`).
- `harmonic_ap_integral_bound` — **the reusable integral-test core**:
  `|∑_{i<n} 1/(a+Mi) − M⁻¹·log((a+Mn)/a)| ≤ 1/a`. Two-sided test (`AntitoneOn.sum_le_integral`/
  `integral_le_sum` + `intervalIntegral.integral_comp_add_mul` + `integral_inv`, telescoped by
  `Finset.sum_range_sub'`). Needed new imports `Mathlib.Analysis.SumIntegralComparisons`,
  `Mathlib.Analysis.SpecialFunctions.Integrals.Basic`.
- **`intTest_class_dev` PROVED** from `classMass_ap_form` (bridge) + `harmonic_ap_integral_bound` +
  log reconciliation (`log(1+u)≤u` on each endpoint ratio ⟹ `|S_r − t| ≤ 2/y`, common `t=M⁻¹log(y^α/y)`).

## → NEXT (hardest-first), all in `Sec5/FirstPassage.lean`

1. **`classMass_ap_form` (:~420) — THE remaining hole; now pure finite combinatorics, not analysis.**
   `∃ a count, 1≤count ∧ classMass y (y^α) (3n₀) r = ∑_{i<count} 1/(a+M·i) ∧ y≤a<y+M ∧
   y^α<a+M·count≤y^α+M` for odd `r` (`M=2^{3n₀}`). Prove: the class `{N∈[y,y^α]:N≡r (mod M)}`
   = `(range count).image (fun i => a + M·i)` with `a` the least member `≥y`; then `Finset.sum_image`
   (injectivity of `i↦a+Mi`) gives the sum form. Endpoint constraints from: `a` least ≥ y in class
   ⟹ `a∈[y,y+M)`; `count`≥1 since `y^α−y > M` (as `M≍x^0.3 ≪ y^α−y≍x`). `Nat.Ioc_filter_modEq_card`
   (`Mathlib/Data/Int/CardIntervalMod.lean`, corpus `mathlib-has-ap-count-and-multimod-crt.md`) counts it.
   ⚠️ Note `classMass` filters `logWindow` (which already carries `N%2=1`); for odd `r`, `N≡r (mod M)`
   forces `N` odd, so the oddness is redundant — but you must *prove* that reduction to match the image.
2. **`intTest_D_lower` (:~511)** — `∃ D₀>0, D₀ ≤ windowMass y (y^α)`. Same AP-count machinery: `#(odds
   in [y,y^α]) ≥ (y^α−y)/2 − 1`, each term `≥1/y^α`, so `D ≥ (y^α−y−2)/(2y^α) → 1/2`; take `D₀=1/4`.
3. **`logWindow_nonempty_of_large` (:~518)** — an odd `N∈[y,y^α]`; explicit witness `≈2⌈y/2⌉+1 ≤ y+2 ≤ y^α`.

Then `intTest_error` → `integral_test_logUnif` (already assembled) → feed `valuation_dist`/`valuation_tail`.
Then `valSum_lower_tail` (:~600, downstream/mechanical) → C7 done → close C8 (obj 4) → C9.

## Rails / notes
- Watched statements (`fine_scale_mixing`/`stabilization`) + all ratified pins UNTOUCHED. Never set
  `\leanok` yourself. `caConst`/`epsBW`/window frozen.
- `git-safe` at `/Users/gotrevor/personal/bin/git-safe` (not on default PATH; `export PATH="$HOME/personal/bin:$PATH"`).
- Axiom-check recipe: write `TaoCollatz/ZZ_ax_check.lean` importing the module with `#print axioms
  TaoCollatz.<name>`, `lake env lean` it, then delete (don't leave it — it'd break the build tree).
- Sorry census: **5 working in FirstPassage** (`classMass_ap_form`, `intTest_D_lower`,
  `logWindow_nonempty_of_large`, `valSum_lower_tail`, + the C9 `stabilization` stub at :~839)
  + **3 in C8** (`ApproxFormula.lean`) + **2 headline stubs**. **0 orange nodes.** The C7 crux
  `intTest_error` is PROVED; its `sorryAx` traces solely to the 3 named holes above.
- `intTest_class_dev`/`intTest_error` `#print axioms` include `sorryAx` ONLY via those holes — expected.
- PENDING_WORK.md top entry (HEAD `b4870c5`) documents the scaffolding; update it as holes close.
