# HANDOFF — 2026-07-14-h (X11d crux `few_white_mass_le` (7.56): pointwise layer + reach term DONE)

**Branch** `main`, HEAD = `9d666e7`, `lake build` green (3282 jobs), working tree clean.
**Re-read `DIRECTION.md` CURRENT DIRECTIVE FIRST** (judge owns it; outranks this handoff).
It narrows to: close X11 `Q_black_edge_case3` via X11a→X11c→X11d. **We are deep in X11d,
attacking the crux `few_white_mass_le` (7.56).**

## The two remaining §7 sorries (both `Case3.lean`)
1. **`few_white_mass_le`** (`:1722`) — THE crux (7.56). Its whole proof layer is now built
   below it; only the assembly + E∗-term remain (see NEXT).
2. **`col_tail_mass_le`** (`:1869`) — standard Gaussian tail (7.54 bad column), gives
   `≤ m^{−A}/2`. Reused (not attacked separately) by few_white's bad-column term.

## What landed this lap (6 green commits `d3b4885`→`9d666e7`, ALL axiom-clean)
The entire **pointwise layer** of the (7.56) three-way split is proved, plus the reach term:
1. `d3b4885` **`few_white_pointwise_dichotomy`** — index-shift kernel: `whiteSet∩strip IS
   whiteStrip`; `cumWhite ≤ myNw+1` (two range-succ splits), so feed the geometry/Markov
   lemmas at `K'=K+1`.
2. `8373fab` **`pathSum_fst_le`** (first-coord monotone) + the ROUTE-DECISIVE 3-way finding
   (below).
3. `172a4f1` **`pathSum_depth_le`** — good-column depth sourcing (endpoint depth ⟹ every
   intermediate position deep).
4. `b61bcd2` **explicit horizon**: `deterministic_encounter_claim_at` (explicit
   `encWindowIter A K R ≤ T`; the `∃P₀` form delegates to it) + `few_white_pointwise_dichotomy`
   rewritten to take explicit uniform `P`. Needed because few_white must fix ONE `P` before
   `∀ n ξ F` (P₀ = `encWindowIter A' (K+1) R`, depends only on A',K,R).
5. `a9d17f0` **`few_white_pointwise_split`** — the assembly's pointwise heart:
   `ofReal(1_{myNw≤K}) ≤ ofReal(1_{reach∧cw≤K+1}) + Σ_{p<P+1} indicator(bigTri ⌊4^{A'}(1+p)³⌋)
   + ofReal(1_{0.9m≤e.1+(pathSum v P).1})`. Cases: myNw>K (0); bad col (3rd=1); good col
   (depth via `pathSum_depth_le`, `adv+g<m`) ⟹ dichotomy ⟹ reach (1st=1) or E∗
   (`bigTriangle_of_encounter` ⟹ one middle summand=1).
6. `9d666e7` **`few_white_reach_mass_le`** — reach term: `Σe fpDist Σv hold·ofReal(1_{reach∧
   cw≤K+1}) ≤ 10^{−A−3}`. Wraps `reaches_fewWhite_mass_le_ten`@(A+2),K'=K+1 per-e via
   `PMF.toReal_tsum_mul_ofReal` + `Σfpdist=1`. EXPOSES shared `ε₀,g`; hyp `(K+1)+(A+5)log10+2 ≤ ε₀R`.

## 🔑 ROUTE-DECISIVE FINDINGS (this lap)
- **(7.56) is a THREE-way split, not two** (from paper pp.48–50): `few_white_mass_le` sums over
  ALL columns, but the dichotomy's depth hyp only holds on the good column `{adv<0.9m}`. So
  `P(myNw≤K) ≤ [reach] + [E∗] + [bad-column]`, each ≤ `10^{−(A+3)}`, sum `3·10^{−(A+3)} ≤ 10^{−(A+2)}`.
  Bad-column reuses `col_tail`.
- **SMALL-A RESOLVED**: use `A' := 2A + A₀` (A₀≥1 constant from estar's A₀ + numeric), NOT `κ·A`.
  Then `A' ≥ 1` for ALL A>0 (dichotomy OK) AND `4^{−A'}·10^A = 4^{−A₀}·(10/16)^A` bounded (base
  16>10), so the E∗ bound holds UNIFORMLY over all A>0 — **no judge flag needed.**

## NEXT (in `PENDING_WORK.md` top, steps 3–5) — ordered
3. **E∗ term** `few_white_estar_mass_le`: `Σe fpDist Σv hold·(Σ_{p<P+1} indicator bigTri) ≤
   10^{−A−3}`. Swap finite `Σ_p` ↔ tsums, apply `estar_union_le` @exponent `A'=2A+A₀`,
   `j=n/2−m−1`, `T=P`. Hard sub-part = numeric `C'(2A+A₀)²4^{−(2A+A₀)}+C'exp(−c(2A+A₀)²) ≤
   10^{−A−3}` (poly·geom bounded ⟹ choose A₀; needs an `x²·rˣ` domination lemma, `r=10/16<1`).
   ⚠ deep-hyp reconcile: estar wants `(n/2−j)/log(n/2−j)²<s` with n/2−j=m+1; my hyp is
   `m/log m²<s` — bridge via Cthr (x/log x² increasing, gap small).
4. **bad-column term**: `col_tail_mass_le` (PROVED, ≤ m^{−A}/2) + numeric `m^{−A}/2 ≤ 10^{−A−3}`
   for m≥Cthr(A). Its integrand MATCHES the split's 3rd term exactly. ⚠ **col_tail is AFTER
   few_white in the file (`:1869` vs `:1722`) — MOVE it before few_white** (it doesn't depend on
   few_white), or few_white forward-refs. Simple reorder.
5. **Assembly** (`few_white_mass_le` proper): pick `A'=2A+A₀`, `K=⌈(A+3)log10/epsBW³⌉` (the
   statement's K), `R=⌈((K+1)+(A+5)log10+2)/ε₀⌉`, `P=encWindowIter A' (K+1) R`, `Cthr≥10g` +
   estar/col_tail Cthrs. Apply `few_white_pointwise_split` inside `Σe fpDist Σv hold·`(**per-v
   support casing**: v∉hold.support ⟹ hold.iid P v=0, so 0≤0; else use the split), tsum-linearity
   (model: `damping_expectation_le`'s `key` at `:~1766`, uses `mul_add`,`ENNReal.tsum_add`,
   `ENNReal.tsum_mul_right`,`tsum_coe`,`one_mul`) → reach+E∗+bad, sum `3·10^{−(A+3)} ≤ 10^{−(A+2)}`.
   Note the split's `hg:(g:ℝ)≤0.1m` needs Cthr≥10g; `hP:encWindowIter A' (K+1) R ≤ P` is `le_rfl`.

## Proved machinery inventory (all `Case3.lean`, axiom-clean)
Pointwise layer: `few_white_pointwise_dichotomy`, `few_white_pointwise_split`, `pathSum_fst_le`,
`pathSum_depth_le`, `deterministic_encounter_claim_at`, `bigTriangle_of_encounter`.
Mass terms: `few_white_reach_mass_le` (reach ✓), `col_tail_mass_le` (bad-col, ≤m^{−A}/2 ✓),
`estar_union_le` (E∗ union, ✓ — feeds term 3). X11c/X11a all proved earlier.

## Gotchas this lap
- Docstring must NOT sit between `open scoped Classical in` and `theorem`: order is
  `open scoped Classical in` THEN docstring THEN theorem.
- `if _ ∈ whiteStrip` in a STATEMENT needs `open scoped Classical in` (decidability).
- `zero_le _` fails when `zero_le` resolves to implicit-arg form (`0 ≤ ?m`) — use `zero_le'`.
- `Finset.single_le_sum (f := fun p : ℕ => …)` — annotate the binder `: ℕ` (else inferred ℝ).
- ℝ≥0∞ split-domination: `self_le_add_left a b : a ≤ b+a`, `self_le_add_right a b : a ≤ a+b`.
- ℝ≥0∞→ℝ bridge: `PMF.toReal_tsum_mul_ofReal p f (hf:∀x,0≤f x) : (∑' x, p x*ofReal(f x)).toReal
  = ∑' x,(p x).toReal*f x`; `PMF.tsum_mul_ofReal_le_one` (both in `PMF` namespace, `Prob/Basic`).
- `ENNReal.le_ofReal_iff_toReal_le (hne:S≠⊤) (hc:0≤c) : S ≤ ofReal c ↔ S.toReal ≤ c`.
- rpow exponent must match the source lemma SYNTACTICALLY before `exact`: reaches gives
  `10^(-((A+2)+1))`; rewrite target with `have hexp : 10^(-((A+2)+1)) = 10^(-A-3) := by congr 1; ring`.
