# Handoff: C10 — `globalGood ⊆ mainEvent` proved; next is the pure union-bound tail estimate

**Date**: 2026-07-14 19:13 UTC · **Branch**: `main` · **HEAD**: `d84eae7` (judge pass 29 corrected, built on my `a52d4ad`)

> ⚠️ Filename clock: this repo's handoff sequence runs on a drifted date (files are dated 2026-07-15
> though `date` says 2026-07-14). I used the real `date` output. Either way this sorts as newest;
> prior newest was `HANDOFF-2026-07-15-2300.md` (mine, same work). Read DIRECTION.md first — it outranks.

## 🎯 What we're doing
Campaign order (judge pass 29 **corrected**, HEAD `d84eae7`): **C10 → C8(pin) → C7(prove) → C8(close) → C9**.
C10 is still first and one hole from done. The C10 crux node is `error_l1_high_bound`
(`Sec6/MixingError.lean`), which is now a **proved wrapper**; the single remaining C10 sorry is
`prob_not_globalGood_le` (`Sec6/MixingError.lean:543`) — a **pure probability/tail estimate**, no event
algebra left. Finish that, then C10 is complete and axiom-clean, and the campaign moves to pinning C8.

## 🧠 Context to carry forward
**The event algebra is DONE.** DIRECTION objective 1 demanded proving `globalGood ⊆ mainEvent`
explicitly ("that inclusion IS the content; do not sorry past it"). I did — `globalGood_subset_mainEvent`
is machine-checked and axiom-clean (`[propext, Classical.choice, Quot.sound]`). The mental model:

- `mainEvent` = ∃ stopping cut `k` and valuation `l` with `mainPieceEvent` (= `pre vt(k+1)=l ∧
  condWindow ∧ stopEvent`) on the **reversed** tail block `vt`. All three constituents reduce to facts
  about **suffix sums** `sufSum a r := pre a n − pre a (n−r)` (the last `r` coords). Key derivation
  (in `mainPieceEvent_of`): `stopEvent` = `sufSum a k ≤ T < sufSum a (k+1)`; `condWindow` at scale `r`
  = `winThr C n r ≤ sufSum a r`; lRange lower from the crossing `T < l`, upper from a per-coordinate cap.
- `globalGood` (Tao (6.2), an **ENLARGEMENT** of `Eₖ` — never document as equal, pass-29 rail) = three
  tail-measurable deviation constraints: (G1) `pre a n > caThr`; (G2) `∀ i, a i ≤ 2C·log n`;
  (G3) `∀ r∈[1,n], winThr C n r ≤ sufSum a r`. The witness cut is the **first-passage** `k = (least k
  with sufSum a(k+1) > T) − 1` via `Nat.find`. `globalGood_subset_mainEvent` needs `0 ≤ caThr` (a
  large-`n` fact, threaded as a hypothesis and delivered by the caller's `n₀`).
- `error_l1_high_bound` wraps: `sum_abs_syracZ_sub_mainHigh_eq` (→ `P(¬mainEvent)`) → pointwise
  `Summable.tsum_le_tsum` via the inclusion (`¬mainEvent ⊆ ¬globalGood`) → `prob_not_globalGood_le`.

**Abandoned mid-lap (deliberately discarded, NOT committed):** I started the union-bound skeleton for
`prob_not_globalGood_le` — a pointwise lemma `globalGood_not_mask_le` (`¬globalGood`-mass ≤ sum of the
three deviation-indicator families) using `winThr` + `Nat.find`-free `simp only [globalGood, not_and_or,
not_forall, not_imp, not_le, not_lt]` to split `¬globalGood` into the G1/G2/G3 disjunction, then
`Finset.single_le_sum` to lower-bound each family by the firing term. **The design is right**; it hit
compile errors (an "Ambiguous term" around `set P`, and `split` failing on the `Finset.sum_nonneg`
lambdas) that I ran out of runway to fix. Redo it fresh — it's ~60 lines and the route below is the map.

## ✅ State
- Build 🟢 green: full `lake build` = 3292 jobs (observed this session at commit `a52d4ad`; pre-commit
  hook re-verified). Judge's `d84eae7` sits on top with my work intact.
- Sorry census **4**: `Statement.lean` ×2 (headline stubs, frozen), `Sec5/FirstPassage.lean:81` (C9
  `stabilization`), `Sec6/MixingError.lean:543` (C10 `prob_not_globalGood_le`).
- Axiom-clean (verified via `#print axioms` this session): `globalGood_subset_mainEvent`,
  `mainPieceEvent_of`, `sufSum_succ_le_add`. `error_l1_high_bound` = trust base + `sorryAx` (only via the
  one remaining tail sorry).
- Tree clean.

## 🎬 Next actions
1. **Prove `prob_not_globalGood_le`** (`Sec6/MixingError.lean:543`). Statement:
   `∃ C>0, ∃ n₀, ∀ n m, m≤n → n₀≤n → 9n≤10m → 0 ≤ caThr (caConst A) n ∧ 2·∑' a, (¬globalGood-mask) ≤ C·m^{-A}`.
   Route (hardest-first), also in `PENDING_WORK.md` top entry:
   - **(a) Union-bound skeleton** — redo `globalGood_not_mask_le` (pointwise) then a tsum assembly:
     `∑' a, ¬gg-mask ≤ P(¬G1) + Σ_{i<n} P(G2_i) + Σ_{r∈Icc 1 n} P(G3_r)` via `Summable.tsum_finsetSum`
     (finite-sum ↔ tsum swap) + `Summable.tsum_le_tsum`. Pure tsum; no measure theory. **Land this first**
     as a standalone green lemma — it's the safe, self-contained brick.
   - **(b) Marginal prerequisite** (the flagged hard piece): `(geomHalf.iid n).map (fun a => pre a r) =
     iidSum geomHalf r` for `r ≤ n`, and the suffix analogue for `sufSum`. Lets each per-event masked
     tsum become `∑' L, if dev L then (iidSum geomHalf r) L else 0`, which `geomHalf_tail_bound`
     (`Prob/LocalInstances.lean:540`) bounds. Check `Prob/Basic.lean` (`tsum_iid_succ_mul`,
     `iid_support_coord`), `Syracuse/SyracRV.lean`, `Syracuse/ValuationDist.lean` for existing marginal
     machinery before building from scratch. This is the genuine multi-lap content.
   - **(c) tail arithmetic**: G3 at `λ≈C√(r log n)` ⇒ `Gweight ≈ n^{−cC²}`; `caConst_tail_exponent`
     (`A+3 ≤ C/400`, `MixingCore.lean`) is the exponent room. G2: single coord (`r=1`), `λ≈2C log n`.
     G1: linear deficit, exp-small. `0 ≤ caThr`: `n log₃/log2 ≥ C² log n` for large n (reuse the
     `log n ≤ 2√n` machinery scoped for `lRange_hbudget`'s `hwin`). n→m: `m≤n ⇒ n^{-A} ≤ m^{-A}`, and the
     ≤n-term sum × `n^{−cC²}` absorbs (cC² ≫ A+1 at C=30).
   Decompose freely into named sub-sorries as you build (raising the src count this way is PROGRESS).
2. When C10 lands axiom-clean, **pin C8** (Prop 5.2 / (5.8), §5 pp.22–25) — statements copy-not-compose,
   verbatim against the PDF, `RATIFY-C8` comments, **never set `\leanok`**. See DIRECTION objective order.

## ⚠️ Gotchas
- `tsum_le_tsum` is **`Summable.tsum_le_tsum`** (dot form): `hf.tsum_le_tsum (∀ i, f i ≤ g i) hg`. The
  bare `tsum_le_tsum` is not in scope here.
- `t <;> [t1; t2]` (bracket goal-splitting) is **not valid** in this Lean; use `t <;> first | t1 | t2`.
- Don't touch watched statements (`fine_scale_mixing`, `stabilization`) or any ratified pin — decompose
  BELOW them only. `condWindow` is an enlargement of `Eₖ`; never write "equal".
- The reversed tail block means Tao's `a[1,k]` is `pre vt p − pre vt 1`, not `pre vt (p−1)` (a real bug
  once compiled green). Suffix-sum reasoning goes through `sufSum`/`pre_cast_tail_prefix`.

## 📁 Key files
- `TaoCollatz/Sec6/MixingError.lean` — the C10 error node; my new lemmas (~lines 140–528) + the open
  `prob_not_globalGood_le` (543).
- `TaoCollatz/Sec6/MixingCore.lean` — `condWindow`, `stopEvent`, `caThr`, `lRange`, `caConst`,
  `caConst_tail_exponent`, `lRange_hbudget*`.
- `TaoCollatz/Prob/LocalInstances.lean:540` — `geomHalf_tail_bound` (the per-event tail).
- `TaoCollatz/Prob/Basic.lean`, `Prob/LocalBound.lean` — `iid`, `iidSum`, `tsum_iid_succ_mul`.
- `PENDING_WORK.md` (top entry), `DIRECTION.md` (corrected campaign order) — read both first.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. Read
DIRECTION.md's CURRENT DIRECTIVE, then pick up at Next action #1: prove `prob_not_globalGood_le`,
starting with the union-bound skeleton (1a) as a standalone green lemma.**
