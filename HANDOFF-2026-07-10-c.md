# HANDOFF — 2026-07-10 (third box session, laps 11–17)

**Branch**: `main`, HEAD `9e322af`. Tree clean (only untracked/ignored `.obsidian/`
noise), `lake build` green, every newly proved item `#print axioms` =
`[propext, Classical.choice, Quot.sound]`. Nothing uncommitted.

## What happened (by lap)

11. **`ecc2ce1`** — `hold_weight_expect` PROVED ((7.43) Geom(4) expectation leaf):
    marginalization chain `hold_map_fst` → `hold_fst_marginal` → `hold_tsum_fst`
    (Holding.lean), `geomQuarter_toReal/_tsum_toReal/_summable_toReal/_tail`
    (Geometric.lean), three-region split in Monotone.lean.
12. **`5885cb2`** — `Q_white_case1` PROVED (Prop 7.8 Case 1 proper, (7.41)–(7.43)):
    `Q_rec` white step + `Q_le_Qm` per hold-atom + `hold_weight_expect`;
    `exp(-ε³)·exp(ε³/2) = exp(-ε³/2)`. (+ `5d0792b` lint.)
13. **`455ee1a`** — harness `check12`: the (7.36)-bridge, pascal-column DP vs
    hold-jump `Q` DP, agreement ~1e-11 incl. amplified damping (judge item 9,
    the LAST open judge follow-up — all retired now).
14. **`20eb928`** — `Sec7/Unroll.lean`: `Qstop` + `Qstop_eq` PROVED — the (7.45)
    first-passage unrolling, D6 form (height-budget recursion, `hold` snd ≥ 3 via
    `hold_support_snd_ge`). Entry point for Case 2 (X8) and Lemma 7.9 (X9).
15. **`2044b1e`** — `prop_7_8` ASSEMBLED: sup split (interior → `le_Qm` at `m-1`;
    edge white → `Q_white_case1`; edge black → NEW named sorry `Q_black_edge`).
16. **`6e28488`** — `Q_polynomial_decay` ((7.37)) PROVED by forward induction
    (`Qm_le_rpow` base below `Cb := max C0 1`, `prop_7_8` step above).
17. **`9e322af`** — `Sec7/Bridge.lean`: `Rcol` column recursion +
    `renewal_white_encounters` (Prop 7.3, MOVED here from Holding.lean) PROVED
    modulo three named X5 sorries; analytic tail fully proved.

## The open ledger on the §7 probability side (post-session)

The entire chain Prop 7.3 → Prop 7.1 → statement now rests on FOUR named sorries:

1. **`Q_black_edge`** (Monotone.lean) — (7.41) for black edge starts = Cases 2–3
   (X8/X10). Route in its docstring: `Qstop_eq` (proved) with `s := l_Δ - l`;
   needs **Lemma 7.7** (2D renewal Chernoff — the hard probabilistic kernel),
   the (7.50)/(7.51) white-exit constant (consumes proved `black_structure`;
   MC ≈ 0.987 in harness check9), and Lemma 7.9's induction (X9) for the deep case.
2. **`bridge_vector`** (Bridge.lean) — iid-Pascal expectation = `Rcol 0 0`.
   Mechanical: induction peeling `Fin.cons` through `PMF.iid`/`expect`;
   `pre (cons a v) (i+1) = a + pre v i`; `Fin.succ` filter reindex.
3. **`hold_tsum_step`** (Bridge.lean) — (7.29) one-column self-similarity of `hold`
   (tsum/ℝ≥0∞). Split `geomQuarter` at `k = 1` (memorylessness
   `(1/4)(3/4)^{k-1} = (3/4)·(1/4)(3/4)^{k-2}`), peel one `pascalNe3` off
   `PMF.iid`; `pascal b = (3/4)·pascalNe3 b` for `b ≠ 3`.
4. **`bridge_renewal`** (Bridge.lean) — `Rcol j l = Σ' d, hold(d)·Q((j,l)+d)`.
   Downward induction on `half - j`: unfold `Rcol` one column, apply
   `hold_tsum_step` to the RHS, match branches via `Q_rec` at `b = 3` landings.
   Boundary `j ≥ half`: RHS = `Σ hold = 1` since `d₁ ≥ 1` pushes past the strip.

All three bridge sorries are numerically validated end-to-end (`check12`, ~1e-11,
incl. amplified damping where a coordinate bug would be O(1)).

## NEXT (in order)

1. `hold_tsum_step` — most mechanical; unlocks `bridge_renewal`.
2. `bridge_renewal` — closes half of X5.
3. `bridge_vector` — Fin/filter algebra; closes Prop 7.3 fully (modulo Q side).
4. **Lemma 7.7 D6 statement design** — the Chernoff bound for the first-passage
   endpoint of the `Qstop` overshoot branch. This is the real wall for
   `Q_black_edge`; start by stating the endpoint-distribution lemma over the
   `Qstop` recursion (no infinite sequences; mirror the `Qstop` branch structure).
   Paper: Lemma 7.7 (p.42–44, (7.30)–(7.33)), Gaussian-type upper bound `G_k`.
5. Parallel threads if blocked: `key_fourier_decay` reduction chain (X1/X2),
   S3 d=1 negative-binomial (`negBinomial_apply`, `pascal_eq_map_iid` in
   Geometric.lean), S-chain upstream sorries.

## Gotchas this session (beyond the corpus)

- `Summable.tsum_le_tsum` is dot-notation on the FIRST summable: `hf.tsum_le_tsum hle hg`.
- Sup-subtype omega trap: after `obtain ⟨⟨p1,l⟩,h1,h2⟩ := p` the GOAL still contains
  `(⟨(p1,l),_⟩).1` — add defeq `have`/`show` bridges before omega/le_Qm.
- Standalone `have h := Q_le_Qm ...` needs explicit `(l := l)` (unconstrained implicit).
- `tsum_add` is gone; use `(hf.add hg).tsum_add` / `Summable.tsum_add`.
- `ring` inside `congr 1` on `Real.exp` args can fall back to `ring_nf` with a
  noisy info — write `ring_nf` directly.
- `Function.Injective.hasSum_iff` + `add_right_injective` for geometric tail
  reindexing; `hasSum_geometric_of_lt_one` + `norm_num at h` for the sum value.
- `PMF.map_bind`/`PMF.map_comp`/`PMF.map_const`/`PMF.bind_pure` compose cleanly for
  marginals; `Prod.fst ∘ (fun v => (k, c v))` is defeq to `Function.const _ k`.
- `open Classical in` goes BEFORE the docstring, not between docstring and theorem.

## State

`src` sorry count: statement chain + 4 named §7-probability sorries above
(net: Prop 7.3/7.8/(7.37) heads all CLOSED as implications). No `wip/`, no axioms,
no `native_decide`. Harness: 12 checks, all pass (~30 s run).
