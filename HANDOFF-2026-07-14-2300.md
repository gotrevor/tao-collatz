# Handoff: Lemma 1.12 `syracZ_recursion` PROVED — SyracRV.lean fully sorry-free & axiom-clean

**Date**: 2026-07-14 ~23:00 UTC · **Branch**: `main` · `lake build` green (3282 jobs), tree
clean after commit. **Re-read `DIRECTION.md` first each lap — it outranks this handoff.**

## 🎯 What happened this run (objective 3 = fruit; §7 crux already DONE)
Closed the **last SyracRV stub**, `syracZ_recursion` (paper Lemma 1.12) — the genuine ZMod
number-theory crux flagged over the last five laps. `Syracuse/SyracRV.lean` now has **zero
sorries** and is axiom-clean (`#print axioms syracZ_recursion = [propext, Classical.choice,
Quot.sound]`). All three paper-cited SyracRV identities (1.21, 1.22, Lemma 1.12) are proved.

## 🧩 New machinery (all in `Syracuse/SyracRV.lean`, all reusable, all axiom-clean)
1. **`cast_Ghat`** — `castHom_{3ⁿ⁺¹→3ⁿ}(Ĝ w) = Gₙ w` (level-`n` offset truncation; `k=n`
   case of `syracZ_map_cast`'s truncation, `w` used directly, no `castLE`, no vanishing tail).
2. **`three_mul_eq_iff`** — `3·A = 3·B ↔ castHom A = castHom B` in `ZMod 3ⁿ⁺¹ → ZMod 3ⁿ`
   (the `3·ZMod 3ⁿ⁺¹ ≅ ZMod 3ⁿ` iso). Proved via `∀C, 3·C=0 ↔ castHom C=0`, both ⟺ `3ⁿ∣C.val`.
3. **`syracZ_fiber`** — THE crux (~90 lines). `∑' w,(iid n) w·[Gₙ₊₁(cons a₀ w)=x]
   = if (2^{a₀}x.val)%3=1 then syracZ n arg else 0`. Head-peel (`syracZ_offset_peel`) → unit
   `2^{a₀}` → guard by mod-3 reduction → divide-by-3 via `three_mul_eq_iff`+`cast_Ghat`.
4. **`syracZ_recursion` assembly** — `PMF.map_apply` → `PMF.tsum_iid_succ_mul` (peel a₀) →
   `syracZ_fiber` → `geom_fold_geomHalf` (the 5 cores from lap fruit-5 plug straight in).
   `f`-periodicity (period 2·3ⁿ): guard via `2^P≡1 mod 3`; value via `two_pow_period`.

## 📋 Remaining non-headline sorries (whole repo)
- `Sec5/FirstPassage.lean:81` `stabilization` (Prop 1.11) — HEROIC analytic (multi-lap; narrow only).
- `Sec6/MixingFromDecay.lean:19` `fine_scale_mixing` — HEROIC analytic §6 (multi-lap; narrow only).
- `Statement.lean:22,28` — the two headlines (discharge only when the whole chain lands; DO NOT TOUCH).

## ⚠️ Gotchas banked this run
- `rw [pow_add]` (and `pow_mul`) will greedily rewrite `3^(n+1)` hiding in `x.val`'s TYPE
  (`ZMod (3^(n+1))`), throwing "motive is not type correct". Give explicit args:
  `rw [pow_add (2 : ZMod (3^(n+1))) a P]`. Same class of failure as rewriting inside an `ite`
  whose `Decidable` instance depends on the term — use `by_cases`/`if_pos`/`if_neg` there.
- `rw [hφ]`-style motive failures on `k^(n+1) ∣ …` : `generalize C.val = v` first, THEN `rw`.
- `if_congr` produces an ite with a fresh `Decidable` instance ≠ `PMF.map_apply`'s
  `Classical.propDecidable`; a `show`/`rfl` bridge fails on the instance mismatch. Do the
  case-split with `by_cases … ; rw [if_pos h, if_pos h, …]` — `if_pos`/`if_neg` take the proof
  and unify the instance, sidestepping it entirely.
- `PMF.tsum_iid_succ_mul` (and `PMF.map_apply`) need the `PMF.` prefix even inside `namespace
  TaoCollatz`.

## 📁 Key files
- `TaoCollatz/Syracuse/SyracRV.lean` — **now sorry-free**; `cast_Ghat`, `three_mul_eq_iff`,
  `syracZ_fiber` added before `syracZ_recursion`; module docstring updated.
- `PENDING_WORK.md` — lap fruit-6 entry (newest) has full route + gotchas.
- `DIRECTION.md` — judge pass 26 (authoritative; objective 3 active).

---
**→ Next session: the two HEROIC §5/§6 analytic stubs (`stabilization` Prop 1.11,
`fine_scale_mixing`) are the only spine sorries left — narrow, don't expect one-lap closure.
Decompose `stabilization` into named sub-lemmas first. Or pick judge objective-3 fruit
(ManyTriangles split, Pin C8). Re-read `DIRECTION.md` first.**
