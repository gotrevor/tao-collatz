# Handoff: ship the explicit-`c` PR (tao-collatz effective-constants campaign)

**Date**: 2026-07-16 18:10 EDT · **Branch**: `explicit-rate-hold-weight` (worktree `~/src/tao-collatz-explicit-c`)

## 🎯 What we're doing

Ship a PR to `gotrevor/tao-collatz` that **proves the explicit exponent `c` of Theorem 3.1 in Lean**: `cTao := 1 / (640000000 * Real.log 2)` + `theorem tao_collatz_quantitative_explicit` — the first published explicit exponent for Tao 2019 Thm 3.1 (MO 341570, open since 2019). The treadmill campaign (2 fable/low laps, ~2h) did ALL the Lean work and is **stopped**; what remains is the host endgame: **code review, verification, PR**. Trevor promised this publicly on Zulip (answering Martin Dvořák), so it has an audience; draft PR #6 (the hand-traced notes) is merge-gated on this landing.

## 🧠 Context to carry forward

- **Design (settled, don't re-litigate):** ONE `Statement.lean` (no sibling file) — appended `cTao` + the explicit theorem beside the two paper headlines; provenance in docstrings (paper's vs our augmentation). Comparator: `Challenge.lean` declares `cTao` **byte-identical** + the theorem sorry-by-design; `theorem_names` grew to 8; `Solution.lean` deliberately untouched (imports the development). `C` stays existential — out of scope.
- **cTao's value was operator-signed-off** after spot-checking the box's step-1 branch trace (4 citations verbatim at `4fb5304`; c8 contains c7 via `approx_passtime_window`; cs bottoms at 1/5; the headline's `c` IS the c7 value). A wrong value couldn't have compiled — `c₀ ≤ branch` obligations are compiler-adjudicated.
- **Method**: sibling+delegate de-existentialization of the whole c-path (Prob/Syracuse leaves → Sec5 c7/c8/cs chains → Sec3 glue → `tao_collatz_quantitative_spine_explicit`); every original `∃`-statement byte-identical (box ran `tao_stmt_diff.py` per commit, 31/31). `check16` (`005ac86`) added an exact-arithmetic numeric trap for the cTao min-tree.
- **State-file incident (resolved)**: a host dry-run deleted the live treadmill's state file (tool bug, fixed in `~/personal/bin/lean-treadmill`, committed; state hand-restored). Treadmill tooling is trustworthy again.
- **Docs style ruling this session**: no quote-as-warrant — operative docs state reasoning, never "Trevor said" as justification.

## ✅ State (all observed this session)

- Working tree **clean**; HEAD `18026b2`; treadmill **stopped**; `lean-sorry -c TaoCollatz` = **0**.
- `tao_collatz_quantitative_explicit` present in `TaoCollatz/Statement.lean`, `Comparator/TaoCollatz/Challenge.lean`, `config.json` (verified by grep).
- Box's final commits: `4aebe01` (3a+3c: claim discharged), `9b4833a` (3b: comparator), `005ac86` (check16 trap), `18026b2` (campaign-complete handoff). Box reported axiom gate ✓ per its handoffs — **re-verify host-side; grep ≠ compiler**.
- Remote branch is at `7c9c494` — the box's Lean commits are **NOT pushed yet**.
- Earlier today (merged): PR #8 (explicit thresholds in `hold_weight_expect` — the `C`-side rate-free fix).

## 🎬 Next actions

1. **Push the branch** (`git-safe push` from the worktree) — CI (`build` + `comparator`) then runs free on the box's commits; watch both.
2. **Host verification**: `lean-axiom-gate --target TaoCollatz.tao_collatz_quantitative_explicit --target TaoCollatz.tao_collatz_quantitative --import TaoCollatz --exact .` (expect exactly the standard three). Run `tools/tao_stmt_diff.py` and `tools/check_blueprint.py` if quick.
3. **Code review** the campaign diff (`git-safe diff main...HEAD`): `/lean-review` for smells (new `maxHeartbeats`, `native_decide` must stay ZERO, deprecations); human-read `Statement.lean`'s appended block (provenance docstrings, header, the two paper headlines byte-identical) and `Challenge.lean`'s new entry (cTao byte-identity with Statement.lean's def).
4. **Open the PR** (`gh pr create --repo gotrevor/tao-collatz --head explicit-rate-hold-weight ...`), body: what's proven, evidence tiers, comparator as external verdict, link PR #6 + MO 341570. Squash-merge on green is the repo norm; PRs on own repo are fine, auto-merge was Trevor-blessed for #8 — confirm with him for this one since it's the headline claim.
5. **Follow-ups**: update PR #6's note (blocker fixed by #8; `c` now kernel-certified — several "hand-trace only" sentences are stale; also its `exp(exp(5e16))` should be `7e16` per its own tool). Draft the Zulip reply to Dvořák (**Trevor posts, never Ren**). PR #6 merge is Trevor-gated.

## ⚠️ Gotchas

- The 8 sorries in `Comparator/` are **by design** (challenge statements); `set_option warningAsError false` covers them. Only `TaoCollatz/` must be sorry-free.
- `scripts/comparator-probe` has a known walks-nothing defect (open todo) — **CI comparator is the real check**, not the probe.
- The box's git-door 127s are fixed two ways (box-context + entrypoint PATH, image rebuilt) — ignore any stale references in mid-campaign handoffs.
- `main` is protected: PR-only, squash, required checks `build` + `comparator` (strict — branch must be up to date; it is, post-#8 merge was reconciled).
- Trevor's usage today was heavy (Fable) — prefer cheap checks before expensive ones.

## 📁 Key files

- `DIRECTION.md` / `PENDING_WORK.md` / `HANDOFF.md` — campaign directive, step ledger + step-1 trace (JUDGE-FLAG), box's final state. Archive-worthy on merge, like the main expedition did.
- `TaoCollatz/Statement.lean` — the trusted surface incl. the new claim. THE review target.
- `Comparator/TaoCollatz/{Challenge.lean,config.json}` — external-checkability surface. Second review target.
- `~/personal/claude/knowledge/todos/open/tao-collatz-pin-constants-lean.md` — KB leaf tracking this; update on ship.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for instructions, and don't offer other projects from the KB - this doc IS the chosen thread. Absorb the context above, then start with Next action #1: push the branch and let CI start grinding while you verify and review.**
