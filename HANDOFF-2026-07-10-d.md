# HANDOFF — 2026-07-10-d (fourth box session, laps 18–21)

**Branch**: `main`, HEAD `c32b37b`. Tree clean, `lake build` green, every newly
proved item `#print axioms` = `[propext, Classical.choice, Quot.sound]`.
Nothing uncommitted.

## What happened (by lap)

18. **`a48b7f4`** — `hold_tsum_step` (7.29) PROVED: geomQuarter memoryless split
    (`gq(k+2) = (3/4)·gq(k+1)`) + one `pascalNe3` iid-peel + Fubini; the mass
    bridge `(if b=3 then 0 else pascal b) = (3/4)·pascalNe3 b`. New ℝ≥0∞ PMF
    calculus in `Prob/Basic.lean`: `tsum_bind_mul`, `tsum_map_mul`,
    `tsum_iid_succ_mul`, `tsum_iid_zero_mul`; `hold_tsum_expand` factored out.
19. **`9c8c29f`** — `bridge_renewal` (7.27)≡(7.28) PROVED: downward induction on
    `half - j`; boundary = mass 1 past the strip; interior = `Rcol` one-column
    unfold, `b = 3` landing matched against `Q_rec` via
    `Summable.tsum_eq_add_tsum_ite`, tail against `hold_tsum_step_real` (new
    real corollary for [0,1] observables). `bridge_renewal` gained `0 ≤ ε`.
20. **`395e35b`** — `bridge_vector` PROVED → **X5 FULLY CLOSED**;
    `Sec7/Bridge.lean` sorry-free. `bridge_vector_gen`: induction on remaining
    columns via new `PMF.expect_iid_succ`/`expect_iid_zero`; count split at head
    column (`Fin.sum_univ_succ` + new `pre_cons`); `whiteSet` ↔ 0-based `white`
    adapter at `(n/2, 0, 0)`. **Prop 7.3 (`renewal_white_encounters`) is now
    fully proved modulo the single sorry `Q_black_edge`** (checked: its sorryAx
    traces only through `Q_polynomial_decay` → `prop_7_8` → `Q_black_edge`).
21. **`c32b37b`** — Lemma 7.7 D6 layer in `Sec7/Unroll.lean`:
    `fpDist : ℕ → PMF (ℕ × ℤ)` (first-passage endpoint, budget recursion,
    normalization free via PMF combinators), support lemmas
    (`fpDist_support_fst_pos`, `fpDist_support_snd_gt`), and
    `Q_le_fpDist_expect` — the (7.45) inequality `ofReal (Q j l) ≤
    Σ' e, fpDist s e · ofReal (Q (j+e₁) (l+e₂))` (ℝ≥0∞ form, PROVED, ∀ budget s).
    `Gweight` (2.2) defined; **`fpDist_location_bound` = Lemma 7.7 is the new
    named sorry (X6)**. MC sanity: mode j ≈ s/4, l ≈ s+O(1) ✓.

## The open ledger on the §7 probability side (post-session)

The whole chain Prop 7.3 → Prop 7.1 → statement now rests on TWO named sorries:

1. **`Q_black_edge`** (Monotone.lean) — (7.41) black-edge = Cases 2–3 (X8/X10).
   Entry inequality `Q_le_fpDist_expect` now PROVED; what remains is the paper
   pp.46–48 assembly: (7.46)→(7.47) split, (7.49) the `E exp(O(A log m / m)·j_k)`
   bound, and (7.50)/(7.51) the ≫1 white-exit probability (consumes proved
   `black_structure` + Lemma 7.7). Case 3 additionally needs Lemma 7.9 (X9).
2. **`fpDist_location_bound`** (Unroll.lean) — Lemma 7.7, the hard kernel (X6).
   Paper route: union bound over last step + Lemma 7.6 (Hold exp tail) +
   Lemma 2.2 (2-D local Gaussian bound for iid Hold sums = node S3; D5 route:
   exponential tilting + circle method, no contour integration).
   NOTE: `fpDist` has no k-index — reformulate the paper's k-summed union bound
   as induction on `s` with the Gaussian weight as invariant ((7.33) is already
   k-summed, which suits this).

## NEXT (in order)

1. **Lemma 7.6 ingredients** (tractable): Hold exponential tail. Concretely:
   a 1-D tail for `fpDist`'s marginals or directly `∃ c C, (hold d).toReal ≤
   C·exp(-c·(d.1 + d.2.toNat))` — from `geomQuarter` mass exact formula +
   `pascalNe3` MGF finite products ((7.30)). This is what the union-bound step
   of 7.7 consumes.
2. **Case 2 assembly over the proved entry**: state the (7.47)-shape bound
   `Q(j,l) ≤ Q_{m-1}·E[exp(-ε³/2·1_W(endpoint))·max(m - j_k,1)^{-A}]` as a lemma
   consuming `Q_le_fpDist_expect`(+ one more `Q_rec` step at the endpoint for
   its damping) + `Q_le_Qm` + (7.42) `max_pow_bound` (exists? check Monotone).
   This isolates exactly what (7.49)+(7.51) must supply.
3. **S3 / Lemma 2.2** (the multi-lap wall): start with the d=1 leaves already
   stated in Geometric.lean (`pascal_eq_map_iid`, `negBinomial_apply` — both
   sorried, good grind targets) then the 2-D tilting integral.
4. Parallel if blocked: `key_fourier_decay` X1/X2 chain (Holding.lean sorry).

## Gotchas this session (beyond the corpus; also in PENDING_WORK.md)

- `(3 + ∑ i, v i : ℤ)` elaborates as cast-of-sum OR sum-of-casts depending on
  context; spell `(3 : ℤ) + ∑ i, (v i : ℤ)` to match `hold`'s def (NOT defeq!).
- `Fin.cons_zero/_succ` dependent motive: pass `(α := fun _ => ℕ)` or use `rfl`
  (cons at literal `⟨0,h⟩`/`⟨i+1,h⟩` reduces definitionally).
- `congr 1` after `Fin.sum_univ_succ` closes the i = 0 head **definitionally** —
  don't write a bullet for it ("no goals" error otherwise).
- `if_congr` with `refine ?_` holes → stuck Decidable metavars; build the `Iff`
  in a `have` first, then `exact if_congr hiff rfl rfl`.
- `unfold PMF.expect` then `dsimp only` to beta-reduce before `rw [← tsum_mul_left]`;
  `rw` with instantiated higher-order lemmas leaves `(fun x => ..) y` redexes.
- `simp only [Nat.cast_ofNat]` runs simprocs too: `3 = 3` becomes `True` — use
  `⟨trivial, hW⟩` not `⟨rfl, hW⟩` after it.
- WF-recursive defs over PMF combinators (e.g. `fpDist`) need `if h : _` (dite)
  so `decreasing_by` sees the guard; plain `ite` loses it.
- rw `show (1:ℕ) = 0 + 1` explodes when `1` also occurs in a type index
  (`Fin (m+1)`) — motive not type-correct; prove the instance directly instead.

## State

`src` sorry count: statement chain + `Q_black_edge` + `fpDist_location_bound`
(+ pre-existing upstream S/X leaves: key_fourier_decay, pascal_eq_map_iid,
negBinomial_apply, dTV lemma, SyracRV ×3, ValuationDist ×2, Sec5 ×2, Sec6,
White, Decay, Statement ×2 — unchanged this session). No `wip/`, no axioms,
no `native_decide`. Prob/Basic + Bridge + Unroll carry the new reusable calculus.
